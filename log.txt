
2023-08-07:
- I moved the OS out of the computie repository and into its own, with the git history preserved
- It will now be known as gloworm, named after the 1980s toy, since my Rust OS was already named Ruxpin after Teddy Ruxpin, so I
  tried to stick to the same theme

2025-01-31:
- the previous time I was working on in this repository was November/December 2023 when I tried to get the monitor and kernel
  compiled in clang, and booted it using Moa.  I ran into the common bug where the MOVEA instruction is assumed to set the flags
  register, but it's the one exception where the flags don't get updated, so a comparison doesn't work.  And it usually affects the
  fputs implementation causing it to print all of memory starting with the welcome message, but never terminating at the null
  character.  I never got around to submitting a bug report
- I've started working on this again since Christmas when I built the k30p-VME, MemBird Woodcock, and CF Card.  I added some commands
  to the monitor to test the VME bus accesses, and test the CF card, and I've since managed to get the kernel booting on the k30p
  and run the network stack

2025-02-01:
- I've been trying to add a config system so that it's easier to conditionally compile things, and linux's config system looks
  simple.  I could try to learn cmake and/or ninja or something, but then anyone building it would have to install those too, and
  they don't look too straightforward for the kind of config I'd like
- Kconfig uses a simple file format for describing config settings and their relations, which generates a single file with all the
  config settings in them.  The config programs are distributed with debian, so they don't have to be included here, and even then,
  the output config file with eg. `CONFIG_M68K=y` is easily human readable and modifiable, it's valid make and bash syntax, can be
  parsed easily.  You do have to make sure the dependecies are satisfied when editing the output by hand, but that's minor.
- A question is whether it's best to try to match the linux config item names, or whether to change some names, like BLK_DEV_RAM
  calling RAMDISK or something instead.  It probably doesn't matter much since I'll eventually write Kconfig files with
  descriptions
- linux seems to not use very many non-bool config items, which makes sense, but I'm thinking of using many more if I can, for
  parameters like device sizes and maybe address offsets for I/O register addresses.  I can't seem to find where linux configures
  those, if it has them configurable at all.  For a lot of systems, they'd all be in the same location, but for me, the ATA device
  on computie is 0x600000 but on computie-vme with the COMET CF Card, it's 0xff800000 or anything in the 0xffxxx000 range I think

2025-02-04:
- there's a lot of issues to fix, but it at least seems to be compiling the kernel and the monitor enough to boot them
- it turns out I was looking in tools/build/Makefile.build in the linux source, but it was scripts/Makefile.build that has the
  more complex build system, which has multiple special variables for targets, and it treats them somewhat specially, so I figured I
  should do the same so I have more control over what gets built how instead of over-generalizing
- I added the userprogs variable with pretty much the same rules to separate out single source file programs from multi-file ones,
  but I haven't tested the multi one yet
- it's building the final step when it shouldn't, and I still need to add the libc makefiles

2025-02-07:
- finally get the new build system implemented.  I had to make some special cases with subdir-y and others to make it build the
  right things, or else it would get confused by too many generic rules.  I still don't have it stripping the binaries for the
  commands yet, but that will come soon

2025-02-08:
- I brought out the k30 and 68k-SMT boards to test the builds on those.  Initially, neither board wanted to boot up.  After a while
  of trying, I finally got the 68k-SMT to run, but it was getting a lot of mistaken characters, not entirely garbage but adacent
  lower case letters, so I guess it was the lower data bits that were getting corrupted occasionally.  It seemed to run ok, printing
  the help text and such, but the characters printed and received were wrong, so maybe something localized to the serial controller
  or FTDI
- the k30-SBC just wouldn't do anything, and then it would print the welcome message but not the prompt, and then it would print the
  prompt but not accept characters, and then it would accept characters but hang when enter was pressed.  Finally I tried a
  different USB port instead of the one on the monitor stand, I used the one connected by the USB3 type-A extension cable and it
  worked
- I was eventually able to get both builds of the kernel and monitor running on them with the ATA drivers using the CONFIG_ATA_BASE
  select as the only difference, so the new build system should work with those boards

2025-02-16 [from retroverse logs]:
- I'm kind of getting sidetracked now, I tried to compile the kernel without -O3 in order to test if the optimizations are causing a
  problem, after reading in the computie logs that I had to add a volatile to strlen in order to get the monitor working previously
  because otherwise it was hanging.  It's very possible a similar thing is happening here with the kernel, so I thought it would be
  quick to check, but then I ran into a problem with undefined fwrite, which is almost certainly related to the wrong object files
  being added to the lib because it compiles both versions of libc.  The only place that fwrite exists that would cause the
  undefineds is in fputs, which is used by fputs in os/ but not in none/
- I compiled it with just the kernel and it worked without errors because it was monitor that had the problem
- trying to load it over serial caused the k30 to reset and run the monitor and start printing out the data instead of reading it
  in.  It was working before I used the unoptimized kernel.  *sigh*  I feel like I had this recently with one of the 3 boards I've
  been playing with
- ok I just checked and it's 100% because the kernel overflows the size that the load script handles (16-bit length) so it loads the
  first ~0x1800 bytes and then boots, but there's more data to send, so I need to keep that in mind now that I have such a big
  kernel.  I could modify the format to use a 4 byte number instead, but I guess I'd have to upgrade the roms on the other boards?
  I'd rather not use a backwards compatible mechanism.

2025-02-17:

- when it works, it switches to the command process and runs that for a while, and before it finishing writing its output, it exits
  and switches to the shell process to handle the exit code, and then to idle again, which switches a couple of times before the
  command's output finishing printing.  It would seem odd that it doesn't have to flush the output before the process exits...
- when it doesn't work, it switches from the command process to the idle process without going through the shell process, and
  the suspend_proc call is made, so it's suspending without exiting.  One of the cases had it switch back to the same command
  process once, before switching to the idle process on the next timer interrupt
- it's calling suspend from the tty write function, which is really just a pass through to the 68681 driver. I don't know why it
  doesn't suspend in 68681 when the only way it could return 0 is if it suspends, but either way, it's not waking up after the
  buffer clears
- it honestly doesn't look hard to add a resume check for tx to the bottom half, or a flush function.  You should make one anyways
  to wait for output to be send before exiting.  I think that comment about never waking up in pipe either was more specifically
  about writing more than the total buffer at once rather than just remaining buffer, or it was before i had the bh stuff. I want
  to clean up the various parts of that

2025-02-18:

- an extra print statement shows it does return 0 like normal from tty_68681_write but it skips the suspend step because O_NONBLOCK
  is set, probably to make the tty driver work correctly, which is why tty_write is the one that suspends.  Either way, it should be
  possible to add a resume
- I added a very quick "if buf not full then call resume_suspended_procs" to the bottom half handler of the tty68681 driver.  At
  first I added one that passes in the tty68681 major device number, and that didn't work because it's the tty driver that suspends,
  but adding a second call that passes in the tty major fixed it up a treat.  It can now print the entire bin listing, suspending at
  least twice during the process, but being woken up both times once the buffer is empty.  So that was the entire problem with the
  listing hanging
- I don't know why I didn't do it earlier.  Maybe I just didn't like doing a double call to resume and wanted to instead put it in
  the tty driver.  It is true that's it's not the most clean, but it's not that unusual since the tty68681 driver is really a
  subdriver of the tty subsystem.  Maybe there's a cleaner way, but at least it doesn't hang anymore

2025-02-22:

- I finally got a refactor done that I had thought would be quick, but ended up taking me 2 days or so.  Even though the standard
  libc and unix are codependent, I kind of had hoped to separate the kernel arch includes from the libc ones, and went back and
  forth a bit, but eventually settled on having src/libc/arch/<arch>/include, and src/kernel/arch/<arch>/include rather than putting
  them together in include/.  I know the existing libc and kernel includes are mixed together, but it's only kernel stuff in
  include/kernel/ and the rest is libc.  Then again I'm still having my doubts
- I also changed the byteorder to_le16, etc functions into endian.h htole16, etc which is posix standard, so the things that use it
  will be more portable to another libc, which might be wanted in some cases
- also using the libc none with other baremetal code easily without needing the kernel

- I also fixed some build issues.  The CFLAGS weren't applying to all downstream directories like I wanted, and I also needed to
  refactor the arch-specific ones so I make a bunch of minor changes.  CFLAGS works now because it doesn't get overwritten in the
  Makefile.defaults file on every decent, and ccflags-y works only for the current directory
- I also added a userprog-ccflags-y, firmware-ccflags-y, and kernel-ccflags-y, which are activated by the respective directory's top
  level Makefile by transfering it to CFLAGS.  The idea is that anything in those categories should get certain general flags that
  are architecture-specific like `-mpcrel` which applies to monitor and boot (firmwares), but not userprogs or the kernel.

2025-02-28:

- I'm working on making it possible to generically register interrupt handlers so that other drivers can be made besides the serial
  driver, and I've had it working for a few days now, in theory, but I'm also re-evaluating how I do contexts, and how I can improve
  it.  I'd like to add the option of doing user mode switching, but there's a limited number of %usp instructions, so I'd have to
  %a6 instead of %a7 to push all the data onto the stack.  There's also the option of not using the stack, although it looks like
  linux is using the stack where on x86 I'm pretty sure it saves the registers in a struct in the process object.
- I'm also not too happy about the double table (the first being the real vector table, and the second being the irq_action table).
  Right now they're the same size, but the second one can at least cut out the exceptions since they're not overridable.  I haven't
  looked closely enough at linux to know how it's storing them, but I want it to be fast, since they're interrupts, so I might still
  stick to a limited table.
- I tried removing the interrupt disable instruction at the entry of the two interrupt handlers I have, and it works when the irq
  handler doesn't have it, but the trap handler does need it.  The 68k only masks the interrupts if an actual hardware interrupt
  occurs, and not on a trap, and I guess without the disable on entry to the trap, it could have another interrupt while the trap
  handler is saving the instructions.  There's only a limited set of trap instructions with specific irqs, so I could get away with
  having the two different types of handlers.  It's maybe splitting hairs over one instruction, but I do still think they'll need to
  be different anyways

2025-03-01:

- I almost have a modified context switcher that can either do what it currently did of always using Supervisor mode, or optionally
  switch to/from User mode, and which in Supervisor-only mode switches to the kernel stack before pushing all the registers, but
  it's not quite working
- I thought I'd try running it on moa last night to "quickly" find the problem, and of course moa isn't working with computie, even
  after changing the memory map to match the VME card.  I can't seem to find where the error is coming from.  It's Emulator(Misc,
  "%d0"), but that error is only created with "Error::new" and all instantiations have a complete error message instead of just the
  register name, so I'm stumped.  The code is such a mess from the time I tried adding emulator-hal.  I really need to pick it up
  and fix it, but I want to work on gloworm first
- ah! the trace function isn't even advancing the instruction, so no wonder it wasn't hitting the fault when tracing in moa

2025-03-02:

- I got thrown off by the fact that I had disabled ATA in the config, but also needed to change the boot stuff as a result.  I need
  to fix how it gets the wrong boot arguments, and also make the boot arguments default use the config value, so it's consistent

2025-03-15:

- I've been trying to rewrite the context switching code for two weeks now, and ...
- I just wandered off and did something else.  It's not that it's hard to do, but I keep just doing other things instead of working
  on it, when I set down to work on it.  Not sure if it's an effect of the medication, or a lack of interesting, or what.  But that
  said, I *do* want to only do it once, and I'm trying to incorporate everything, and read the linux m68k context switch code to
  figure out how it does it
- namely, in arch/m68k: entry.S and include/asm/entry.h
- interestingly enough, it seems to only save the higher numbered registers when it actually switches processes, and otherwise it
  doesn't touch them.  And it can do this because even though it enters the kernel's C code, as long as it doesn't actually change
  states until it's returned all the way up the call stack, out of each re-entry of the kernel, and is finally ready to return from
  the irq or syscall that entered the kernel, the stack will be exactly back to when it entered, and all the extra registers will
  have been restored as part of the C calling convention to preserve their values on the stack.  It will be as if it had just
  entered, and only pushed the scratch registers on the stack.  I'm pretty sure this is what was meant when I read that now all
  interrupts were the "fast interrupt" in Linux, as in it only saves the full context state if it's switching and otherwise only
  saves the minimum (ish) by default

- So, what I want is:
    - to be able to get the interrupt number that occurred (for the handler lookup table)
    - to configurably do what it currently does of only using the supervisor stack, or optionally using user mode
    - save the system call on the stack in a way that doesn't duplicate the values like it currently does
    - don't save all the registers unless a task switch is actually going to happen

- I think that's it.  The questions I still need to answer are: should I always save the systemcall registers, whether in a system
  call or not, or only when in a syscall instead of irq, and how do I know which is which when looking at the stack?
- also, should I keep my syscall register assignments or copy linux which uses %d1-%d5 primarily, still can't find a good reason why,
  Minix on Mac might use the same as I am, but it looks like it uses a movem to save %d0-%d2/%a0-%a1 instead of the weird order I
  have... this is kind of minor by comparison

- for the first question, I'm pretty sure linux saves all of %d0-%d5/%a0-%a2, which are all the syscall registers plus some (still
  don't know why %a2 specifically), but either way, every interrupt always saves those registers, so it doesn't try to be too fancy
  and have two ways of entering?  It looks like it.  That would certainly be easier
- ah, I have them in the slightly weird order they're in (%d0, %d1, %a0, %a1, %d2, %d3) based on the read and write syscalls, which
  (SYSCALL3) which takes an integer (%d1), and then a pointer and size (which is pointer-like).  Interestingly enough, I don't have
  any SYSCALL5's, even though I've defined it (and not 4).  Oh it's just not in unistd, but it's in socket.  And there it looks like
  it doesn't make much sense... "a" registers have int flags and the ints in %d2 and %d3 are the actual pointers

2025-03-16:

- Ohhh!  Linux is using a different kernel stack *for each task*.  That's why the context switch seemed a bit weird.  It pushes the
  basic registers to the kernel stack and I think it also keeps the exception frame on there as well.  That's why it doesn't seem to
  copy anything from the kernel stack to the user stack when switching tasks.  It just stores all the data on the kernel stack.
- I'm not sure why it's doing this yet (other than convenience, if you have lots of memory).  Unless there's a better reason, maybe
  I should avoid this to keep the memory usage down?
- what happens when a signal occurs?  I think that's the only weird case where you'd end up with 2 contexts on the user stack

- I'm kind of thinking, at least for now, I'll use only one kernel stack (per cpu, but only one cpu atm), and that kernel stack will
  always be in the ISP stack pointer, which is backwards compatible.  I won't make use of the MSP or the M bit for the time being.
- is it worth it to always switch the stack to the user stack, push the context, and then switch back to the same kernel stack and
  only push the rest of the context when switching, even though switching is probably going to be quite frequent.  Or should you
  push it to the kernel stack along with the exception frame, and copy it to the user stack when switching fully?
- or is it even best to push a0 to kernel stack, use it to push context to user stack, etc

2025-03-18:

- I think I will eventually move to a kernel stack per thread model, but for now I'll just do a single kernel thread and change
  things over more slowly.  I don't even have threads yet

2025-03-23:

- Making progress, but still don't have a working context switch that delays the switch until it's returning, and only switches if
  it's actually changing processes
- so a little while ago I figured out that there was an issue with returning from a syscall, and that was because the code that set
  that value is manipulating the context itself on the user stack.  This is a major point to note when changing the context layout.
  Previously, the register %d0 was at the top of the stack, but that's not the case now, if the extra regs are only pushed as
  needed.
- I caught this by make it do the full switch but using the new macros.  I had it working when the extra regs were pushed before the
  syscall regs, but not in the opposite direction.  Greping the code found places where proc->sp was being used, and that lead to
  finding the issue.  I moved that code to a function in syscall_entry.S so that it would be more obvious that that was happening.
  The code that actually set it was in schedule.c, so it's not supposed to know about the context format anyways
- the symptoms were that it would appear to hang when starting multitasking, but it was still context switching according to the LED

- I then tried to get the delayed switch working, and that's been a nightmare.  It usually gives a privillege violation, because the
  flags register ends up being 0 due to an invalid stack.  Using the moa emulator, I was able to figure out that (at least in some
  cases), it was doing the switch correctly the first few times, but not after that
- there is an issue with starting multitaskings since the context needs to not try to save the previous process, which is NULL.  I
  had some code to check if the previous_proc was null, but that was maybe causing more issues, so I disabled it, and that caused
  different issues
- one of the problems was that it was restoring the current_proc stack, and then saving the previous_proc stack, and not looking at
  the current_proc value again before popping, but if the check to see if the proc is the same is disabled, then it won't use the
  updated sp when popping current_proc, so for now I have it switch to current, switch to previous, and then switch back to current
- It was still having a bad switch somewhere, and moa with a breakpoint just before it starts the stack manipulation found that
  after 4 or so switches, it messes up, when trying to pop off a new stack.  The stack doesn't have the full context on it, so when
  the switching code does the swap, it pops the extra regs off the stack which aren't there and thus later on pops the wrong data
  during rte
- more grepping of the code didn't find the issue, because I didn't look at the code around it.  I tried setting a breakpoint on the
  signal code because that involved some stack manipulation, but that didn't get hit before the bad switch happened
- I can't quite remember how I clued into the process clone.  I might have been looking at new_proc and stuff, but somewhere along
  the line, I realized that the process cloning/fork was the problem.  It's cloning the current process which has an incomplete
  context on its stack, and so when it goes to actually switch procs, the new proc doesn't have the extra regs, only the syscall
  regs!
- fixing this made the privillege violation stop, so it's successfully switching, but it now hangs before the shell starts.  I
  *think* that might be because of the return value again.  I think the problem in general is that the context on the stack is not
  consistent.  If you're doing something with the stack of the current proc, then it's only got the syscall, and if you're doing
  something with any other process, it has the extended stack
- nope, trying to conditionally set the return value in the context doesn't fix the problem.  It seemed to instead make context
  switching halt.  When it's swapped from what it should be, it continues context switching and hangs before starting the shell

- ok so after a bunch of debugging, I realized that since the clone function is just subtracting some size for the extra registers
  (because it has no access to the extra registers that weren't saved before the syscall), they'll be all random values, and that's
  probably what's causing the emulator to jump to an address which is actually some instructions.  It almost works but won't work
  correctly unless those extra registers are actually cloned along with the rest of the process...
- So looking at linux, it has specific entry points for fork, clone, sigreturn, and a few others, which save the switch stack every
  time.  It feels a bit like cheating, because only "blessed" syscalls get the special treatment needed, but it's certainly more
  optimized/performant.

- This is the key point!  I'm pretty sure I knew this a few years ago too when I was looking at refactoring the context switch, but
  gave up at the time because it wasn't important then.  When using a switch that delays the full switch until necessary, you can't
  do it so casually.  You either need to have special entry points for the syscalls that could create a new task (or else the full
  state can't be cloned), or you need a way to delay the full clone/stack clone until exiting to a user process, so you can get the
  full state.  There's really no other way (besides always saving the full state or the non-recoverable state, MMU and FPU state
  could be delayed still, but integer regs state can't because of C and what it will do (ie. save regs as needed instead of putting
  them all in one place))

2025-03-28:

- looking at it more, I'm not sure how it actually uses those special entry points.  Oh I see, it's a table in assembly of entry
  points, which is the same as gloworm is doing but it C.  I could shave off a few instructions by not saving arguments on the stack
  but using the registers directly, and saving a jump instruction by putting the actual table call in assembly
- the special entry point would go in the syscall table (so jumps to C, the jumps back to the same entry file, and then back to a
  hard-coded syscall function in the same C syscall file as the jump.  Given that, it does kind of make more sense to move it to
  assembly.  There are those LED control calls in the C version, but actually I could make a syscall entry/exit hook with compiled
  in optional code like that, so it can be added or removed more easily with a kconfig setting

2025-03-30:

- I still haven't gotten back to this.  I had previously tried looking at linux's other archs to see the switching code, and I can't
  see that same trick that the m68k code has with redirecting the fork and clone syscalls.  I wonder if that was an older solution
  and the maintained archs have a newer better way of doing it, or if they're just really convoluted because they're more highly
  optimized
- I'm still thinking of doing the redirect solution.  The delayed clone might work too, and it would require less refactoring of the
  syscall code, but might be too complicated?  I guess you'd have to store a pointer to the new process, and check that pointer when
  returning to see if you need to complete the clone, and it would only work with a single clone/fork, even though that's likely
  all you'll ever need
- looking at the mips code, the fork/clone calls are wrapping in save_static_function, and there's a comment about that being
  replaced with SAVE_STATIC.  Etiher way, it seems that the same trick is in place to save some of the extended regs conditionally,
  but it's been made generic so any syscall  that needs it can access those values.  I don't really know how it works yet though

2025-04-08:

- I tried implementing the do_fork hook to save the regs before it actually clones the process.  I still have it using the
  do_syscall entry, but that should be ok because it's not using anything but the scratch registers during that, so the extra regs
  will still be preserved when it enters the new __do_fork.  That new entry saves the regs on the user stack of the current process,
  calls the normal entry (which doesn't take arguments anyways), clones the process, and returns to pop the extra registers again.
  I tried with both a sub instruction and a proper RESTORE, and both are causing a fatal exception, privillege violation, because
  it's popped the wrong value into the flags register and jumped to address 40, which is a MOVE to SR instruction.
- I have gotten this before, but I can't seem to find it in my notes.  I know it's because there's some kind of inconsistency in the
  user stack, but I haven't changed that as far as I can tell.  It should be working
- I inspected the stack of the process that failed (according to the stack trace) and couldn't find the expected flags value of
  0x2000.  On a hunch, I tried commenting out the set_proc_return_value function in the fork, since it will calculate the wrong
  address now, with the extra regs on the stack, and while it didn't work, it did at least avoid the fatal exception.  The LEDs are
  flashing to show it's context switching, but it's hanging, as it does
- I tried a hack where I change the value of current_proc, and it's still hanging, and only prints "forking 1" so it's not getting
  past the `init` process.  I feel like I'm close though


- I worked on this again tonight, and I've been going in circles of sorts, but still fixing issues in the process I think.  It
  either has a fatal error, or it hangs after the first fork, depending on what I change.  I thought it was just an issue with the
  return value, so I removed the explicit set function in the context, which needed to conditionally know where in the stack to
  store the return, and replaced it with a value in the proc struct that is set just before returning, but that uncovered a
  behaviour where it doesn't always set the return value, so I had check and reset the flag in the bits member of the proc struct
  when it sets the return value, and in the process removed a run check before setting, and then changed the logic of the bit to
  suit a bit better, and it's still fatal erroring
- I'm pretty sure the current issue is that it doesn't pop the extra registers for the process that is just starting after being
  cloned?  Well either way there's 9 extra long words on the stack (maybe 11 since the rte has already popped those off and caused
  the fatal to begin with).  So it seems like some other condition where it gets the logic wrong on whether to skip parts of the
  restore_context final work
- Ah! right!!! the problem is with exec.  I keep forgetting about exec.  Pretty sure I previously came across this, but this has
  been so drawn out over the last two months, I can't remember all the things I ran into.  I put a print statement on the syscall
  entry to trace what's going on, and it ends in EXECBUILTIN, which is probably trying to launch the shell.  The exec's context is
  probably wrong since it's actually the same current process, even though the executable is different.  create_context is wrong at
  least in this case

2025-04-09:

- I commented out the extra reg values in the create_context function so that when exec was called, and the current process was
  recreated, it would create a new short context so that it would restart properly, but that also fatal'd.  The syscalls were different
  though
- it was doing a bunch of syscall 4 (write), and then it does a syscall 3 (read) and fatals.  That's the first point where it would
  block.  So something else happens that breaks, possible the idle task or some other task that runs that doesn't have the right
  context.  I really need some way to know when creating the context if it's the current process or not
- I wonder if Linux has a similar thing, or if there's a higher level interface to the arch-specific context code, or if it doesn't
  need this difference for some other reason

- oh, just looking at RESTORE_ALL in linux's m68k arch, it's got two values on the stack that it pops off last.  One is the orig d0,
  and the other is a stack adjustment (according to the comment), but I don't yet know what value that is.  It's added to the
  existing stack pointer, so that makes me think it's inherently the size of the context.  It could be 0 if it's a full context and
  36 if it's a short context, and there's no conditional stuff.
- The same thing could be done with the d0 value, since it's comment is orig d0.  Store the value of d0 (in the struct process) when
  entering the context, and restore it when leaving.  That way you don't need flags.  You just restore whatever the value is, and
  it'll be updated with the syscall return if there is one.  That's much more efficient
- searching for the stack adjust value, and they're all clr instructions that set them, so it's not actually used in linux.  I
  wonder if it was used for the reason I'm thinking of using it, but it's been removed for some reason and is kept for legacy
  purposes, or it's expected of all architectures, and others use them but the m68k doesn't need it? *shrug*

2025-04-11:

- Ok I finally fixed the crashing, but it's still not getting to the shell.  I had to add a flag to create_context to selectively
  create an extended context or not.  When using exec on a proc that's the current proc, it will use an extended context, otherwise
  it won't.  That gets through both the initial process start (with the exceptional extended restore on first start), and also
  the initial switch to the idle task, which needs an extended context because it's not the first process run.  I think the
  remaining issue might be related to the return value still, which probably is still buggy
- the current approach is messy and fragile, so hopefully I can make it better by initializing the return d0 value when entering the
  kernel, and fixing the need for the extended context flag, or the exceptional case there.  Honestly, the easiest fix is of course
  to have two stacks per process, but I still want to avoid adding that just yet
- btw the issue I had with it not working last night was that the create context flag passed by C was a long word, so it was always
  using 0 instead of the actual extended flag value when creating the context, and always created a short context

2025-04-12:

- Today I'm going to consolidate the changes I've made that aren't related to the context, so I've got my changes stashed and
  re-applied so I have tho originals, and I've reverted the context changes and am adding back in the minimal changes needed to get
  the enter_exception and other non-asm changes working
- so far it's hanging with task switching working, so I'm pretty sure there's a problem with the return to user process and the
  return value itself, which makes sense.  The other changes to delay the full switch are probably working but the return value
  isn't which is causing the hanging behaviour I've been seeing in the full change

- I've spent all day trying to chase down why it's not working.  I'm not doing the delayed switch.  I went back to the first commit
  after main, 76c14006e0fd669b2b4aa3596a3ff12a8bbdde52, and that commit is broken for some reason, even though I thought it was
  working before I committed.  The syscall gets 0 as the number, so it prints the test message (syscall #0)
- the latest commit, f72add615976e9a5b021d3cc19b02ede7982ca76, fails for a different reason.  It seems to almost work but when it
  returns to the shell process, pid 3, the stack seems to be corrupted somehow.  My emulator doesn't behave the same way, but I've
  definitely got some bugs in the instructions, so not surprising.  It runs the init process alright, and I think it probably
  switches to the idle process at some point.  The init process does a fork syscall, and then waitpid, and when it should switch to
  the shell process, it either hangs or gives a format exception (stack format word is incorrect)

- hmm... when I reverted the changes to the proc struct, it started working enough to be switching tasks, but the return value is
  still likely wrong when the child fork returns because it's showing two waitpid calls (syscall#7), and then hangs, but the LED was
  flashing
- adding just the return value into the struct made it halt due to out of memory, so it's not happy with that
- moving the bits member makes the stack use 0xFFFFFFF7 as the size for some reason, which then makes it hang

- I don't know what's going on.  I fiddled with some things in the proc struct, and now it seems to be working with return_value up
  high.  I moved mem map to below the pid numbers and made the bits a uint32_t.  Not sure if there's a corruption issues somewhere

- alright, I have it working it seems.  It was of course the return value.  I finally got it to go in the proc struct, and I set it
  in the enter_syscall and enter_irq, and then always (instead of conditionally) set it on exit to user space, and that sort of
  worked but then it would go directly to the monitor as soon as a key was pressed...
- after a second of thinking, I figured it was probably the enter_irq, since that is re-entrant but I was storing it in the single
  process struct... so it was being overwritten.  The existing return code was complicated with the bit flag for a reason, because
  it's a tricky problem...
- moving the enter_irq d0 copy to the SAVE_CONTEXT macro, so it's only saved when entering from user mode, which only happens once.
  Otherwise it will do the normal kernel scratch registers entry.  I still have the syscall entry setting it to -1, but I think
  that's ok because it should always set that value anyways in the syscall handler code
- hmm... I wonder if that's why it was broken when I substituted the C version of the syscall handler... because I forgot to call
  the return_to_current_proc(ret) call

- well signal handling is definitely wrong.  I ran tcpserv and set up slattach and could send messages just fine, but when I did a
  ctrl-c on tcpserv, it went directly to the rom again.  So the signal handling stuff is wrong somehow, maybe still the return
  value... maybe it's actually the syscall set, if a signal happens
- nope, that didn't fix it, but there's no harm in removing it anyways

2025-04-13:

- another whole morning of fighting with it but I finally got the delayed switch working.  The idea of putting the context size on
  the stack, which can then be popped off, and checked before popping the rest.  It's much simpler than what I had before with
  trying to "know" what context is on the stack
- the main stumbling blocks that caused it to take so long were 1, the saving of the previous process was happening for kernel
  re-entries via the irq entry, as well as coming from user mode, which would overwrite the intended value
  2. the wrong stack was being used sometimes when resuming, which was caused by the code that compares the current and previous
  procs and skips the switch, but in doing so would skip past the setting of a0 and a1
  3. the wrong syscall was being done because the saving of the current_syscall pointer was happening after the context size was
  pushed onto the stack, so the syscall number was always the context size and not the actual value of d0

- now I just have this signal issue, where when a signal is sent and a custom handler is registered for that signal, it will jump
  straight to the rom instead of exiting the process.  It works fine for binaries that don't have a custom handler, but tcpserv
  specifically does, to test signals and socket cleanup (I think)
- there may be multiple issues, caused by the various changes, but the current one is caused by something that effects both the full
  regs switch and delayed switch code
- there could be an issue where the process that causes a signal to be sent to the same current process, the switch regs won't be
  available.  This would be solved with a kernel stack per task because it would be saved on the stack from entry into the signal
  dispatcher, so there's no need to save them at the start.  It's only because I'm trying to use the user stack, and practically
  throw the kernel stack contents away each user entry/exit pair.  I can only throw away those C-saved extra regs on the stack if
  they're already saved on the user stack, but the delay switch will never save them.  But that's not the straight to rom cause
- well that's interesting.  I put a printk into the syscall again to print which number was called, and now tcpserv is exiting
  properly now
- tcpserv actually sets the signal handler to NULL for some reason.  I guess overriding the default action?  But it still fails

2025-04-14:

- I thought the signal problem might be related to `previous_proc` being set, so I explicitly set it to NULL in close_proc but that
  didn't seem to change the jump to rom issue.  It's definitely happening after exit_proc though
- If I comment out the body of `exit_proc`, it works without crashing, so it's something related to the deallocation/free code there
- oddly enough, it seems to be related to release_fd_table, and not to the process memory itself.  Not sure why though
- I thought I was chasing it, but I had forgotten to save a file, so I was, infact, not
- ah interesting, it was not working still with the file->ops->close function commented out, but working with free_fileptr commented
  out, and when it worked, it complained of "double free on file pointer 3".  The explicit ops close happens on the previously freed
  file pointer, which might explain why it jumps straight to rom (calling a NULL address)
- change the call to only call close if refcount == 1 instead of <= 1 has at least fixed it so it doesn't crash, but does print out
  the double free error message

- so it's not a problem with the refcount being calculated wrong. It's a problem of the file pointer/table being incorrect, with a
  pointer to address 3 (totally invalid) and refcount in the -30720.  The table is all null when starting, and is corrupt when
  exiting
- ah I missed some functions, it's defintely a set_fd call that causes it
- Ok, I found the cause.  It's setting it to 3 when exiting from the accept system call after blocking and suspending the current
  system call, since nothing has connected.  This is literally the only place in the code that uses suspend_current_syscall() that
  has anything other than a return after the suspend happens deeper in the code.  Every other suspend returns 0, and every parent
  call just returns the result.  The accept() syscall is the only one that checks for an error code (< -1) and then sets the file
  pointer and returns.  I don't know if the other calls like connect() should also return -1 if there's an error, or just generally
  what's the best way to solve this in a way that's regularized for future calls
- also, this is defintely not a new problem.  I probably even found it a while ago, but maybe didn't look into it at the time
- I decided to go a bit defensive, since it is the kernel, and set NULL both before calling functions that take a double pointer,
  and also set the result to NULL when exiting from net_socket_accept() due to a suspend

2025-04-18:

- I keep debating whether to go with two stacks per thread, and I feel like the path forward either way will be complicated so I
  should decide now rather than deferring.  I'm also thinking, when using user mode which I'm not using at the moment, the exception
  frame will be on the kernel stack.  The only reason that doesn't happen now is because the supervisor stack on an interrupt is
  the user stack
- if user mode is not configured, it would only use one stack, but if using user mode, then it uses two stacks.  That should be
  easier to implement
- since I'm targetting homebrew systems mostly, I should definitely stick with monolithic instead of microkernel.  Microkernels are
  neat, but more complicated to get working and have more overhead as well, so a monolithic would be easier to port

2025-04-19:

- for a while now I've been chasing this last bug where when I activated the `request_irq` table call in `do_irq`, it would hang at
  some point because it had the wrong irq number.  I kept thinking it was a problem with getting the right irq number from the
  stack, but no... it turns out I just had the wrong function set by the tty driver *facepalm*.  It was going to enter_irq, which is
  what it set the direct vector table to in order to make the hardcoded version work, but it needed to be changed to calling
  `handle_serial_irq`.  Now it works and get the right irq number every time...

- I now have a fully working full context switch committed, and a working minimal patch for delayed context switching.  *phew*  Now
  I don't know which one I want to use
- I'm now strongly thinking of use two stacks per thread, when using user mode, and when not using user mode like it does now, it
  can use only one stack since that's all it needs, and I think it should be relatively minimal to change the existing context
  switch to suit both (ie. the user mode version basically just doesn't switch stacks in most places, where the non-user mode
  version switches like the existing ifndefs use)
- but I should probably add threads and a page allocator first?  I guess threads still aren't that important so I can leave them,
  but I could at least start making room for them

2025-04-20:

- I've started implementing the page allocator and virtual memory/mmu although it's still very early.  I have a page allocator using
  bitmaps but it needs testing (doesn't work yet), and I haven't done much about the mmu
- I've put the Kconfig stuff in arch which is where linux puts it since it's platform dependent, but the global CONFIG_MMU flag is
  defined in each architecture that implements it.  I keep thinking in a very type theory oriented way, where each flag must be
  globally defined and then selected by different archs, but I need to get over that
- I have the same reaction to the parts of the code that call into arch/ code.  I've taken to at least prepending the `arch_` mangle
  but I haven't defined them in a common location in include/kernel/.  I feel like I should defined a common interface and not just
  copy mistakes from other operating systems

- also it turns out Kconfig has the option of conditional defaults, `default <value> if <condition>`


2025-04-22:
- Using page memory for the bitmap doesn't make much sense in some cases.  It's copied from ruxpin which allocated 256 MiB of RAM
  which requires 2 pages of memory for the bitmaps, or 8192 bytes. For a system with 1 MiB, it's 32 bytes of bitmaps, 4064 bytes
  unused, and only 8 *bytes* of memory for a 64KiB block, which too, keep in mind there, will be multiple blocks rather than one
  big block that covers all of RAM


2025-04-24:

- I have the page allocator working, both contiguous and single, but I think I still have an off by one page error, because it
  allocates 0x208000 when it should stop at 0x207000


2025-04-26:

- I think I have paging working, and I have the start of a testing framework for unit tests that run on the host.  I need to make it
  able to compile for m68k as well, so I can run tests there instead of putting them in monitor.bin


2025-04-30:

- there's a problem with the lib .a rule, which is needed to build a library but if no dependencies are supplied, it won't decend,
  and instead make an empty library that needs to be cleaned before it will even attempt to recompile the lib.  This is a problem
  when compiling bare-tests from a clean repository, where libc won't be compiled and it will fail during linking
- I ended up making the test include really only contain the rules for tests, and put the variable resolution stuff into
  Makefile.build, because the prefix-adding lines in the build file needed to come after the test variable filter/substitute lines,
  but not that I think about it, I could just put the test rules above all the variables


2025-05-05:

- looking at the options for page sizes, and how to set initial shift (IS), the TIA/TIB/TIC/TID fields, and page size.  The manual
  says they must all add up to 32.  There is also a function code option not shown here, where you can have up to 3 extra bits that
  make up the function code used like an address space expansion.  That's not really an option on some hardware, such as the k30p
  because it uses the function codes to change the address modifier bits of the bus

- that leaves these combinations, if you have one entire page used as each table.  It would be possible to make one or more levels
  be multiple pages, or less than a page in order to make it work without the initial shift, but if each page table level was the
  same size, filling exactly one page:

fn main() {
    for page_bits in 8..16 {
        let remain = 32 - page_bits;
        println!("with  long descriptors, initial shift: {}, levels: {}, table entries: {}, page: {}", remain % (page_bits - 3), remain / (page_bits - 3), 2_u32.pow(page_bits - 3), page_bits);
        println!("with short descriptors, initial shift: {}, levels: {}, table entries: {}, page: {}", remain % (page_bits - 2), remain / (page_bits - 2), 2_u32.pow(page_bits - 2), page_bits);
    }
}

with  long descriptors, initial shift: 4, levels: 4, table entries: 32, page: 8
with short descriptors, initial shift: 0, levels: 4, table entries: 64, page: 8
with  long descriptors, initial shift: 5, levels: 3, table entries: 64, page: 9
with short descriptors, initial shift: 2, levels: 3, table entries: 128, page: 9
with  long descriptors, initial shift: 1, levels: 3, table entries: 128, page: 10
with short descriptors, initial shift: 6, levels: 2, table entries: 256, page: 10
with  long descriptors, initial shift: 5, levels: 2, table entries: 256, page: 11
with short descriptors, initial shift: 3, levels: 2, table entries: 512, page: 11
with  long descriptors, initial shift: 2, levels: 2, table entries: 512, page: 12
with short descriptors, initial shift: 0, levels: 2, table entries: 1024, page: 12
with  long descriptors, initial shift: 9, levels: 1, table entries: 1024, page: 13
with short descriptors, initial shift: 8, levels: 1, table entries: 2048, page: 13
with  long descriptors, initial shift: 7, levels: 1, table entries: 2048, page: 14
with short descriptors, initial shift: 6, levels: 1, table entries: 4096, page: 14
with  long descriptors, initial shift: 5, levels: 1, table entries: 4096, page: 15
with short descriptors, initial shift: 4, levels: 1, table entries: 8192, page: 15

- there are only two that work evenly, and they're both only possible with the short descriptor, 256 byte pages and 4kb pages
- the long descriptor 1 level 8k page size would use 512 entries of the 1024 top level page, so adding an extra level would not
  waste too much memory
- the top level page could even be split between the page table and other memory information that a process might need
- I'm not sure if i should only support two page sizes for now, or make it possible to sacrifice address space optionally


2025-05-19:

- haven't had much time to work on this with the house hunting and closing the deal on the new place

- I've got a mapping function working.  Starting it was the hardest part tbh.  I'm still not sure if I should put everything into
  that one function, or have a separate unmapping function, at least for the total unmapping (freeing the page table pages
  themselves)
- the trickiest part of unmapping is knowing whether to free a page or not.  With ruxpin, since it's aarch64, and there are some
  extra bits available to the user in the page descriptor, I used one of those bits as the copy-on-write indicator, and if it wasn't
  set, that also means the page is not shared and should be freed, but if I'm using short descriptors on 68k (which are the only
  ones that work out evenly for 4k and 256 byte pages), I don't have those extra bits to store in each descriptor.  It looks like
  x86 is the same, not having user-defined bits
- Given the above, I think it's better to find a way to do this without storing bits in the descriptors, so how do I know if a page
  is shared or not?

- thinking about adding the mmu table to the process, and it should be context because it's arch specific and loaded during a
  context switch, but now that I dig into it, since context is used by signal handlers as well, which don't need to change the root
  table, it seems to not fit to put the table there...
- maybe there needs to be two halves to this


2025-05-20:

- was just laying in bed and thought, for the copy on write bit, you could set the WP (write protect) bit in each descriptor, and
  have the segment it belongs to be marked as read/write, so when you get an exception when that segment is written to, it can check
  both bits to know that copy on write is set, but that particular page has not yet been copied, and copy it before resuming

- I have the `clone` syscall piped through, with a test program `threadtest`.  I just need to actually differential clone from fork.
- I don't need to rename `process` yet
- part of the clone/fork implementation is in clone_process_memory, but some is also in new_proc, namely the pid related stuff...
- the stack init part of clone_process_memory is only needed in fork, and not clone
- is there a bug where the heap isn't cloned properly, only the stack?  It probably wouldn't be noticed much since most things call
  exec() right after forking


2025-05-27:

- I broke through the block I was having by deciding that I can use kmalloc, and I'll eventually fix kmalloc to use pages, if it
  mostly allocates small things.  The pages don't have to be contiguous
- so then I was able to alloc the memory_map and memory_area structs, and construct the address space like that.  I only need to
  make the entire map shareable, and not the segments.  The file is already shareable, and accessing its blocks will always return
  the same page memory holding the cached data, which can be directly added to an MMU map with copy on write set, or if no MMU, it
  would be copied into the contiguous memory block of the process
- I'm kind of bogged down again in fixing the mess of switching from an array of pointers to a queue of private structs, and
  breaking all the code that sets up the process's memory.  That code is going to need to be partly architecture specific


2025-05-28:

- I should be able to abstract the actual adjustment functions in a way that's generic, so I'll have a library for manipulating
  segments and such, but what part of the whole process should be arch-specific?
- what should be arch-specific and what should be mmu vs no-mmu?

- I'm getting close to something testable, but I still have a few areas to patch up, and it's still broken, but I'm trying to make
  it work again before changing more about how the process's memory is initialized
- once I'm done that, I'll still only have no-MMU support, and I'll have to add that and somehow change the code in
  clone_process_memory, where it kmalloc's a new stack.  That will have to include the code, but also, when using an MMU it will not
  kmalloc at all, and rely on the memory_ops and page faults to load or allocate everything

- should I introduce something like FreeBSD, which has a vm_object for the thing that backs the memory area?  Linux just has some
  hardcoded values instead of a separate abstraction.  Should it be allocated and refcounted?


2025-05-29:

- Oh I see why the data segment was initialized to 0, and the stack to everything, and that somehow wasn't glaringly broken.  It's
  because all the data segments are loaded into the code segment.  That also means the data and bss segments would work without an
  error since they're the correct offset from the code, and the heap doesn't need to be anywhere specific.  It's just really
  confusing to me to do that
- maybe I could make the heap its own segment, so that it's clear it's distinct from data

- I have it almost working, but I forgot to add the code to create the map itself, which isn't done automatically in case the
  process is cloning the memory map of another process


2025-05-30:

- now that I think about it, if you just copy the stack, the pointers will all point to the old code segment.  I'm pretty sure I've
  come across this before, just haven't looked for any existing notes yet.  It mostly works because exec will be called shortly
  after on the child process.  The code itself will still use pcrel but any relocation table updates would have to be redone, and
  any addresses on the stack or in registers would be invalid


2025-05-31:

- just to remind myself, the reason I didn't put a refcount in for the memory address space in ruxpin (and why one doesn't exist in
  linux I think), is because it can changing the address space, and just set copy on write on all the existing pages.  The address
  space refcount is enough.  But without the MMU, you can't intercept a write operation so you need to copy at least the stack ahead
  of time
- on 8086, you can duplicate all memory and change the segment registers to point to the new area, and all the addresses on the
  stack (assuming they aren't changing the segment registers) will still work correct while pointing at the duplicated memory
- for m68k, this isn't the case, and your stack, even though duplicated, will have addresses on the stack that return to code in the
  old process's space.  There could also be pointers that point to the old stack, if they were passed as arguments, which could
  definitely cause problems
- the short of it is that on m68k, it's just not really possible to fork a process unless that process almost immediately calls exec
- but this is the case where I acutally need some kind of alternate means of refcounting the backing memory, since what I'm doing to
  make the problem less problematic, is copy the heap/stack but make a ref to the existing code/data, and that's currently what's
  causing the double free issue I'm running into

- the code/data memory is freed twice because it just copies the addresses to a brand new memory_area
- I can refcount the memory_object, but then I'd have to allocate it as well.  So there'd be memory_map, memory_area, and
  memory_object, all wasting 8 bytes each to hold the alloc info
- I can't insert the same memory_area into the linked list in two maps, but I can go back to the array of pointers style list.  I'd
  have to realloc the area though, or else alloc the array separately, to allow the map to grow, so... I guess I still have 3 allocs

- oh right, now I remember how it was with ruxpin.  The pages themselves are the cached open executable file, so at least for those
  pages, they don't need to track the refs, as long as the file handle/vnode ref is held.  Unfortunately, that trick only works with
  an MMU, so non-MMU mode will have to behave differently, and that difference needs to be easy to make configurable

- now, unrelatedly, I'm thinking maybe the notation of `clone` is not good when it's always refcounted by meaning, and sometimes I
  need some other kind of `duplicate` function... so how about `memory_object_ref` or `memory_object_make_ref` or get_ref, or...


2025-06-03:

- I'm pushed into using 3 allocations, and generally such a complicated memory map solution, by the no-MMU m68k architecture.  It
  also requires a fairly unique memory map, as would the no-MMU 8086 arch.  I'm struggling with  how best to isolate the process
  creation code for each arch


2025-06-08:

- I've made a new file, exec.c, in addition to binaries.c, fork.c, and init.c.  I've pushed the reset_stack stuff into exec.c and
  the reset stack call into binaries.c for now.  My intention at least, is to put the common exec stuff into exec.c and try to keep
  init.c as small as possible, so all the memory related creation is done in exec.c, and can be changed once without having to
  consider both so often.  fork.c will have process forking/copying which is quite different from new executions.  And eventually
  some of what's in binaries.c, the common non-file-related parts can also be pushed into exec.c so it has all the common new process
  initialization code
- also, I have a bunch of memory leak issues with memory map, and objects, where the refcounts are wrong and the process memory,
  which is kmalloc'd at the moment, never gets freed


2025-06-10:

- I mentioned the issue of forking a process on 68k in the Usagi Electric - Homebrew Computing discord and PenguinOfEvil (the
  creator of Fuzix) responded:
      You copy it back when you task switch. That's how Fuzix works, it's how Minix works on 68K. There's only one generic old era
      small Unix program commonly used that forks and doesn't then exec something which means no more copying needed - that's kermit
- I was looking at Minix last night, and I found it has an extra shadow value in the proc struct with the memory map tables, and it
  delays the copying until the kernel (and not the mm process) handles the fork, at which point it does a copy.  Last night I was
  thinking it was just one copy, and it looks like just one copy, but Penguin is alluding to it copying the shadow region into the
  same working region (primarily the data segment from the looks of it) on every task switch?  I can see why that would be slow, as
  he mentioned much earlier
- I feel like I've had this conversation with him before, but I remember it only vaguely.  Generally this solution is called the
  shadow solution or shadowing, and there's a special shadow.c file just for it in the Macintosh Minix source code that I have
- the Minix code mentions 500ms as the FLIPWAIT time.  It seems to be doing the copying itself in the clock code and not the context
  switching code.  I'm not sure what's up with that.  It does seem like a weird implementation of the concept, or just difficult to
  follow
- The last time I heard of this, I had probably decided not to implement it, and I'm kind of thinking the same thing this time,
  since almost all procs will call exec() after a fork(), but it would be nice if the new refactor would make it possible to
  implement later
- that said, now that I'm thinking about memory usage and constrained systems, I'm starting to second guess the approach I'm taking,
  with so many allocations.  Maybe I should try to simplify it a bit?  It's a tradeoff between flexibility for larger systems and
  memory usage for smaller systems, so in that sense, it's maybe better to focus on larger systems, but I still feel bad about it.
  Maybe I could make some of it compile-time selectable?

- from the todo file:  after looking at linux code, it initializes a new mm_struct when loading the binary, and then swaps it in
  into the process when it executes, which is a much better way to do it, now that I have a separate alloc for the map itself

- I've cleaned up the map and segment allocating code, with init.c using exec.c to launch new processes, and both kernel execution
  and elf binary from fs works and runs correctly enough to not blatanly fail.  The increment and decrement messages are being
  printed, so that's a big one to fix still.  I don't have that in my todo list yet


2025-06-14:

- I was making this pretty complicated, but realized that if I switch to the pages system instead of kmalloc for user memory, I
  wouldn't need to free the block at once and could instead have it free the single block as multiple blocks (so that the heap and
  stack segments could be free separately).  That means when cloning the process, it doesn't need to ensure it only copies one
  memory_object as the heap/stack
- now that I write that though, I realize that not only am I probably not adjusting the cloned heap correctly, but I'm also probably
  not ensuring that when the heap grows, it only grows by PAGE_SIZE increments


2025-06-19:

- I was thinking that maybe I could eliminate the memory_object by forcing adjacent kmalloced data to be allocated separately, but
  then I remembered that that would mean the node tracking data would be between segments, causing the addresses to be all wrong,
  for an elf file, so that doesn't work
- I don't think it's possible to make kmalloc'd non-paged memory work without significantly more complexity
- if I go all in on pages for user memory in every situation, and I can always free arbitrary pages, can I simplify things more?  I
  could allocate contiguously and then break it up, but if I want to ever ^^^^ reuse pages without an MMU ^^^^ I need to keep track
  of how many things hold the pages still
- I can do that on a page-by-page basis like I did in ruxpin, or I can do that with memory_object

- I guess it's actually a waste of memory to use per-page refcounts without an MMU since the same page can't be mapped into a
  processe's address space on a per-page basis.  It can be mapped on a per-region basis (so all pages in a range)


2025-06-20:

- I feel like I have more code now, and I do in terms of creating memory maps and segments, but it seems like some of that is
  duplicate.  Not only that, but some can be reduced by removing it when using an MMU, or simplifying the memory map.

- I could possible go back to an array of segments structs (not pointers to structs), but then it's cumbersome to move them around
  if you want to insert a new one.  For non-MMU, that will rarely ever happen, but if you can use mmap, then it would happen more
  often, whenever using mmap, which would certainly need to insert into the middle where the heap is


2025-06-21:

- not that I'm unhappy with the current massive commit that I'm about to make, but I feel pretty conflicted about the memory
  segment/map changes.  I know it can do with a lot of massaging, but it's probably better to get it in and then change it after.  I
  should probably start making branches and then merging those branches without squashing which means I can commit without waiting
  so long, and still avoid main getting broken.  I certainly could have used diffs a couple times during this massive change

- I should change the memory stuff to not deal with segments directly but deal with mapping and unmapping addresses, and let the
  impl handle the rest


2025-06-24:

- even in no-MMU I can still have mmap support.  There's just nothing in hardware stopping accessing the memory without mapping it
  first.  As such, you can actually implement vnode bufcache paging things, so that it uses the same pages for all processes that
  access the same read-only data, so long as the address it's mapping to is NULL or can otherwise be changed by the OS.  I fixed
  mapping would still fail


2025-06-27:

- alright, I have mmap and unmap in the process memory code, minus the actual MMU stuff.
- I'm still thinking of how I could get rid of memory_object (possibly by splitting them when dividing memory?  I only want to use
  them as a per-region refcount instead of having to maintain per-page refcounts which would take like 4KB or more for 1MB of 256
  byte pages)
- I feel like I should get rid of the resize code as well, and use the mmap/unmap code instead, but maybe that's not so important
- I still have more to do with the memory code, specifically adding and testing the MMU table mapping, and separating the no-MMU
  cloning code and platform-specific cloning code from the MMU cloning, but maybe I should work on the context instead
- for the context, I need to merge the previous change that delays the full switch until later, and also add in the user mode
  support which is going to uncover a bunch of bugs.  I can't have a separate MMU table without adding that to the context switch,
  and I can't add that until the CPU switching works, but I would also need to add two-stack support and so on, so it will be a big
  change before I can get back to MMU support again


2025-06-29:

- I was about to dive into adding the second kernel stack in user mode, but instead I've been investigating why signals don't work
  properly anymore, since it's related to the context changes I've made
- programs like the shell which register a custom signal handler for SIGINT will have that handler run, but then exit afterwards,
  and it's acutally calling the exit syscall via `_exit` in the syscall_entry.S file, which makes me think the stack is somehow
  getting corrupted.  It calls _sigreturn, and then _exit right after, so it's likely a problem in the cleanup code
- the problem was introduced somewhere during the context refactoring, but I couldn't pinpoint exactly where because there are a
  bunch of broken commits in there
- disabling the special __do_sigreturn alternate entry point doesn't change anything, so I don't think this is related to that
- it's not strictly related to the delayed context switch that I just enabled recently.  It's related to the earlier context
  refactoring
- the stack pointer seems to be the same before the custom handler is called, and after sigreturn cleans up afterwards, which is
  throwing me off.  It should be right!  There must be something else, something subtle, going on
- that said, between when the handler returns and when sigreturn is called the stack pointer is different, but also when sigreturn
  is run, the process is the current process whereas when the signal is first run, it's not necessarily the current process that
  handles the signal.  I wonder if even sending a signal to a background task would work where the current process doesn't


2025-07-03:

- I had been banging my head on this problem for a while, where it wouldn't start, fork wouldn't work.  I had it working when
  user mode was disabled, and the changes when it is enabled are small, but it wasn't working.  It would call syscall 5, then 4,
  then 1 (which is exit) and then stop
- I thought it was something to do with the stack or the _exit return address on the stack, but it was actually the address of the
  `stop` instruction after the `trap` instruction in the `_exit` syscall, so it actually called the exit syscall for some reason
- just this morning I looked at it again and thought to check the syscall_entry.S file because it's the only place other than the
  stuff inside the CONFIG_USER_MODE blocks that might be affected by the few things changed by the ifdef blocks, and also at struct
  process, and then saw that the return_value field is after the task_info struct which adds the kernel_start field when user mode
  is enabled, so that explains why it wasn't working.  The return value was incorrect for the first syscall, which is fork, so it
  thought it was the child process and ran the utility and exited like normal!
- when I fixed the order, it wasn't working still.  It would run the shell but then crash on startup.  I started looking into it
  (around 8pm, after work), and recompiled fresh with the shell compiled into the kernel, and now it just seems to be working fine
  now, so maybe it needed a clean and make?
- nope, it's broken when it doesn't have the shell compiled in.  It forks the shell, runs /etc/rc, and then has some kind of
  exception.  Perhaps this is actually a privillege violation or something, and it's not displaying right (status: 7B72, vector: 19A)

- I changed `ls` to use a `stop` instruction, which should cause an exception if it's actually in user mode.  That instruction is
  privilleged.  When I tested it though, it gave a weird status and vector number.  Maybe it's using the wrong stack or something
  because it seems to be displaying wrong with other exceptions too

- yeah something's not quite right.  It's getting better, but it's not fully working
- threadtest is unable to write usp and then gets out of memory and dies


2025-07-04:

- so the threadtest issues was caused by sbrk, which used arch_get_user_stackp(), which wasn't implemented when a short context was
  on the stack and was returning 0, so I refactored to compare if it's the current proc that's changing its stack, and return the
  value from usp itself, and that's working now.  Threads are working again
- the signal problem was caused by not using the user stack, and putting the _sigreturn return address onto the kernel stack.  I've
  split that now, like the process context, but haven't tested yet

- using init on disk isn't working, but it's because of ntpdate from the looks of it.  It is returning from printing "timeout",
  which is delivered via a signal, but there's an extra word on the stack, or the address is just wrong on the stack.  It should be
  calling sigreturn there
- I had just forgotten to change the get/set stackp calls for setting _sigreturn, so it was getting/setting the kernel stack, and
  also not updating it because it used ksp to set instead of usp
- but it's still not working...

- in either case, the problem is handling custom signals.  Everything else appears to work, although I haven't tested extensively


2025-07-05:

- I finally found the cause of the signal handlers not working and crashing.  I was printing out the stack and the context looked
  ok, but I had forgotten that there were two contexts in play with a signal, the one that saved the process state when entering the
  kernel, and then the signal context added afterwards.
- I was overwriting the usp of the process context before I added the new signal context onto the kernel stack, so when the process
  returned after the signal context was removed, the restored usp would be pointing to _sigreturn still
- it might still be broken when sending a signal to the current process, because the usp might get modified by the signal context
  without being saved before hand!

- [recalling later] either later last night or early in the morning, I realized I had forgotten to set the usp in the
  create_context() call when adding the signal context, so USP was set to NULL when the process started.  This was causing it to run
  the add_signal_context call, but not run the remove_signal_context calls, and once I set usp to the correct value, it ran the
  remove_signal_context too, but then crashed again shortly after.  I then forgot all about this when looking for the problem I
  described above, because it was directly related

- OH! the custom signals are working now as it turns out.  I didn't notice, but it now won't exit the shell when pressing Ctrl-C.
  It's supposed to print ^C but it's not doing that for some reason.  But it's actually working from the looks of it.
- No, it's only working when the init/sh are not compiled in.  When sh is compiled in, it doesn't work.  It exits Process 3 (sh)
- So maybe it's something to do with changes to the commands compiled into sh?


2025-07-11:

- just looking at some numbers:
    + ext2 = 8 KiB
    - net = 11 KiB
    - shell = 11 KiB
    - minix = 5 KiB?
    - ata driver = 1.3 KiB


2025-07-13:

- I'm back to the memory stuff, and while I still need to sort out the memory_object and what to do about it when using MMU, I'm
  running into the mmu map failing because the numbers aren't rounded to the nearest page size.  It's because of the elf program
  segments not being aligned as I expected
- they're aligned to the nearest 0x100 (sort of, sometimes) but the lengths are not rounded.  You have to look at the next segment
  to see where it starts to know if it's safe to round up
- that said, I saw the threadtest has a data segment that starts at 0x908 and I have no idea why it's not rounded.  The rest seem to
  be rounded

- so I was looking into the program header issue, and I found a post on osdev talking about this very issue, with the answer being
  the segment is still a page in size and it can be loaded still, but if you want the expected page aligned vaddr behaviour, you
  need to use a linker script
- I looked up in ruxpin and it turns out I ran into this problem there, and factored it in to the address, but I'm not entirely
  clear on how it works, of it it uses two pages in memory or something


2025-07-15:

- I was running into a problem where it would return -17 EEXIST when running init on boot.  I had fixed the alignment issue, but it
  was still failing.  I printed the output of the table mapping, and it has weird invalid addresses as if the page directories
  weren't initialized, but I had already tested that
- I looked at it a bit more and saw that it was using address 200000 as a page table, which should be where the bitmap for the page
  allocator is located.  I just clued in that the top level table, table 0, is actually NULL!  It just so happens 200000 which looks
  like an allocated page address almost just happens to be the starting stack address in rom at address 0.  So that would explain
  why it's failing


2025-07-17:

- last day before the move, it wasn't working, even with MMU disabled
- I went and commented out the three main parts that I had change, two in binaries.c where it used roundup to increase the area of
  memory allocated and mapped, and one in memory.c where I added extra checks for both MMU and no-MMU.  That one, and the segment
  start/end in binaries.c are causing it to not work, but I think I know why now, at least for one
- for the binaries.c one, I'm using memcpy to load the data, but it must have a bug in it
- even with just the memory.c checks though, it fails to load.  I'm not sure what's causing that one, but they might be co-related


2025-07-24:

- finally have my computer set up after the move to East Van and have my project set up again.  I probably won't do anything
  tonight, but should be able to work on it tomorrow


2025-07-27:

- it seemed to be related to the memset in load_binary.  I commented that out and it's now booting, so I guess it was overwriting
  something important


2025-08-04:

- ok, so I have it working with MMU disabled, but it's broken with MMU enabled and I can't remember what I was doing last, so I
  don't even know where to start with it.  I'm still trying to pick this up again after moving but haven't had much time.
- it tries to start up and initializes the kernel (with MMU enabled) but fails when running user programs.  The shell isn't compiled
  into the kernel so it's trying to run them from disk.  It fails with a bunch of illegal instruction exceptions (vector 4)
- compiling into the kernel, it seems to run the shell but then gets a privillege violation exception (vector 8).  I'm confused by
  this because it's not actually using the MMU tables yet but maybe I need to set the CPU root table register or it will always
  fail?  Am I not doing that yet?  It still managed to print the pseudo shell message, so it can't be the case that no table is set
  or else it wouldn't get that far


2025-08-06:

- trying to figure out where I was, and tried not initializing the mmu to see if it was related to it being enabled vs disabled, and
  at first it worked, but then I realized the config was disabled, and when I tried again, it had not effect, broken in both cases.
  So it's related to the other code, and uncommenting the map and unmap calls, which were the only thing that wasn't unallocation
  (well the unmap is unallocation too)
- it worked without them, and it worked with the unmap call, so it was map, and changing the flag to something else instead of
  MAP_FLAG_WINDOW worked, so it was just that flag

- ah! an idea, what if it's because the table is not cloned during fork, and anything that tries to map will cause an exception
  (it's trying to use a table that's NULL all over again).  That's gotta be it
- no that can't be it because a new root table is allocated every time a map is created, and MMU_FLAG_WINDOW doesn't do anything
  except use the virtual address as the physical address, so it's not a NULL table, but it is an unmapped table that's trying to
  unallocate or map something that hasn't been mapped...
- could it be not checking for NULL before recursing?

- well when I compile the shell into the kernel, it works enough to get to the shell but then crashes in the kernel it looks like
  when trying to run `ls`
- so are the user programs different somehow?  I couldn't find a CONFIG_MMU in any context or process code, so it would seem not
- address 10D0E0 is the fault address, and it halts so it was likely the kernel?  nope it's actually the exact same place as the
  the fault when running the applications from disk!  It's the RTS instruction in the fork user function, just after the fork
  syscall, so it's the same problem: a corrupt stack on return from fork, and it seems to be the parent that's affected and not the
  child?

- wait, is it not copying the extra stack?  When MMU is enabled, it uses two stacks? or maybe that's user mode.  If user mode is
  working, it should be alright, but I tried removing the fork intercept thing where the extended regs are saved, knowing it
  wouldn't work because important registers won't be cloned, but there might be some kind of problem with it, even though it
  doesn't have any MMU-specific code.  But it then printed the "fuck, trying to write usp to different pid 3?" message, so that got
  me thinking about the user stack.  I guess I can look at that code, but I don't think it's likely the problem
- but what if the usp is overwritten when it shouldn't be?`

- well, fuck me.  I just tried it the opposite way, commenting out the unmap and leaving the map, and it works, so it's not the map
  that's causing the problem, it's the unmap...
- I had previously just tried printing when usp was set, and it's set once, but that run also crashed silently before getting to the
  first prompt, and an unmap happens before then (and then a map, and then the usp set), and that got me thinking that maybe if it
  was freeing the old memory, it would reuse a page and thus overwrite its memory causing the stack corruption
- and sure enough that was the problem... or at least I'm pretty confident of that now.  I don't understand it fully, but there must
  be an early free somewhere


2025-08-11:

- I didn't work on it today, progress has been slow, but I think I have it able to track page refcounts.  I just need to add
  refcounting to the existing page uses


2025-08-17:

- finally back to this again, and I went through a few things looking for that page free bug that was plaguing me before.  I had
  thought that the page_free call in the mmu table code was causing an early free that would then allow the page to be reused and
  then cause the stack problem when returning to the user process, but that turns out to not have been the cause
- I had tried putting print statements around the alloc/frees, there weren't that many, and honestly it didn't seem like there was a
  chance for an early free to be happening.  That ran fine, and re-enabling the free didn't cause the problem again
- I found some code that I had changed at some point in the last few weeks since this problem happened, involving the MMU status
  flags for page descriptors.  I remember at some point when looking through things noticing that the S (supervisor) flag was being
  set, and had tried disabling that to no avail, but somewhere along the line I also noticed that I was using the LONG descriptor type
  instead of using the header-selected descriptor type (I was using MMU_DT_TABLE_LONG instead of MMU_DT_TABLE).
- acutally I just thought I had recreated the problem by putting it back to _LONG but that was just the timeout for fetching the
  date, so acutally I don't think that recreated the problem
- I can't seem to make it fail again like it was doing before...
- either way, I think it's safe to re-enable this page_free code again

- motherfucker... I had the init_mmu() call commented out, so it wasn't initializing the root table.  That would explain why I
  couldn't reproduce the problem
- still can't reproduce the problem
- Ok there! Finally was able to reproduce it by removing the refcount page free code that I had recently added as well.  So there's
  a problem there where it's not freeing the pages.  I don't think I have a make_ref call anywhere so it should be acting exactly
  the same with or without the refcount check, but since it's not behaving the same, there must be an incorrect count

- alright, so the previous problem with reproducing the problem was caused by the fact that the mmu table was not initialized, MMU
  was disabled, and the page refcounting code wasn't working properly so no pages were actually freed
- now the MMU is enabled, the proper page descriptor types are being used, supervisor bit is not set, and the refcounts are working
  correctly, and the problem is occurring where page_free in MMU unmapping is causing 3 successive exceptions

- Ok, so put a bunch of print statements back in, and I'm looking at the problem.  The first exception is the keep problem, and it's
  always the same addresses.  It's an address exception with the fault address being 0x2038D4, which corresponds to the RTS
  instruction on returning from the fork syscall, as executed from the user process code segment.  The instruction data in the
  exception stack trace shows it being the RTS instruction that causes the problem
- the objdump of the user process in questions shows 0x1D4 being the RTS instruction in the user side of the fork() call, which
  corresponds to a code segment address of 0x203700 which matches that process's code segment, so that all lines up
- the actual fault is caused by the return address on the stack, I believe, which corresponds to 0x206690, where the stack itself is
  shown as 0x2034A4, but that's wrong.  The process address space says the stack should be between 0x204700 and 0x206700, so the
  stack pointer is pointing to the wrong stack
- while it's true the stack seems to be wrong, the fault address doesn't seem to be a problem.  The CRP register is pointing to the
  root table, which should allow it to access all addresses, and the fault address itself isn't an odd byte or anything, so I don't
  see why this would cause an exception besides just being wrong/pointing to the wrong place.  It should misbehave without causing
  an exception, so why the exception
- wait, I'm getting myself confused between the stacks.  There's two stacks here, and the exception always ends up on the kernel
  stack for the process, so 0x2034A4 is the kernel stack address, not the user stack address, but even then, there seems to be
  something not quite right.  I don't see an alloc for 0x203400 or a new ksp print statement for pid 1, so that's odd
- the exceptions only occur when the shell on disk is used, and when compiled into the kernel, it hangs instead of causing an
  exception.

- I don't know what's going on.  I was trying to get more log messages with more info in, and I added the pid to the kernel stack
  alloc print statement, but it was set to 0, so I moved the arch_init_task_info call from near the top to near the bottom of the
  process init function, and now it seems to boot fine.  Not sure if this is coincidence or related.  Either way, it does match that
  the kernel stack for pid 1 is 0x202D00 to 0x203500, so that ksp of the exception from before is correct, it's on the right kernel
  stack
- actually it doesn't boot fine.  It's causing an exception still, but it manages to get into the shell.  It doesn't have the
  address exception, but it has an illegal instruction during boot and a privillege violation when running ls
- moving that code back, it's still booting to the shell with a random exception (sometimes exception 4, sometimes 11, and then
  exception 8 privillege violation whenever I run a command)
- when it gets the vector 11 exception, that's 1111 emulator and the instruction pointed to is FFFF FFFF, and the code address is
  0x2066BE, but that corresponds to the process's stack and not its code space... it feels like stack corruption, and given it's
  magically fixed by not freeing some pages, I still think a page is being incorrectly reused somewhere that causes the corruption
- this is really hard to trace and to fix
- the address where the stack appears does indeed get freed that problematic page_free, but I don't know what it was originally used
  by.  I think it's another process

- Ah!  I think I see the double free.  The map/unmap code is separate from the memory_object code, and the refcount there, and it's
  still not incrementing refcounts when it maps existing memory, so it's always freeing the stack area when a process terminates or
  exec()s, even when the clone of that process still has a reference to that memory
- while I don't like the fact that the memory object has its own reference(s) to the data, the proper thing to do is to increment
  the refcounts when it maps a range of memory that's in the page space.  I don't know how I'll distinguish that though from
  hardware memory mapped addresses

- uuugh progress is so slow here.  I don't have as much motivation to work on it, plus so many other distractions, so while I sort
  of know the cause now, I'm not sure about the solution, or how I should fix it properly, and I kinda don't want to work on it
  right now because I also need to get some groceries and other things to do related to settling into the new place


2025-08-21:

- only now did I realize that the page free that was causing all the problems is slightly incorrect because I mapped the data using
  the WINDOW flag, so it shouldn't be freeing the page unconditonally anyways.  There's no way for the mapper to know how it was
  mapped, and the flags uses a type field when mapping, so the WINDOW flag can't be added while the UNMAP type is also provided
- I hackishly added a flag that I can pass in to control freeing the pages in the table, and I'm always passing it in because I'm
  always passing in WINDOW as well.  I'll have to fix it properly somehow later

- I started trying to add the context switch stuff for the MMU table, and caused an MMU exception (56 decimal), which is because the
  CPR register is not just the address.  I forgot it's a 64-bit word, so it's just plain wrong the way I have it
- that then gets me thinking about whether I should all a function to do the extended switch instead, so I can do it in C and not
  have to hard code the struct offsets, but there's also the MMU TLB invalidation that's needed, which then has me rethinking the
  whole context switch, and the idea of doing it in C when the rescheduling happens rather than when returning
- I think I have two approaches I can take here, both are the lesser refactoring options
    1. keep things like they are, and add a call to a C function during the final context restoration which does the extra switch
       (MMU switch, eventually FPU registers, anything else in hardware that needs to be done)
    2. keep things mostly like they are, but put the actual switch call in the process reschedule() function or somewhere like that
       and continue to do the extended register swap just before returning
- option 2 would sort of split things up a bit, but it has the advantage of being done while interrupts are enabled
- option 1 would be the simplest but would mean the context switching code would be kind of special with interrupts disabled, and
  it would have to save the 4 scratch registers, or otherwise just be really careful about the code that gets generated


2025-08-22:

- I'm trying to get it to fail by having a user program access an invalid memory address, but no matter what I do, I can't get it to
  generate an exception
- I had some of the status bits set incorrectly, and the root pointer, but it didn't fail despite that.  I'm getting confused
  between what the short descriptors and long descriptors support, but I can only get the full address space working if I use the
  short descriptors, which don't support the supervisor bit! wtf
- is it the TT0 and TT1 registers not being explicitly set that's causing it to work when it shouldn't?

- omg! I had the wrong E bit value to enable the MMU, so the MMU hasn't been enabled this whole time!  I had hex 0x1000_0000 as the
  enable bit, but it's actually 0x8000_0000  *headsmack*
- now that I have the right bit, I'm getting an infinite number of exceptions, all exception 2, bus error.  I have no idea what's
  wrong but it's definitely doing something different, so there's that


2025-08-23:

- the decision to not add the code segments to the memory map is coming back to bite me.  That's what's causing the exceptions, and
  the processes are killed, but since it's pid 1 (init) and pid 2 (idle), it still ends up switching to them when scheduling, hence
  the infinite exceptions
- or at least I think/thought that.  I tried switching to not compiling the shell into the kernel, and it seems to run pid 1 (init)
  but then has an exception on pid 3 (sh) and then pid 2 (idle) which it doesn't detect so it exception loops
- the fault address seems to be 0x2066C2, which isn't in the memory map.  It's between the code/data area and the stack
- oh, it's the process cloning that's the problem, and the fact that it still points to and uses the parent process, because pid 1
  runs without an exception, which launches pid 3, and pid 3 is what has the first exception.  The address 0x2066C2 is in the stack
  area of pid 1 but not pid 3


2025-08-30:

- once again haven't had much time in the past week, and I'm trying to pick up where I left off.  I think I had fairly reliably
  tracked it down to being the missing code segments causing trouble, but I wasn't sure how I'd go about fixing it in light of the
  stack cloning and all that
- should I go straight for cloning the process's address space exactly instead of copying the stack elsewhere, or should I continue
  trying to gradually switch over, but then have to map two processes's memory into the same space when cloning?


2025-11-08:

- linux only uses one offset instead of 3 like I used on ruxpin.  I'm sure I can make it work somehow
- I'm going to make memory_object only be used for the non-MMU mode, and move the memory ops, file, and file offset to the segment
  instead.  The offset will likely be different for each segment, so it's only using one more long word than it would otherwise use,
  but saves having an extra allocation for the memory_object struct, and the refcount and all of that


2025-11-11:

- I've renamed memory_area to memory_segment, and memory_object to memory_region to be more descriptive.  A memory segment is a
  single mapped entry in the memory_map.  A memory_region is a pre-allocated chunk of memory that is divied up between different
  segments in a memory map.  It's only used when virtual memory is not used, since otherwise it will use and allocate pages on a
  per-page basis, with a per-page refcount and per-page sharing between segments.  The memory_region struct is itself allocated, and
  holds a refcount that is used to share regions (which also means an entire region is kept around until no processes use any part
  of that region).  Since I'm still using the separate stack/heap allocation and code/data allocation instead of the shadow ram
  method, and since most processes fork and then exec right after anyways, it shouldn't actually keep much in memory


2025-11-19:

- last night I was briefly looking at the code again, and thinking about the issue.  It's crashing on boot but now it's getting
  exception 11, which is F line emulator, so it's executing an 0xFxxx instruction which confused me at first, but then I realized I
  had changed the user memory start address when executing a new binary to 0x40000 which is a nonsensical address.  That got me to
  thinking about hacks to make to the MMU option code that will run, and then thought well I can add pre-allocated memory for the
  code and stack that I delete later once I get everything working, and I won't need to comment out anything because it'll be only
  in the CONFIG_MMU code
- I can use the page allocator to allocate a chunk (instead of using memory_region), and then I can probably getting it sharing code
  based on page refcounts
- I modified binaries.c to do the allocation where the non-MMU code does it, and then it crashed on 0x10224c with an address error
  during a fork, possibly because of an issue with memory_map duplication?  It's in the clone_process_memory function, although
  actually in the duplicate_memory_map/remap_all_copy_on_write functions which are inlined into the former, and it's just before
  calling the memory_map_remap_copy_on_write() function
- wait, did I just find the bug?  It was calling that remap (which does nothing atm) and iterating over the parent's map and then
  the child's map, but it was actually calling it on the childs map in both cases?  Well that shouldn't matter since the function
  does nothing yet, but either way, there's some kind of bug there.  This might be the first time that codes' actually been run

- turns out it was a bug where `proc->map = new_map;` was only in the copy_stack function and wasn't run when calling
  remap_all_copy_on_write.  Now it's back to crashing in a loop

- the fault causing the infinite loop is different now.  It's an address error caused by 103AB8, which is an rte instruction in
  restore_context, so it's trying to return, and the stack it's using is corrupted?
- here is the output of the first two stack traces after booting:

      ```
      Booting with "..."

      mmu: setting TCR to 82806666
      mmu: read back TCR of 82806666
      mem0: ram disk of 131072 bytes
      ata0: found partition with 524288 sectors
      ata1: found partition with 1048576 sectors
      minixfs: mounting (400) at /
      procfs: mounting at /proc
      slip0: bringing interface up
      begin multitasking...

      Error in pid 3 at 103AB8 (status: 0, vector: 2)
      pid 3 memory map:
      203700 to 204000: flags=51
      204000 to 204200: flags=32
      204300 to 204300: flags=33
      204300 to 206300: flags=134
      Stack: 204DC8
      204DC8: 0000 0010 3AB8 B008 1EEA B64D 0000 0000
      204DD8: 0000 0000 0020 4E24 0000 0000 4E73 4E73
      204DE8: 0000 0004 0000 0002 0010 3ABA 0000 0000
      204DF8: 0000 0000 0000 F07C 0000 0000 0000 0000
      204E08: 0000 0040 0000 0000 2034 0000 0020 4E24
      204E18: 0000 0000 0011 FF34 0000 0000 0000 0000
      204E28: 0000 0000 0000 0000 0000 0000 0000 0000
      204E38: 0000 0000 0000 0000 0000 0000 0000 0000

      Code: 103AB8
      103AB8: 4E73 2079 0010 DC48 2268 000C 5889 48E1
      103AC8: 0F3E 4E7A 4800 2304 233C 0000 0040 2149
      103AD8: 000C 4EB9 0010 1242 2079 0010 DC48 2268

      error: double free of file pointer, 0
      error: double free of file pointer, 0
      Proces
      Error in pid 2 at 103AB8 (status: 0, vector: 2)
      pid 2 memory map:
      Stack: 202BA4
      202BA4: 0000 0010 3AB8 B008 1EEA B64D 0000 0000
      202BB4: 0010 3600 0020 2C00 0000 0000 4E73 4E73
      202BC4: 0010 3604 0010 3602 0010 3ABA 0010 3600
      202BD4: 0000 0000 000F F07C 0000 0000 0000 0000
      202BE4: 0000 7CF0 0000 0000 2008 0000 0020 2C00
      202BF4: 0000 0000 0000 0000 0010 3600 0000 0000
      202C04: 0000 0000 0000 0000 0000 0000 0000 0000
      202C14: 0000 0000 0000 0000 0000 0000 0000 0000


      Code: 103AB8
      103AB8: 4E73 2079 0010 DC48 2268 000C 5889 48E1
      103AC8: 0F3E 4E7A 4800 2304 233C 0000 0040 2149
      103AD8: 000C 4EB9 0010 1242 2079 0010 DC48 2268
      ```

- I thought I saw something in it, but it's not what I thought.  It's only the second trace that has an address to the idle task's
  instructions, and that's pid 2 which is the idle thread, so it makes sense, and the address at the top of the stack is 0x103ab8,
  which is the rte instruction that's causing the problem
- I'm not sure where the offending stack is that the rte instruction pops and causes the address error
- an address error pushes 46 words, or 92 bytes, 0x5C, onto the stack

      ```
      > dump 204dc8 100
      204DC8: 0000 0010 3AB8 B008 1EEA B64D 0000 0000
      204DD8: 0000 0000 0020 4E24 0000 0000 4E73 4E73
      204DE8: 0000 0004 0000 0002 0010 3ABA 0000 0000
      204DF8: 0000 0000 0000 F07C 0000 0000 0000 0000
      204E08: 0000 0040 0000 0000 2034 0000 0020 4E24
      204E18: 0000 0000 0011 FF34 0000 0000 0000 0000
      204E28: 0000 0000 0000 0000 0000 0000 0000 0000
      204E38: 0000 0000 0000 0000 0000 0000 0000 0000
      204E48: 0000 0000 0000 0000 0000 0000 0000 0000
      204E58: 0000 0000 0000 0000 0000 0000 0000 0000
      204E68: 0000 0000 0000 0000 0000 0000 0000 0000
      204E78: 0000 0000 0000 0000 0000 0000 0000 0000
      204E88: 0000 0000 0000 0000 0000 0000 0000 0000
      204E98: 0000 0000 0000 0000 0000 0000 0000 0000
      204EA8: 0000 0000 0000 0000 0000 0000 0000 0000
      204EB8: 0000 0000 0000 0000 0000 0000 0000 0000
      204EC8: 0000 0000 0000 0000 0000 0000 0000 0000
      204ED8: 0000 0000 0000 0000 0000 0000 0000 0000
      204EE8: 0000 0000 0000 0000 0000 0000 0000 0000
      204EF8: 0000 0000 0000 0000 0020 5002 0020 5102
      204F08: 0000 0000 0000 0000 0000 0000 0000 0000
      204F18: 0000 0000 0000 0000 0000 0000 0000 0000
      204F28: 0000 0000 0000 0000 0000 0000 0000 0000
      204F38: 0000 0000 0000 0000 0000 0000 0000 0000
      204F48: 0000 0000 0000 0000 0000 0000 0000 0000
      204F58: 0000 0000 0000 0000 0000 0000 0000 0000
      204F68: 0000 0000 0000 0000 0000 0000 0000 0000
      204F78: 0000 0000 0000 0000 0000 0000 0000 0000
      204F88: 0000 0000 0000 0000 0000 0000 0000 0000
      204F98: 0000 0000 0000 0000 0000 0000 0000 0000
      204FA8: 0000 0000 0000 0000 0000 0000 0000 0000
      204FB8: 0000 0000 0000 0000 0000 0000 0000 0000
      ```

- it seems to be 0, which shouldn't cause an address error (I don't think).  Unless it's a special case in the hardware?
- I wonder if it got clobbered by the RTE instruction, and it didn't push the data back onto the stack, so the only way I can know
  is by catching it before the error

- I mean it's probably because a stack is being shared or something, and there's no copy protection yet, since copy on write is not
  implemented


2025-11-23:

- I've added some code to the MMU mode to copy the stack, keep the old segment in the newly forked segment, and also increment the
  page refs of all the segments, and it's now crashing on pid 4 instead of pid 3, in the same spot, where the RTE instruction in
  restore_context is executed
- maybe it's some other code that's swapped with CONFIG_MMU that's causing the the context to be asymmetric?  The stack after the
  address error info seems to be all 0s, as if it's either the wrong address or it wasn't copied or something
- can't find anything...

- Ok, I printed out the memory map for exec'd and forked processes, and there's definitely something wrong somewhere somehow
- also pid 3 is created, but then forked before it's exec'd, which I think is where it runs `sh` on /etc/rc, so maybe that's a weird
  case that's causing problems here?  I haven't tried compiling the utils into the kernel in a while

      ```
      ...
      new process exec'd pid 1
      pid 1 memory map:
      203700 to 204000: flags=51
      204000 to 204200: flags=32
      204600 to 204600: flags=33
      204600 to 206600: flags=134
      begin multitasking...
      new process forked pid 3
      pid 3 memory map:
      203700 to 204000: flags=51
      204000 to 204200: flags=32
      204600 to 204600: flags=33
      204600 to 206600: flags=134
      207200 to 207200: flags=33
      207200 to 209200: flags=134
      new process forked pid 4
      pid 4 memory map:
      203700 to 204000: flags=51
      204000 to 204200: flags=32
      204600 to 204600: flags=33
      204600 to 206600: flags=134
      207200 to 207200: flags=33
      207200 to 209200: flags=134
      20A100 to 20A100: flags=33
      20A100 to 20C100: flags=134
      new process exec'd pid 4
      pid 4 memory map:
      20C300 to 20F300: flags=51
      20F300 to 20F800: flags=32
      20FD00 to 20FD00: flags=33
      20FD00 to 211D00: flags=134

      Error in pid 4 at 103C48 (status: 0, vector: 2)
      pid 4 memory map:
      20C300 to 20F300: flags=51
      20F300 to 20F800: flags=32
      20FD00 to 20FD00: flags=33
      20FD00 to 211D00: flags=134
      Stack: 209AA4
      209AA4: 0000 0010 3C48 B008 1EEA B64D 0000 0000
      209AB4: 0020 DB08 0020 9B00 0000 0000 4E73 4E73
      209AC4: 0020 DB0C 0020 DB0A 0010 3C4A 0020 DB08
      209AD4: 0000 0000 000F F07C 0000 0000 0000 0000
      209AE4: 0000 7CF0 0000 0000 2034 0000 0020 9B00
      209AF4: 0000 0000 0011 FF34 0020 DB08 0020 9C0A
      209B04: 0000 0000 0000 0000 0000 0000 0000 0000
      209B14: 0000 0000 0000 0000 0000 0000 0000 0000


      Code: 103C48
      103C48: 4E73 2079 0010 DFBC 2268 000C 5889 48E1
      103C58: 0F3E 4E7A 4800 2304 233C 0000 0040 2149
      103C68: 000C 4EB9 0010 1270 2079 0010 DFBC 2268

      freeing 209300 to 209B00
      new process exec'd pid 3
      pid 3 memory map:
      211E00 to 214E00: flags=51
      214E00 to 215300: flags=32
      215300 to 215300: flags=33
      215300 to 217300: flags=134

      Error in pid 3 at 103C48 (status: 0, vector: 2)
      pid 3 memory map:
      211E00 to 214E00: flags=51
      214E00 to 215300: flags=32
      215300 to 215300: flags=33
      215300 to 217300: flags=134
      Stack: 206DA4
      206DA4: 0000 0010 3C48 B008 1EEA B64D 0000 0000
      206DB4: 0021 3608 0020 6E00 0000 0000 4E73 4E73
      206DC4: 0021 360C 0021 360A 0010 3C4A 0021 3608
      206DD4: 0000 0000 000F F07C 0000 0000 0000 0000
      206DE4: 0000 7CF0 0000 0000 0020 0000 0020 6E00
      206DF4: 0000 0000 0000 0021 0021 3608 0000 0000
      206E04: 0000 0000 0020 6F0A 0000 0000 0000 0000
      206E14: 0000 0000 0000 0000 0000 0000 0000 0000
      ```

- the key thing to note here is that the stack address for the first process (pid 4) that crashes is 0x209AA4 which doesn't appear
  in any of the segments
- actually I guess that's the kernel stack, and not the user stack, which makes sense to not be mapped, but also, it's wrong...  Is
  the problem with the kernel stack not being copied?  That would make a lot more sense here
- there's definitely something weird with arch_clone_task_info, but it's using USER_MODE to select whether to use one stack or two,
  so it might not be that alone that's the problem.  Either way, the call needs to move or be copied somewhere else so that MMU code
  will do the right thing when copy_stack is eventually bypassed when MMU is enabled

- ARGHH looking at the manual again, it's a BUS error that's occurring NOT and address error.  It's the same exception frame format
  that occurs, but it's a bit different.  I didn't think there was an odd numbered address, which would cause an address error, but
  a bus error has different causes
- From the manual: "A bus error exception also occurs when the MMU detects that a successful address translation is not possible"
- so I think it's using 0 as the return address, and 0 is not mapped to any segments, which is why it gives a bus error (or most
  likely does).  That still doesn't change the above investigation, since there's still a problem with the stack that makes it be
  all zeros.  This just explains it properly

- everything boots fine, with the arch_clone_task_info() call moved up to the common code, and with CONFIG_MMU not enabled, but when
  I then also disable CONFIG_M68K_USER_MODE, when the `sh /etc/rc` command completes, it dumps me back to the monitor, with no
  exception... that really looks like it rte'd to a zero stack, but the MMU didn't catch it this time... it could also be mem ops or
  something being called on a NULL pointer, so I'd have to trace it a bit to confirm


2025-11-28:

- yesterday I looked into the problem where if CONFIG_M68K_USER_MODE was disabled, it would start to boot, and run /etc/rc but then
  drop to the monitor right after.  I was testing with the wrong config setting to compile the shell and init into the kernel, but
  once I got that right, and had it all in the kernel space, it booted fine.  I seem to recall I had this issue before which I maybe
  didn't fix, where when it uses the init on the compactflash disk, it fails on boot?  At least it's not a fundamental stack issue
  or something

- I tried disabling the MMU, since I haven't actually changed the address space much, and it should be using physical addresses in
  most locations.  It booted fine and the I could run programs in the shell, so it's not a critical issue.  It's an invalid MMU
  table that's causing the infinite exceptions
- I had tested it with the shell and init compiled into the kernel, and it was giving a different error, namely:

      ```
      Error in pid 1 at 103C64 (status: 0, vector: 2)
      pid 1 memory map:
      0 to 45400: flags=51
      203900 to 203900: flags=33
      203900 to 205900: flags=134
      Stack: 2034A4
      2034A4: 0000 0010 3C64 B008 1EEA B64D 0000 0000
      2034B4: 0010 B770 0020 3500 0000 0000 4E73 4E73
      2034C4: 0010 B774 0010 B772 0010 3C66 0010 B770
      2034D4: 0000 0000 000F F07C 0000 0000 0000 0000
      2034E4: 0000 7CF0 0000 0000 2034 0000 0020 3500
      2034F4: 0000 0000 0011 FF34 0010 B770 0020 360A
      203504: 0000 0000 0000 0000 0000 0000 0000 0000
      203514: 0000 0000 0000 0000 0000 0000 0000 0000
      ```
- the actual fault address in this case is 0x10B770.  Not sure if I was reading the wrong address from previous traces.  It's the
  first two words on the second line of the trace (words 9 and 10).  Looking at the traces above, the first exception occurs on
  address 0, and the second exception occurs on address 0x103600, which isn't mapped, and in the second of the two traces above, the
  addresses are 0x20DB08 and 0x213608.  In the first pair, the addresses are totally wrong.  In the second pair, only the first
  address is not in the map, and the second fault address does appear to be mapped.  Either way, this is a good step forward

- ok, in the case where the init/shell are compiled in, I'm using the linker's addresses for the kernel start and end, but they
  don't take into account the actual start and end it would seem, so they're 0 and 45400?
- kernel start is definitely 0, but kernel end is 0x115000 according to the symbols table...
- tucking the `__kernel_start = .;` line into the .text section fixed the starting symbol address at least
- but it still calculates the wrong end address...
- fixing that with a cast to uintptr_t and now it's got a fatal kernel error

      ```
      Fatal Error at 101B2A (status: 2700, vector: 2). Halting...
      Stack: 11FEF4
      11FEF4: 2700 0010 1B2A B008 16EA 076D 0681 FFFF
      11FF04: 021F E0BE 021F E0BE 0020 2100 5272 0800
      11FF14: 0010 1B32 0010 1B30 0010 1B2E 0020 0000
      ```

      ```
      00101b00 <page_block_make_ref_contiguous>:
        101b00:       2f0a            movel %a2,%sp@-
        101b02:       226f 0008       moveal %sp@(8),%a1
        101b06:       206f 000c       moveal %sp@(12),%a0
        101b0a:       222f 0010       movel %sp@(16),%d1
        101b0e:       2008            movel %a0,%d0
        101b10:       90a9 0010       subl %a1@(16),%d0
        101b14:       e088            lsrl #8,%d0
        101b16:       d080            addl %d0,%d0
        101b18:       0c81 0000 00ff  cmpil #255,%d1
        101b1e:       6206            bhis 101b26 <page_block_make_ref_contiguous+0x26>
        101b20:       2008            movel %a0,%d0
        101b22:       245f            moveal %sp@+,%a2
        101b24:       4e75            rts
        101b26:       2469 0018       moveal %a1@(24),%a2
        101b2a:       5272 0800       addqw #1,%a2@(0,%d0:l)
        101b2e:       0681 ffff ff00  addil #-256,%d1
        101b34:       5480            addql #2,%d0
        101b36:       60e0            bras 101b18 <page_block_make_ref_contiguous+0x18>
      ```

- the fault address is 0x021f_e0be which is totally wrong, and it comes from a relative address in page_make_ref_contiguous, which
  is incrementing the per-page refcounts, so definitely something wrong there, probably the wrong page number is passed in or
  something

- with the external init/shell it's back to:

      ```
      Error in pid 4 at 103C08 (status: 0, vector: 2)
      pid 4 memory map:
      20C300 to 20F300: flags=51
      20F300 to 20F800: flags=32
      20FD00 to 20FD00: flags=33
      20FD00 to 211D00: flags=134
      Stack: 209AA4
      209AA4: 0000 0010 3C08 B008 1EEA B64D 0000 0000
      209AB4: 0020 DB08 0020 9B00 0000 0000 4E73 4E73
      209AC4: 0020 DB0C 0020 DB0A 0010 3C0A 0020 DB08
      ...

      Error in pid 3 at 103C08 (status: 0, vector: 2)
      pid 3 memory map:
      211E00 to 214E00: flags=51
      214E00 to 215300: flags=32
      215300 to 215300: flags=33
      215300 to 217300: flags=134
      Stack: 206DA4
      206DA4: 0000 0010 3C08 B008 1EEA B64D 0000 0000
      206DB4: 0021 3608 0020 6E00 0000 0000 4E73 4E73
      206DC4: 0021 360C 0021 360A 0010 3C0A 0021 3608
      ```

- the fault addresses are 0x20db08 and 0x213608 which are both mapped locations...
- the page descriptors *must* be what's wrong here.  Maybe everything else is ok but the page tables themselves aren't set
  correctly, since I'm using physical addresses still and not using the page fault exception to load a page on demand

- I've been modifying and printing and running for a while now, and I'm pretty sure the mmu table is wrong, which is causing the
  address error.  I wrote a function to print the entire table in the exception, as well as in the when the memory is mapped at the
  start, and it prints out the tables correctly when mapping them, but not during the exception.  It skips over a large chunk that
  includes the fault address

- found it.  During mapping there's this output:
    ```
    mapping to 20C200: 20C300 to 20F300
    mapping 20C300 length 3000 flags 3
    set new table 20F800 [table: 20C200, i: 0, bits: 26]
    set new table 20F900 [table: 20F800, i: 2, bits: 20]
    set new table 20FA00 [table: 20F900, i: 3, bits: 14]

    <the entire range from 20C300 to 20F200>

    mapping to 20C200: 20F300 to 20F800
    mapping 20F300 length 500 flags 3
    set new table 20FB00 [table: 20F800, i: 2, bits: 20]
    set new table 20FC00 [table: 20FB00, i: 3, bits: 14]
    0020F300: 20F300 (1) [table: 20FC00, i: 51, bits: 8]

    <the range from 20F300 to 20F800>
    ```

    It overwrites the same entry in the table located at 0x20F800, at index i=2, at the address level bits=20, and then when it
    prints the table later, it never prints table 0x20F900, which has been erased and leaked by that point

- actually, now I'm not sure the output was correct?  yes it's correct, but I still don't understand how it gets overwritten
- it's really late (12:30am) and I need to get to bed.  I'll have to find this tomorrow.  I'm so close though (I've said this before
  though, haven't I?)


2025-11-29:

- I'm sure this kind of problem would be easier with an actual debugger.  I wonder how hard it would be to connect gdb?  I know gdb
  has a remote target option, and we have that working where I work.  In that case, it's using a semi-substantial amount of python
  code to glue it all together but I wonder if there's a pre-made solution using serial that would be easier to integrate.  It might
  only need the target-side code to be written
- using print statements for the time being, but boy do I have a lot.  I can't see any reason why the table, 20F800, would have its
  entry point to the correct new table, and then when the next segment is mapped, it has 0 again in that spot, causing it to
  erroneously create another table which overwrites the first.  This really seems like a bug from memory reuse, but I haven't yet
  confirmed that
- I have a hard coded watcher that prints the value of the offending memory location before and after entry into the mmu table
  mapping function
      ```
      <call to mmu_table_map to map 20C300 to 20F300>
      0020F200: 20F200 (1) [table: 20FA00, i: 50, bits: 8]
      >>> watcher after mmu_table_map 20F902
      mapping to 20C200: 20F300 to 20F800
      >>> watcher before mmu_table_map 0
      mapping 20F300 length 500 flags 3
      ...
      ```
- So the first call ends with the memory location set correctly, and then somewhere outside of the function, between the two calls,
  that memory location is set to 0...  I think that's conclusive that the problem is not in mmu_table_map() but somewhere else, so
  I'll check further up the stack
- Ok, put a watcher print statement on entry to memory_map_mmap(), and one on entry to memory_map_unmap()
      ```
      ...
      0020F200: 20F200 (1) [table: 20FA00, i: 50, bits: 8]
      >>> watcher after mmu_table_map 20F902
      >>> watcher entering memory_map_mmap 0
      mapping to 20C200: 20F300 to 20F800
      >>> watcher before mmu_table_map 0
      mapping 20F300 length 500 flags 3
      ...
      ```
- So it's definitely not calling unmap, which is good to confirm, but also the problem is occurring outside of mmap as well...  I
  think this is called from fork, since it should be the shell forking itself...
- wait, could it be the fork itself that's broken, which would make the most sense.  That code isn't well tested, but I thought I
  had fully tested the mmap code already, which is why this has been so confusing, but it's definitely a memory misuse or
  use-after-free issue.  Did I test what happens when it doesn't fork to run /etc/rc?  I'm using it without the shell built into the
  kernel blob
- it's definitely that spot in the init process where it checks for /etc/rc, then forks, and execs if it does (which creates pid 4
  to run /etc/rc), and then the parent calls exec on itself to run the shell
- there aren't many spots this could be.  I have another print in duplicate_process_memory(), which is the call to mmap
- Oh! it's definitely not that spot.  That's not the call to mmap, so I'll look for another location.  This must be the one in
  binaries, which is called by exec()
- I notice a memset() call in here, to zero part of memory.  I wonder if that's causing the problem.  A factor here is that the
  memory addresses being mapped to and the memory locations of the tables themselves are all jumbled together, so a wrong address or
  size could easily overflow into a memory table
- aaaand that looks like exactly the problem, the memset.  It's overflowing into the table allocated at 0x20F800
      ```
      ...
      0020F200: 20F200 (1) [table: 20FA00, i: 50, bits: 8]
      >>> watcher after mmu_table_map 20F902
      >>> watcher in exec on program segment 1: 20F902
      memset 20F44C length 464
      calling memory_map_mmap with 20F300 to 20F800
      >>> watcher entering memory_map_mmap 0
      mapping to 20C200: 20F300 to 20F800
      >>> watcher before mmu_table_map 0
      mapping 20F300 length 500 flags 3
      found 20F802 at [table: 20C200, i: 0] so checking 20F800
      descending into 20F800 [table: 20C200, i: 0, bits: 26]
      found 0 at [table: 20F800, i: 2] so checking 0
      ...
      ```
- so the watcher after mmu_table_map shows the correct value in the page table, and returns to the for loop in binaries during
  exec(), which then loops to the next program segment in the elf, and prints the watcher in exec at the top of that for loop, where
  the value is still correct.  It then calls memset() which will write to 0x20f44c to 0x20f8b0!  0x20f808 is our watched location
  that's changing erroneously!
- alright, so what's the solution here.  I think I just have the alignments wrong, or maybe I'm not allocating enough memory
- this is of course running it from disk, and the build of sh.elf might not match what's on disk.  It's probably been 6 months at
  least since I last recompiled and copied the version to the compactflash it's running from.  I want to get the address right here
  for future reference
- the program segments for sh.elf which is being executed are:
      ```
      Program Headers:
        Type           Offset   VirtAddr   PhysAddr   FileSiz MemSiz  Flg Align
        LOAD           0x000100 0x00000000 0x00000000 0x02fb9 0x02fb9 R E 0x100
        LOAD           0x0031b4 0x000030b4 0x000030b4 0x00098 0x004fc RW  0x100
        GNU_STACK      0x000000 0x00000000 0x00000000 0x00000 0x00000 RW  0x10
        GNU_RELRO      0x0031b4 0x000030b4 0x000030b4 0x0004c 0x0004c R   0x1
      ```
  and it's being loaded into the two segments:
      user_mem_start: 20C300, file_segment_start: 20F2B9, file_segment_end: 20C300, memory_segment_start: 20F300, memory_segment_end: ???
      user_mem_start: 20F3B4, file_segment_start: 20F44C, file_segment_end: 20F300, memory_segment_start: 20F800, memory_segment_end: ???
  (not sure why the memory_segment_end didn't print correctly, it was cut off... a bit odd, but maybe unrelated)
- it's the memset of the second segment that overwrites the table

- ok, taking a step back... I was just trying to collect some data first but I already notice a problem.  user_mem_start is
  different between those two print statements when it should be the same both times...  there is no code that could change the
  value of that.  This now seems suspiciously like stack clobbering, but it's in the kernel, so it must be the kernel stacks being
  clobbered?
- the kernel stack size is set to 8 (pages) and page size is 256, so that's 2kB for each process's stack, but it should be using the
  global stack at this point in the code, shouldn't it?
- this might explain why the print statement was cut off...
- so printing out the stack pointer, it's always 0x11FE60 in the elf loading loop
- unrelated, but the linker is putting the text section at the end for some reason, even though the linker script says to put it at
  the start =/
- I can't find a problem here, the code ends at 0x10beaa, BSS goes up to 0x11025c, and then the stack is there with a reserved size
  but the actual stack starts at 0x120000, and it's only at 0x11fe60, so plenty of room
- this is soooo weird!  I'm also running out of time again, so will have to pick this back up later, but before I do I'll try
  printing out the user_mem_start address and value
  `user_mem_start: 11FE90, value: 20C300`
  ...
  `&user_mem_start: 11FE90, value: 20C300`
- Ok! so it's not a kernel stack clobbering problem
- *facepalm* it's a print statement mistake.... I'm not printing the user_mem_start, I have the wrong count

- Ok, lets try this again
- it's still glitching the print statement format string, so bizzare
- motherfucker, the printk buffer is only 128 characters, and my line was too long...

- Ok, the data is loaded into memory using these values:
  ```
  user_mem: 20C300, file_seg_start: 20C300, file_seg_end: 20F2B9, memory_seg_start: 20C300, memory_seg_end: 20F300
  user_mem: 20C300, file_seg_start: 20F3B4, file_seg_end: 20F44C, memory_seg_start: 20F300, memory_seg_end: 20F800
  ```
- for the second segment, virtaddr + filesiz = 0x314c, so the file segment end of 0x20f44c makes sense, and with memory size it
  would be: virtaddr + memsiz = 0x35b0, which matches 0x20f8b0... so memory segment end is rounding down when it should round up?
- it's specifically using 0x20F300 (the rounded down address) + 0x4fc and rounding that up!  So it's almost right, but since it's
  using the value that's rounded down instead of the one that has an offset of 0xb4, it ends up being a page less than it should be

- HAHA *sigh*, I changed the calculation to use file_segment_start instead of memory_segment_start when calculating
  memory_segment_end, and now it fails to create init task, error -22, which is EINVAL, probably coming from one of the checks in
  memory_map_mmap
- Oh, actually it was still wrong.  It was only rounding up the size and not the whole value (the start plus the size)
  `memory_segment_end = memory_segment_start + roundup(prog_headers[i].p_memsz, PAGE_SIZE);`
  changed to
  `memory_segment_end = roundup(file_segment_start + prog_headers[i].p_memsz, PAGE_SIZE);`
- aaaand it's still broken
- it is now mapping more memory, correctly, but the fault is the same
- Oh of course, I forgot to also fix the memory calculation further up, so it's still only reserving up to 0x20F800, but mapping
  up to 0x20F900, so of course it wouldn't work.  It's still using 0x20F800 as the mmu table, which gets overwritten
- this isn't going to be trivial to fix, since that code is just adding up the memory_size value for each segment, but it needs to
  factor in the rounding.  I'll have to come back to this later
- this has problem been the hardest bug to find and fix, made worse by the fact that I've had very little time to spend on it, so
  it's taken me forever to actually get into it enough to find it.  I'm also not surprised since I've had so much difficulty getting
  the elf values to work, both here and in ruxpin when I added elf support


2025-11-30:

- didn't get any more time yesterday, so now I'm back at it.  [not quite, ended up writing in journal instead, so I'll come back to
  this later today]

- fixed it, and it seems to run further, and then has a fault in pid 5 this time at address 0x103e74.  I'm not sure if I just last
  ran with another build of the binary, but 0x103e76 is the user_exit() function that I knew would be a problem when I got to this
  point.   So I think it's fixed, but there's one more problem in the way

- I finally got past that error by hackishly adding a segment during exec() for the page that the exit signal is located inside.  It
  didn't work first time of course because it needed an exception for if that area is already mapped or else it would fail to create
  the init process.  That seems... incorrect, since it should only be adding a kernel segment if it's not launching a binary, but
  adding the exception got past that fault
- aaand now I'm back at the same fault, in pid 4 on the RTE instruction, which leads to an exception loop.  I can now see the mmu
  table, and it's definitely correct and has the faulted address mapped correctly.  I'll check the MMU status byte, which is 1, to
  make sure it's correct
- hmm... I see that there WRITE PROTECT bit instead of a WRITE bit, and that's every page's status is just 1, so that bit is not set
  for read only data, and the other bits are not set correctly either.  That needs to be fixed.  *Maybe* it explains why it's still
  not working?
- wtf is going on, I added WP on exec and it failed to create_init_task, and then I removed that and now it's failing on pid 5 on
  the _user_exit address, which isn't in this process's memory map, but two executables loaded show... oh that print statement is
  probably printed no matter what, rather than being inside the conditional.  I'll move it
- ok added that, and then realized the conditional is wrong, it should be inverted...  If it was working before, I'd expect it to
  fail now, even though that should have fixed it... something weird
- yup, fails when adding the user exit segment
- I forgot the bitwise complement on the (PAGE_SIZE - 1) so it was the wrong address.  Rather than rounding the address to a page,
  it was preserving only the unaligned portion of the address
- now I'm getting a fatal error in kernel mode when it begins multitasking, which hints that another error is occur as a result of
  this new code, before it even gets to the original exit error point.  It's a fault when accessing 0x21FE3A, which is the kind of
  address a process would use, but it's not one that the executable loaded has and the fatal is in kernel mode.  At least I'm
  further?

- no actually, the fault address is 0x21F_E13A, which is a garbage address


2025-12-03:

- Tried commented out the new binaries.c lines that check for an existing kernel memory map location, and if not, creates one for
  the _user_exit and _user_sigreturn functions
- without it, it has the fault on exiting pid 4, and with it, it faults in kernel space with the address being 0x101B2A
      ```
      mmu: setting TCR to 82806666
      mmu: read back TCR of 82806666
      mem0: ram disk of 131072 bytes
      ata0: found partition with 524288 sectors
      ata1: found partition with 1048576 sectors
      minixfs: mounting (400) at /
      procfs: mounting at /proc
      slip0: bringing interface up
      adding _user_exit: 103E00
      LOAD vaddr: 0 file_addr: 203700, filesz: 83A memsize 83A, mem_start: 203700, mem_end: 204000
      LOAD vaddr: 93C file_addr: 20403C, filesz: 34 memsize 178, mem_start: 204000, mem_end: 204200
      begin multitasking...


      Fatal Error at 101B2A (status: 2700, vector: 2). Halting...
      Stack: 11FEF4
      11FEF4: 2700 0010 1B2A B008 16EA 076D 0681 FFFF
      11FF04: 021F E13A 021F E13A 0020 2100 5272 0800
      11FF14: 0010 1B32 0010 1B30 0010 1B2E 0020 0000
      11FF24: 0800 FF0B 000F F681 0000 0000 0000 0000
      11FF34: 0000 00FF 0000 0000 0020 0000 021F E13A
      11FF44: 0000 0001 FFF0 1D00 021F E13A 0012 18B8
      11FF54: 0010 23C8 0010 CB48 0010 3E00 0000 0100
      11FF64: 0000 0000 0000 0000 0010 D994 0011 FFB4


      Code: 101B2A
      101B2A: 5272 0800 0681 FFFF FF00 5480 60E0 48E7
      101B3A: 3020 246F 0010 206A 0004 2628 001C 0683
      101B4A: 0000 02FF 0243 FF00 42A7 2F0A 4878 0041

      Welcome to the 68k Monitor!
      ```
- that address is in `page_block_make_ref_contiguous`
- I think it's now creating the memory map eariler on and it's failing because it's trying to treat it like an allocated block of
  memory?
- yeah, in fork is the only place where page_make_ref_contiguous occurs, and it's when duplicating the parent process's memory.
  There's no flag for making windowed memory in the memory_map_mmap function, but this code in fork.c would have to selectively
  ignore calling the page_make_ref function, even though it's slated to be removed later... not sure why?  Oh I think I remember.
  It's because it'll be using copy_on_write later, so will increment refcounts during that function instead

- how does linux do this?  Does it just have a window flag accessible through mmap(2)


2025-12-04:

- I put a simple check around page_make_ref_contiguous, and now it boots into an infinite fault loop starting with pid 4, which is
  where we started from... It's on 103cc8, which
- WAIT A MINUTE ****************
- that's to remind myself to notice this, this happens a lot where I see the code address of the fatal error, but that's the
  location going out of the kernel, which is a common time to fault.  Instead, there's a separate fault address on the stack, for a
  bus error, which is what occurred, and that address is 0x20E108
- how many times am I going to make this same mistake again?
- well, that said, the address is mapped as the code segment from 0x20C900 to 0x20F900, so it should be fine
  From the MMU table:
  `20E100: 20E100 to 20E1FF (1) [table: 210300, i: 33, bits: 8]`
- or it is actually the next address that is the fault address?  It's 0x209F00, which isn't mapped, but according to the refman,
  that's an internal register and not the fault address

- this is just dumb.  It's now failing on some address within the user process's code segment.  It's failing on address 0x20db08,
  when the code segment starts at 0x20c300.  Which would mean it's 0x1808 into the shell (I think), that fails?  That's the
  instruction after a bsr.l to strcmp, so that doesn't make sense as a fault address... maybe it ran the process for a bit and then
  halted when returning from an interrupt?  So unrelated to the process execution?


2025-12-05:

- well at least today after sitting off, it's still failing at the same spot, so it's not a cache or TLB issue
- thorpej was mentioning chasing a bug that involved the 68030 adding negative TLB entries into the TLB cache.  I'm not doing any
  cache flushing yet, so that could be a bug I come across, but for now I don't think that's the problem

- I tried printing out the value of the root table, and the CRP root pointer when printing out the table, and lo and behold:
   `??? root table: 20C200, MMU: 209B00 (2, 7FFF)`
- the current pid should have the root table 0x20c200, but the CRP is set to use 0x209b00, which is also a valid table, so that's
  good, but it's the wrong table, which would explain the faults.  It must not be switching correctly
- what the FUUUCK, added a print statement to print the value of CRP before returning to the process, and not only is it working in
  that the CRP matches the pid's root table, it now fails on the _user_exit error
- something is very weird, where sometimes it works and sometimes fails due to another error that seems to happen with certain
  kernel builds.  It's reproducable with the same kernel image, but each time i build the kernel, there's a chance it will switch
  between the possible errors

- I turned off the system over lunch, and then turn it on, uncommented the conditional around the page_make_ref_contiguous call in
  fork, and it's now switched to erroring on 0x20DB08, and the CRP is wrong in the only two restore_context calls that returns to
  pid 4
- restoring the last change I made doesn't make it switch back to _user_exit error

- ok, it's still erroring on 0x20db08, but I can see the bug now, and it's fairly obvious.  When exec() is called, the new process
  uses the old memory map, and that gets replaced, but when it returns to the process, since it's the same process struct/id, it
  won't call the arch_extended_switch_context(), so it's using the old process's memory table
- so how to fix it?  Should the context switch be explicitly called in exec() somewhere (or binaries.c?) or should it invalidate the
  previous_process value such that it will always to a full switch?
- I feel like for the latter case, there's probably some code from before MMU additions that tries to handle that case.  It will
  either handle it fine, or break because it expects previous_proc to only be NULL on first run

- I added a conditional and `previous_proc = NULL` to the load_binary() function at the end, which should clear the previous_proc
  and hopefully do a full context switch when returning.  Running that, it then errored on the _user_exit call, which is expected,
  but didn't error on the other odd one
- I enabled the exception for _user_exit and it's now failing on a kernel error much earlier on, which is a bit weird, 0x101b2a is
  the code position, and 0x21fe13c is the fault address, which I think is the same as I was getting somewhere above
- oh I thought the conditional around page_make_ref_contiguous was still there, but it was commented out

- now we get pid 2 error, fault address 0x103854, code address 0x103d46.  The code address is the rte instruction in restore_context
  and the fault address is the idle task loop, so something's broken with the _user_exit exception code??

- ok I just removed the _user_exit function from the stack, although I could have just ignored it and lookd at previous runs, but
  the first process just exits and terminates abnormally, the second process is pid 6 which terminates on 0x217d8c, which is mapped
  in the table, buuut `??? root table: 217B00, MMU: 209F00 (2, 7FFF)`, so the previous_proc hack isn't enough?
- but it seems to call an exec() from the other print statements?  So why didn't previous_proc not work?

- I added more print statements, and of course it doesn't fail like before, but now it fails again at the idle process, which it
  then can't recover from, and it turned out the kernel thread creation code wasn't creating an exception for the kernel itself.
  That might have been the wrong fix, to add it to alloc_kernel_thread, since I think it should be sharing the same memory map, but
  it didn't have the code segment (it was empty).  Maybe the problem is actually elsewhere.
- even with that change, it's still showing pid 2's memory map as being empty?  What gives?  I guess something went wrong, like a
  refcount didn't work, so it freed the kernel's table when it shouldn't have
- oh there's actually a another function for creating the idle task.  I need to clean up those functions in init.c

- adding the kernel code segment to the idle process's memory map, I think fixed the last problem, so now it actually gets to the
  shell, and I can run ls from there!


2025-12-06:

- looking at the diffs, there were a lot of things that were wrong and needed fixing
    - the binaries size and offset calculations were wrong, so it wasn't allocating enough memory for each program segment
    - it wasn't allocating memory for the stack, which was causing it to be reused (I think)
    - the code without the stack duplication would not have created the task info struct on the stack
    - it wasn't incrementing the per-page refcounts
    - the process wasn't doing a full context switch after an exce() call, which meant the mmu crp register wasn't set correctly
    - the idle task didn't have a segment for the kernel code
    - the processes that still require _user_exit didn't have an exception so that they could access it
    - it would sometimes try to free a NULL file ptr when freeing a memory segment with file = NULL

- and that's just the ones in the most recent work, not including the commits I made to clean up the diff
- has this taught me to take it really slow and incrementally?  No
- I need to clean this up and commit it, and then fix the _user_exit issue properly, and then I'll need to work towards not
  duplicating the stack by getting copy on write to work.  That will require getting page faults working, primarily
- I also need to test with CONFIG_MMU=n to make sure my recent changes didn't/don't break anything

- the issue with _user_exit, I think, is moreso the kernel threads, to avoid the need for some kind of weird generic main function
  that calls exit at the end


- I tried banging out a load_page_at impl for anonymous memory, and made the stack use it when allocating.  It's not working, but
  I'll debug it tomorrow


2025-12-07:

- well that was relatively easy to figure out.  There was a page fault when accessing address 0, but that didn't make much sense
  The difference between working and not was a line commented out: `start = page_alloc_contiguous(...)` in
  memory_map_insert_stack_and_heap() to allocate memory for the heap/stack.  Without it, the stack segment would not be allocated
  and it would use the heap_start argument instead.  That change won't do what I expected it to do since mmap will still use WINDOW
  to map the segment, but there were a bunch of other bugs that occurred at the same time
- the error occurred in pid 0, which maked no sense at first.  It turns out since the segment is already mapped, it fails with an
  EEXISTS error when forking the shell, pid 3.  There's no error saying that that happened, but either way it bails out and kills
  the process
- at that point, the pid is set to 0, but it's still the current process!!!!  that's a big bug.  It runs a dead process.  I think it
  might still be in the run queue, which is why it says "switching to pid 0", even though it's what was pid 3, and the memory map is
  already NULL and freed
- when the page fault occurs, it doesn't call memory_map_load_page_at because the map is NULL, so it just prints the error and
  terminates
- so when a process is terminated during a fork at least, it might still be runnable

- also easy to fix, just replaced close_proc everywhere with exit_proc(), which is defined in the scheduler, and which calls
  close_proc, as well as removing the process from the queues.  I fixed it in clone() and in init.c

- pushing further, it also has the descriptor type set in the mmu tables despite being set to a table of NULL when using the
  MMU_FLAG_UNALLOCATED, so it would think it had correctly updated the tables and returned to the process


2025-12-13:

- I'm working on rewriting the mmu table mapping code so it can be more easily reused with another function to traverse and modify
  the mapping (for copy_on_write)
- what I wrote seems to work fine, I separated out a function that gets the most terminal table given a virtual address and returns
  it.  The mapping code can then just call this function when it gets to the end of the current table, and it will do all the
  complicated `bits` and stuff, and it doesn't need `levels` and multitiered table pointers because it just looks it up fresh each
  time it changes, which isn't often
- that said, there's a problem with it.  It's really slow on boot.  I'm pretty sure the old code would map with early termination
  descriptors, so when the kernel tables are created, it doesn't have to fill them all out.  That's likely the only code that would
  benefit from early terminators, but either way, I should add them again.  The question is how it should be requested in the
  get_leaf_table()


2025-12-17:

- I've been writing some of the necessary table manipulation code for eventually getting copy on write.  I had to refactor the map
  code so that some of it could be reused in other functions.  That allowed me to add a separate mmu_table_copy() function, which
  couldn't be easily integrated into the map function.  It will copy a range of addresses in an existing table to another table, so
  if some of that data is already assigned a page, the page will be assigned to both.  I'm sure there's still some bugs in it, at
  least related to page refcounts or something.
- I also implemented code to free an entire table.  I don't think it's being used yet, or maybe it is when the process is removed,
  but the map function doesn't properly remove the tables when unmapping.  I probably have some bugs in the unmap code since the
  page free call is weird
- one of the issues that probably still exists is the single page vs page block difference.  The mapping code can map an entire
  granuale (ARM's MMU term for a early termination).  With the current config of 256 byte pages, a 64kb region could be mapped and
  save an entire table.  I haven't tested that though, and it will likely run into other problems

- so the issue I'm facing now is that I've changed a bunch of things, and even though copy on write isn't implemented yet, most of
  the segment copying and such should be in place.  I compiled it without MMU though, and it boots into the shell, but crashes when
  running a command, so I've clearly got something wrong.  I'd like to fix this first before switching back to the MMU code, since
  this bug probably applies to both
- I'm pretty sure it's related to the new memory_map_copy() function, which is the only common code that's new

- didn't take long to find the issue.  The new copy code wasn't incrementing references on the memory region or file pointer, which
  for non-MMU mode means it would have freed and reused the memory region of a process.  It seems to be running fine with no
  exceptions in non-MMU mode

- compiling for MMU mode, there's a bunch of errors for some reason... I thought it was compiling fine before
- was just the wrong names for the other segment fields that are copied when copying the whole segment
- it boots into the shell with just the normal _user_exit exception, but when running a command like `ls`, it crashes
- it does seem to return to the shell though, and I can run a second command, but the command I run doesn't actually run.  It
  crashes quite soon after starting
- I think all the programs are crashing from a page fault @ address 0 (null pointer dereference), but since the program should be
  fine (exact code runs perfectly with no-MMU), then it must be memory related, either the stack getting clobbered because copy on
  write is not fully working, or some page is 0 or something?
- oh interesting, in the mmu tables, some of the status values for the page descriptors are 9 or 19 instead of just 1.  1 is the
  page descriptor type, 0x08 would be the U bit, which I think is the updated or dirty bit, so the CPU sets that when it's written
  to that page.  The 0x10 bit though is the M bit.  Oh, the M bit is modified, and the U bit is updated, which isn't the same as the
  dirty bit (M is the dirty bit).  U is set when the descriptor is accessed as part of a table search.  I'm not sure what the
  utility of that is to be honest


2025-12-18:

- so I've been playing with it in various ways.  I added the file-backed segment loading, which does seem to actually be working.
  I'm still using copy stack and leaving the anonymous memory as WINDOW for now
- it does seem to have page faults that make sense for running the init process, and it seems to load the data correctly, but it
  still ends with pid 1 terminating (and the idle process continuing to run in the background).  I was wracking my brain as to why
  it's failing, and I think I finally realized the issue
- it's likely using the wrong addresses when a systemcall comes in.  The user program has loaded the program into 0x40000 in virtual
  addresss, and the data segment will be in that range too, so any addresses passed into the kernel will be wrong, and the syscall
  will probably fail.  I would have expected it to print the failed to execute message at the end, but it doesn't.  It doesn't call
  exec() either, from what I gather.  But it's got to be the lack of address translation in the kernel that's messing things up,
  because it otherwise seems to be running correctly
- I had been hoping to put off the address translation changes for a while, but unfortunately everything is pretty interdependent.
  I think I might be better off backtracking a bit, disabling the changes that cause page faults, and implement the translation
  first, which should be a no-op in non-MMU mode, and with MMU enabled but phys addresses used, should translate to the exact same
  address

- so pretty sure that's the problem.  I confirmed it by printing out the address of the string passed into open()
- I tried putting it back so that it won't page fault anymore, but there's still one transient page fault.  When starting up, pid 6
  (which I think might be ntpdate or something in the /etc/rc) has a page fault for address 20AC88.  The address seems to be mapped
  correctly, and points to a fixed location in memory according to the table, but for some reason, it has that page fault!  Still
  trying to track down the cause
- my hunch is that the CRP or something is incorrect at that moment, which is why the page fault occurs?

- yup
  ```
  page fault @ 20AC88
  current 10E3C8, pid 6, map 125CA4, pc 103DF4
  root table for pid is 20AA00
  SRP 202100 (2)
  CRP 20A500 (2)
  printing table 20AA00
  ```
- 20A500 is the table for pid 5 (not pid 6), and it definitely doesn't have the address 20AC88 in its mapping
- it doesn't happen every time, sometimes I need to recompile and reupload.  I think I've tried booting again with the same memory
  contents and not had it happen twice, but maybe that's coincidence.  It probably has more to do with the properties of the timer,
  whether it just so happens to catch things at the right time
- it's either a context switch not happening when it should (possibly during a fork or exec), or it could be... well I was thinking
  could be stack corruption, but in this case, the table is for pid 5, but pid 5 has already exited, and only pid 5 would share a
  stack with pid 6?  Or is it pid 4 that forks pid 6, in which case they would share a stack, but an address for pid 5 shouldn't be
  on either pid 4 or 6's stack anyways...

- tried 2 times running `boot` again on the same load of the kernel, which doesn't seem to produce the 20AC88 page fault issue.  It
  boots normally, and times out when running ntpdate
- next I'll try reloading the kernel over serial again.  That produced the issue!  So reloading it has something to do with it, but
  not the compiling of it, but just `boot`ing again doesn't seem to cause it

- this is weird, I've put some messages into the code about the extended context switch, and it says, after pid 5 terminates, that
  it switches from NULL (ie. invalidated previous process) to pid 4 (twice), and then switching from 4 to pid 6 which has the page
  fault.  But despite the root table for pid 6 being 20AA00, the CRP register is still reading back as 20A500, so is it not being
  written always?  is it failing to write sometimes?
- Ah ha!
  `EXTENDED SWITCHEROO from pid 4 to pid 6 (with table 20A500)`
  So the root_table of the current_proc in the last extended context switch was 20A500, even though by the time the page fault
  occurs, it's 20AA00!  So why is it the same as pid 5's until later?
- Is it the same map, or just the same table?  I would assume it's literally the same map, and this is before the exec or something
- the plot thickens?  This is a working run, without the page fault issue
  ```
  EXTENDED SWITCHEROO from pid 5 to pid 4 (with table 20C400 and map 1232C8)
  EXTENDED SWITCHEROO from pid 4 to pid 5 (with table 20A500 and map 1230A8)
  ...
  EXTENDED SWITCHEROO from pid -1 to pid 4 (with table 20C400 and map 1232C8)
  EXTENDED SWITCHEROO from pid 4 to pid 6 (with table 20A500 and map 1230F8)
  EXTENDED SWITCHEROO from pid 6 to pid 4 (with table 20C400 and map 1232C8)
  EXTENDED SWITCHEROO from pid 4 to pid 6 (with table 20A500 and map 1230F8)
  exec
  EXTENDED SWITCHEROO from pid -1 to pid 6 (with table 20AA00 and map 125CA4)
  EXTENDED SWITCHEROO from pid 6 to pid 2 (with table 202C00 and map 120008)
  EXTENDED SWITCHEROO from pid 2 to pid 6 (with table 20AA00 and map 125CA4)
  ```
- so the map is different between pid 5 and early on in pid 6's life, but after the exec, the map and root are very different, which
  is the correct thing.  When the issue happens, it must be happening before the exec when the map is in a weird state

- ok, so I commented out the conditional around previous_proc = NULL in the exec code, hoping it would always do an extended switch
  when an exec occurs, but that didn't fix the problem.  It reoccurred after 3 attempts.  The page fault occurs right after `exec`
  is printed, at the start of the exec, and when it occurs, pid 6 does actually have does still have the same root, despite having a
  slightly different map address
- does it have to do with duplicate_memory_map() in fork.c?  that seems the most likely culprit

- what if the fact that the root table is the same is a red herring, because pid 5 exits before pid 4 forks to pid 6, so it might
  just reallocate the same table, but otherwise erase it
- this is so weird.  Printing out the mmu table on each switch, and pid 4, 5, and 6 seem to all have same MMU map, even when they
  have different root tables

- when the error occurs, it calls exec, and exec exits, and then it has the page fault as soon as rte is executed in
  restore_context, but it doesn't print that it did a context switch...  Could blocking print (or lack there of in this case) be
  part of the problem? (a delayed extended switch message)?  Or is it really just failing to call schedule
- well... it looks like the syscall entry code, where schedule() is called from, only checks if needs_reschedule is true or not, and
  skips it (I think, I just looked briefly), but that would make sense.  Invalidating the previous proc would not be enough
- there's an explicit `request_reschedule()` function that should be called from exec instead...  I want to make sure there isn't
  another bug here though, so I'll do some diligence before trying that.  Anyways, I'll stop for today and pick it up tomorrow


2025-12-19:

- ok I'm pretty sure I've confirmed that the same table address (20A500) is not a bug.  It's just reallocating the same page when
  the new process is created, and the table is empty at that point, before the segments are duplicated into it
- I'm still not sure why the segments are all the same between pid 4, 5,  and 6...
- I don't think it's a problem.  Pid 4 has those segments, and they appear when printing out the segments for pid 5 and 6 after the
  fork, as expected, but then those processes exec, and while I haven't looked closely at the tables after the exec of pid 5, I know
  pid 6 ends up different
- well actually it looks like pid 5 doesn't exec() which is a bit weird, but maybe that's because the shell has some utilities built
  into it?  It looks like it prints the `Running /etc/rc` message right after the fork, which is an `echo` command in the /etc/rc
  file

- Ok, I'll leave that as is.  Back to the original problem
- last night in bed, I quickly searched through the code and found that the syscall_entry.S file is checking the `needs_reschedule`
  flag for whether it should call `schedule()` or not, so `previous_proc = NULL` is not enough to guarantee the extended context
  switch happens!
- the `request_reschedule()` function will set the flag (that's all it does atm), and there's only one place that that's used, which
  is in the tty_68681.c file after the timer interrupt fires.  It calls that function to switch running tasks.  That might not quite
  be what I want for after exec() but at least it will force the extended switch
- the `previous_proc = NULL` on the other hand is used in two locations, one of which is the location in binaries.c that I recently
  added, and the other is in process.c in `close_proc()` which has a specific comment saying
        // Set the previous process to NULL so that we skip over attempting to
        // save the context during restore_context
- So setting previous_proc = NULL will skip over trying to save the context when it's returning to user space, which might not be
  what we want always (in terms of adding it to request_reschedule())
- that said, in binaries.c during exec(), we might actually want that, as in we don't want to save the extra registers of the
  process we're returning to, or if we switch to another process, we don't want to save the previous process's pre-exec() register
  values to the newly exec'd process's task struct, so I think it makes sense to invalidate the previous process in that case
- I don't like how it's directly modifying that value though, so maybe we can wrap it in a function
- how does linux deal with this?  minix might not have anything special since it might not have a delayed extended switch?

- in section 20.4 of Understanding the Linux Kernel 3rd edition, it talks about what happens during an exec(), and item 8 mentions
  it calls flush_old_exec(), which does a whole bunch of things to free resources from the old process before launching the new one,
  including releasing signal handlers, unsharing shared files, releasing all memory descriptors and such, and calling
  `flush_thread()` to clear the floating point registers and debug registers saved in the TSS
- it doesn't explicitly mention the MMU registers, but it would make sense for that to be part of flush_thread, or the memory
  freeing code.  Either way, I think there's a lot of things I haven't thought to reset during an exec, which I should

- after looking into the code, I found that the file descriptor table wasn't being freed properly.  I had modified it to have a
  refcount, and it was incrementing the refcount when cloned/forked with CLONE_FILES, but I was still using the `release_fd_table`
  function without freeing the allocation when the process exited.  The free_fd_table() function also wasn't checking the refcount,
  or releasing the table.  I've fixed that now so it actually decrements the refcount and frees the table properly if it reaches 0
- I added a reset_proc() function to process.c which will copy the stdin, stdout, and stderr files to a new table, and decrement the
  refcount to the old one (so if it's held by another process, it won't be modified when one of the processes exec()s).  The
  function also resets the signal table and the blocked and waiting process data.
- It might need to reset even more data.  I'm not sure about start time and such, and I think there was a reference to some of the
  pid fields being changed by an exec() as well
- I put the arch_extended_switch_context() call into the exec_initialize_user_stack_with_args call, and the kernel one too.  I don't
  like them being there, but it works for now
- I've tried loading it a bunch of times, and it seems to run ntpdate and reach the timeout every time now
- there were quite a few bugs fixed by all of this, which weren't caught before when the MMU was disabled, so I'm pretty happy about
  this

- tomorrow I'll clean up the code a bit, and commit it, and then start in on some of the userspace-to-kernelspace translations


2025-12-21:

- I'm having a really hard to getting started on the userspace-to-kernelspace translation code.  I've tried looking at the minix and
  linux source to understand how they're doing it, but I can't seem to discern much.  There's a few functions in linux which are
  relevant, like copy_to_user, copy_from_user, strncpy_from_user, and get_user (which seems to be used for small values like ints).
  And there are some higher level functions like getname() in fs/namei.c which copies a filename/path into a temporarily allocated
  cache area to be passed into other functions (it loads it into a `struct filename`).  But from what I can tell of the code, I
  can't see it doing per-page lookups.  It's almost as if it's relying on the user process's page tables, but I don't know how it
  could and still have access to kernel memory that the process shouldn't be able to access directly.
- I couldn't really find how minix is doing translations, and instead only found the vir2phys function that seems to just use the
  data segment address in the memory map to calculate the physical address (as if it's linearly mapped, which might be the case)
- For my implementation, I can only see a table lookup per-page, with a loop for when it hits the end of the current page, being the
  only "correct" option, but making matters worse is the fact that my current means of syscall restarting will erase all stack
  values, so I can't store the buffer on the stack unless I "redo" the copy when restarting.  This might only be an issue for
  file read/write and socket read/write, which are the primary syscalls that would block, and in those cases, I might be better off
  doing the user copying in the actual data read/write functions rather than using an intermediate buffer.  I'll need such a buffer
  for path syscalls though
- I just really don't like any of this
- oh and I don't think I implemented this is ruxpin.  At least I have some comments about needing to add it later, so I might be
  getting away with it by using physical address for the elf segments, and only the stack is copy on write, and the fact that most
  things fork and then exec() means I rarely run into trouble?

- I'm looking through the 68030 manual again for the MMU, and one thing that jumped out was a note about the MOVES instruction being
  an exception for some things (which bypasses the protection mechanisms).  In the linux code for the m68k that copies data from
  user space to kernel space, I did notice there were MOVES instructions inserted (in a weird way by exiting the quotes of the asm()
  inline).  I'm sure that's significant for how copy_from_user() works.  Those functions were entirely implemented in inline
  assembly, presumably for speed, but maybe also the need to specifically use MOVES?
- hmm... the MOVES instruction is called "Move Address Space", available on CPUs without an MMU, which moves data to or from a
  register into a different function code address space, using the DFC or SFC registers to select the address space.  So it doesn't
  seem like it's doing an MMU table lookup.  I don't think my hardware is using the function code address spaces, so not sure how
  useful this is to me

- Ah! ok, reading the manual, section 9.5.5.4 WRITE PROTECT, it clearly says that if a write access is requested for a page with the
  WP bit set, then it results in a bus error, which is what I'm seeing (rather than a privilege exception, as I would have
  expected).  I currently don't have the page fault code recognizing a copy-on-write violation
- I had tried to enable copy on write only on the stack segment when fork is called, but it fails with a page fault where a page is
  already set, therefore it terminates the process, so fixing this might be enough to get copy-on-write working for stack segments
- I'm trying to do something other than implementing the copy_from_user function
- but how the hell do I know that the bus error that caused a page fault was a write access?!?
- looking at the linux kernel's m68k implementation (unsurprisingly, it has a different impl for the 030, 040, and 060, among others
  because motorola in their infinite (ass-backwards) wisdom decided to make them all slightly different and incompatible)
  But those impls use the SSW (special status word) in the mass of otherwise meaningless stack info that a bus error produces.  I
  thought it wasn't documented because the bus error format is at the end of that section of the manual, but scrolling up, the
  special status word is defined above (8.2.1)
  Special Status Word
  ---------------------------------------------------------------------
  | FC | FB | RC | RB | × | × | × | DF | RM | RW | SIZE | X | FC2-FC0 |
  ---------------------------------------------------------------------
    FC -- Fault on stageC of the instruction pipe
    FB -- Fault on stageB of the instruction pipe
    RC -- Rerun flag for stageC of the instruction pipe*
    RB -- Rerun flag for stageB of the instruction pipe*
    DF -- Fault/rerun flag for datecycle*
    RM -- Read-modify-write on datacycle
    RW -- Read/write for datacycle-- 1= read,0= write
    SIZE -- Size code for datacycle
    FC2-FCO -- Addressspace for data cycle

- looking at ruxpin source for copy on write, and was reminded that I don't have to find the other uses of the page and convert
  them.  I only need to convert the page that faulted.  If I wanted to save one unnecessary copy, I can check the page refcount, but
  it's probably not worth it?
- well I think I got copy on write working.  It boots into the shell, although it doesn't have the normal _user_exit faults, nor
  does it timeout on ntpdate, so I think there's something wrong, some kind of bug, but it does boot and run commands, so it works
  better than it did before I remembered to actually implement the copying page fault
- that's really all I did was add an extra case to the memory_map_load_page_at() function, which converts the faulted page into a
  new copied version of the original page

- why doesn't it get to the exit syscall on pid 1 and others?
- echo seemed to work, but ls didn't seem to, or at least the second command didn't work correctly?  It seems to have the same copy
  on write page fault in pid 3 at two addresses in the same page... 2067AA and 2067C2, which resolved to page 206700, and was
  replaced twice... which makes me think it's not changing the status bits correctly???


2025-12-22:

- oh, looking closer at the output now, it doesn't double fault on the same page.  It faults in pid 1, and then it faults in pid 3,
  so different pages for different processes.  Pid 3 faults on another page later on.  But yeah, neither seem to fail with the
  _user_exit exception, which makes me think they didn't run correctly, maybe the faulted addresses weren't retried?o

- unrelated to above, I was skimming through the manual a bit, and came across the diagram, figure 9-31, which shows the cpu root
  pointer pointing to a small table with the function codes being used to index user data space, user program space, supervisor data
  space, and supervisor program space.  I wonder if that's being used by linux's m68k impl, which could be in supervisor mode, with
  the same root pointer as userspace uses, and have the supervisor entries point to the normal kernelspace addresses, but the user
  spaces are still accessible, and in user mode, only the user space tables would be accessible.  The supervisor mode would then
  only have to use the MOVES instruction, to read data from userspace, which will use the current user process's address space!  No
  switching necessary, so no TLB flush, and it still uses hardware to lookup data, making it faster.
- I'm not using the function code portion of the address at the moment.  Not sure how easy that would be.  I guess it would make
  sense to make it architecture-specific to copy_from_user, and point it to generic functions for now, and later implementing a
  custom assembly function, like linux does

- oh, that seemed to be easy to at least get a change in behaviour.  I was previously setting the SSW_FAULT_OR_RERUN bit, but
  changed it to also set the STAGE_B rerun and STAGE_C rerun bits, and it's now getting the _user_exit exception on pid 5 and pid 4,
  which I think is right
- I'm noticing two page faults in pid 3 which occur after pid 4 exits, and just before the prompt is displayed, which is for
  adjacent addresses (same page), and the page is swapped out, and then the same page swapped back in on the next fault.  It
  shouldn't be doing that at all.  It should only have at most one page fault there, so something's still broken
- oh yeah, it's also not stopping for the timeout, and there's a string of diamond characters (non-printable) that gets printed out
  just before a page fault
- could it need a flush after the page fault?  Does it need specific rerun bits set based on what happened?
- some of the SSW values are different for these page faults, now that it's executing more of them.  I think some might have the RM
  bit set, instead of just the RW bit (read-modify-write vs read-write), so maybe it's misunderstanding the cause sometimes?  But if
  it was, then it should be terminate the process early, which isn't happening

- only having the rerun stage C bit set (and the rerun or fault set), makes it behave the same as if no bits are set, that is to
  say, it never has the expected exceptions and doesn't really run most of the programs it should on boot
- having only the rerun stage B bit set (and the rerun or fault bit set), makes it fail on pid 1.  The error looks like it's
  attempting to write to a segment that is not writeable.  It's trying to write to its code segment, from the looks of it
- pid 3 also fails because of the same thing...

- from 8.2.1:
    Depending on the state of the pipeline, either RB and RC are both set, or RC alone is set. To correct the pipeline contents and
    continue execution of the suspended instruction, software must place the correct instruction stream data in the stage C and/or
    stage B images requested by the rerun bits and clear the rerun bits.
- So it sounds like it needs a very specific recovery, and it's up to software to sort out which...  Not terribly impressed
- section 8.2.2 talks about how to use software to complete the bus cycle, which sounds super complicated and involve, but then
  right after it is section 8.2.3, which is titled Completing the Bus Cycle with RTE, which *seems* to explain that just calling RTE
  without changing any bits will restart the cycle automatically?  So I need to change no bits? =|
- clearly no, that behaves the same as having only the DF bit set, which is to say, it doesn't seem to run the boot processes
  correctly, and has no _user_exit exceptions, just a couple page faults, no timeout, no "Running /etc/rc"
- honestly, the manual is hard to read, and not very clear on details.  It's very similar to the S32 manuals from NXP, so not
  surprising that they're still kinda shit, and the chips are still needlessly complicated.  I might as well dig into the linux code
  for all the good the manual will do me
- yikes, this thing is complicated!  Yeah, it's definitely not doing nothing.  It looks like it's checking a bunch of conditions in
  SSW and others, and in some cases executing special assembly instructions to get values from the MMU and such.  This is really
  non-trival...  But I think this is going to be a better guide than the manual.  In either case, it does seem to be doing more of
  what 8.2.2 says to do?  *shrug*
- it actually doesn't seem to modify the SSW value at all, but what it *does* do is update the TLB (called the ATC is the manual),
  and it does that using the PLOADR and PLOADW instructions.  I assume there's a different ATC entry for reading and for writing?
  One part of the bus_error_030 handler runs both instructions (probably a write to read only page), and the other part using just a
  PLOADR instruction (probably a missing page).  I'm definitely not doing that, which might explain the problem.
- I wonder if a PFLUSH would work just as well (but of course not as fast, potentially, as a PLOAD would be)
- no, I guess not, tried a single flush or flush all, and neither seemed to change the behaviour from skipping over everything (no
  SSW change)
- the linux impl is treating the data fault and the instruction fault conditions separately.  It's also reading in the MMUSR
  register, which seems to be accessed as the %psr register with PMOVE, but in either case, it seems to execute a PTEST instruction
  just before, and the manual seems to imply that the MMUSR register will only be set after a PTEST instruction, and not from a
  the fault that caused the exception (ie. you have to re-try the same fault address just to figure out what acutally caused the
  fault in the first place?  This seems so dumb on Motorola's part)
- doing the same ptestr/pmove that the linux handler does fetches the mmu_sr value, which when SSW is not changed (so only 3 page
  faults occur), each page fault has SSW 0x709 and MMUSR 0xA04.  That corresponds to "Write Protected" and "Modified" bits being
  set, and the level the fault occurs at being 4, which is the correct leaf table, so each should be a copy-on-write fault, and they
  all seem to convert a copy on write page... the only thing is that the flush doesn't do anything and I'm not doing the PLOAD
  instead...
- adding the pload instructions doesn't change anything either, so there's probably something I'm still doing incorrectly
- I'm not looking too closely, I'm just trying quick fixes atm, but it's late and I should come back to this tomorrow

- the DF bit *is* set in all 3 of these faults, so it seems like it should be running the code I copied


2025-12-23:

- I've looked through the linux page fault handler a lot, and while it does check a bunch of bits to determine what to do, most of
  those things end in a fault that terminates the process, or a kernel panic.  The one that handles the page fault is the only
  condition that occurs in my program, and I'm doing the exact same things, namely fetching MMUSR, and the PLOAD instructions.  All
  the other cases that linux handles don't occur in my example, since the only page faults are the copy-on-write exceptions
- In all 3 cases, SSW is 0x709, and MMUSR is 0xA04, which means DF is set, when the RTE instruction executes it will retry the
  fault, no other fault bits are set, and in MMUSR it's only a write protect exception and the modified bit, and the number of
  levels searched.  Linux in that case will only run the page fault handler, and then the PLOAD instructions that I copied
- so, the first page fault in pid 3, I dumped the mmu table and it shows the same   status bit as before 0x1D instead of 0x19 in the
  table.  I'll trying printing it after the change, but maybe my status changing code is wrong?  I'd still expect repeated faults on
  the same address though if that were the case
- the first time it occurs, the status is changed to 0x19, but I guess the process forks which sets it back to 1D? Then the second
  page fault is for another address, 0x209300, which has the status correctly updated to 0x19
- I keep getting confused by these page faults, thinking they're the same page, but they aren't.  The addresses are just inverted
- Something to note though, is that pid 3 has two heaps and two stacks, which I assume is that hack I put in place when copying the
  stack so that it can access both stacks.  Maybe that's causing problems?

- I removed the copy_stack by uncommenting the #if block around it, and I ran it again, and I'm pretty sure I'm seeing two page
  faults for the same address in the same pid, but I'm pretty sure it's because there's a fork.  There are 4 page faults this time,
  pid 1 for 0x2067AA, then pid 3 for 0x2067AA, then again for pid 3 for 0x2067AA, and then pid 4 for 0x2067AA
- 0x206800 is the top of the stack in all the forks of these processes, so that makes sense, and when a process is forked, I think
  it returns to the parent process before switching to the child.
- the first page fault for pid 3 occurs when it first runs, and writes to its stack, and then it forks itself to make pid 4, which
  enables write protect for both the child and parent processes.  The first to page fault on that is pid 3 again, which copies the
  page, and then pid 4 runs and page faults because the first time it writes to its stack.  Pid 4's original page is 0x207500, which
  is also the original page for the second pid 3 page fault, and it's the new page that the first pid 3 page fault allocated to
  replace the original 0x206700 page
- so in short, this looks like it's doing exactly what's expected, and it runs pid 4, but it does not seem to run pid 5, since
  there's no page faults for it, implying it's somewhere in pid 4 that the problem occurs?
- running `ls` produces no output, but many page faults, and a null pointer exception...

- Ah!  `sh` has a RELRO section, for relative read only data if I'm not mistaken, and I haven't change that section yet.  Could that
  be messing up something, or not being loaded?  I only have copy on write for the stack, and the segments are still preloaded, so I
  don't see how this could have any effect... hmm...

- you know what might be going on... so I tried running `ls` and no output, just null exception, and then I ran `echo hey` and it
  printed a bunch of garbage characters.  Since it's using copy on write, the stack addresses are all in the 206700 range, but only
  pid 1 actually has a stack that points to the same physical address as the stack address.  Every other process has a physical page
  mapped there with an address that doesn't match, and the api syscalls are not translating still, so it's using the wrong addresses
  and getting the wrong data as arguments
- so it doesn't matter that it's only the stack, the translations are still required to make any of this work =(
- I don't think ruxpin has the same problem, despite having a copy on write stack... not sure why it worked there

- yup, adding the copy_stack and removing the COPY_ON_WRITE flag from memory_map_copy_segment's mmu_table_copy() call makes it work
  as expected
- interesting to note though, that with just the copy on write removed, but also not having copy_stack() called made all the same
  null exceptions and garbage characters printed exceptions occur, but without the page faults... So I think the copy on write stuff
  is working fine, but there are translation problems


2025-12-26:

- I've been avoiding the translation stuff, but also chipping away around it, looking at linux's source, and looking at the 68030
  manual.  The MOVES instruction seems to do what I need, even though the manual isn't explicit about it using the MMU or CRP
  register to do the access.  I've already made a simple implementation of copy_to/from_user with it, and debugged it enough to know
  it will actually copy data.  I have it in the exec() syscall, and it's certainly copying the data
- the big thing that I haven't yet figured out, is how to avoid copying or allocating a buffer entirely when CONFIG_MMU is not
  enabled
- I was looking very seriously at copying linux's iovec/iov_iter pattern to hide the details of user memory and copying from deeper
  within the kernel than just the first level of syscalls, but any syscall that might block is still susceptable to erasing the
  stack before the syscall restart, so that won't work unless I change how that works (and the alternative way is to use the stack
  (possible the user stack when CONFIG_M68K_USER_MODE is not enabled) throughout the kernel).  At the moment, I'm going to try not
  using iovec to avoid that question
- Oh, and one thing I came across in the manual was that the SFC/DFC registers were significant to the PFLUSH instruction, so since
  that's not set, that might explain some things, and also need to be fixed

- I quickly tried initializing the kernel stack with 0xAA and then ran it, and dumped the stack area to see how much of the kernel
  stack was being used.  I put it just before the begin multitasking, so it should have be post-initialization.  It still ended up
  using 1008 bytes of the stack, which is quite a bit to be putting on the user stack, if I used the user stack when user mode is
  not enabled
- the stack usage is probably the biggest obstacle to using the stack for syscalls (and thus being able to access buffers on the
  stack when restarting a syscall)
- but that said, it doesn't seem like anything except the READ and WRITE syscalls will suspend, with the exception of select() which
  is kinda custom implemented as well

- actually, the syscall restart isn't an issue because it will always reenter at the very start of the syscall, thus reallocating
  a stack buffer and recopying data. So I think it's safe to have a stack allocated value, and that opens up the possibility of
  using an iovec like abstraction. That might allow both userband kernel users of the vfs interface


2025-12-28:

- looking through the code I'm changing to add iovec into the read/write functions, I came across the driver interface, and debated
  again whether to actually get rid of it, and make the next level in drivers be the subsystems (block and tty for the moment, and
  eventually moving kernel/net/if into kernel/drivers/net/ or something).  The bufcache code would then move to block/ and always
  take an offset, and tty drivers (possibly renamed char/ or tty being put under char/)
- it would fix the awkwardness of the offset being passed in
- but then again, why didn't I want to just do an lseek + read w/o offset?  I feel like that's how it was before, but I changed it
  at some point for some reason


2025-12-30:

- I haven't written here much in the last few days, but I've been slowly refactoring the code to use the new iovec/iovec_iter stuff,
  and humming and hawing about how and what I want to change
- I ended up side-stepping the exec arguments/environment variables by just copying them to a temporary buffer, which works for now.
- I activated the copy-on-write for the stacks (I think) and got page faults without it breaking like it did before, so I *think*
  the user space copying is working, but I'll test it again once I get more of the translations in place
- I ended up converting a lot of the networking code to use iovec, which went all the way into the packet and ringbuffer code.  The
  packet one was easy enough, because I could just add another function to copy an iovec into the buffer, and left the existing one
  to copy a kernel buffer directly
- for the tcp code, it uses a ringbuffer to store the data received, and I tried adding new functions to the ringbuffer impl that
  will get/put to/from an iovec.  It's quite a different implementation, since rather than doing it byte-by-byte, I'm using the
  memcpy functions and trying to calculate the maximum chunk of data I can read.  It should only ever require 2 chunks if the buffer
  wraps around, so I coded it like that, without a loop.  It might end up being bigger code-wise than using a loop.  Since they're
  static inline functions though, it should only use them in the tcp code for now
- that said, I suppose I could use some of them in the tty code if I wanted to

- I tried to get the slip line setup again, and haven't had any luck getting data through.  I tried stashing my changes and the
  previous commit doesn't work either, so it's not (only) related to my refactoring

- I'm finding a lot of bugs.  When I tried to run httpd, it gives an error about double freeing the fileptr, which I've traced to
  being the executable itself, but it only seems to do this for httpd, or anything that forks
- after running that, trying to run `ls` produces no actual directory listings, but when running `ls` right after boot, while it
  prints the permissions and stuff, it's missing the filenames, so something didn't get copied correctly somewhere

- the readdir() bug was just because I allocated a pointer instead of a struct for the intermediate buffer
- also signals don't work, but I expected that
- interestingly enough, daemontest seems to work, at least they both attempt to exit, but there is also an error that gets split up,
  about it failing to open something

- the signals issue was were Ctrl-C did nothing, but that's controlled by the TTY, and it turns out the ioctl function that I just
  converted to use iovec was actually what was broken.  Still is, I thought it might have been because I switched int to pid_t for
  the hackish "set pgid" function, but I think it might be the iovec being size 0 that's causing it to not work
- yup, it was the length that was causing the problem, which is kind of unfortunate, since we don't have a size
- I found another bug in the process where running `test` (which I had forgotten about) starts an interactive program with a prompt,
  and ctrl-c will kill it and then also kill the shell (pid 3)

- oh I see, httpd uses select(), which I don't think I've ported over.  It should still *work* since I don't have any page fault
  conditions enabled.  It could still be something else


2025-12-31:

- playing around with the networking a bit more, there are some packets that get through, but it seems like pings from the host
  aren't being forwarded correctly.  Maybe something's changed with the networking on debian since I last tried this, which has made
  my commands no longer work as expected.  I'm not convinced the problem is with gloworm, and not with my desktop's setup

- ok, I committed the big chunk of changes to add address translations and iovec.  There's definitely a few things I've forgotten,
  but I needed to get it added to debug more effectively
- I've disabled the stack copying in MMU mode, and enabled copy-on-write for the stack segment only.  It's running and will execute
  ntpdate with the right timeout, but what's failing is the execbuiltin() syscall.  The echo in /etc/rc won't run, and `ls` and `ps`
  won't run because they're using the shell's builtin versions, but it runs when I try running `/bin/ls` and `hello` works fine.  So
  it's definitely something with that syscall
- the address passed to execbuiltin() seems correct, and in the forked process, that segment is there, so it should be a correct
  address to use (no translation needed), but it seems to fail with an bus error exception on address 0, so a null pointer
  somewhere?
- a thought that just occurred to me, during an exec or fork, if there's a page fault caused by accessing the new process's memory,
  it might not resolve correctly because the current_proc won't be the new proc yet
- some more print statements shows it's memory_map_reset_sbrk() that's failing.  Maybe one of the addresses in the map aren't copied
  correctly, or not recreated correctly

- the sbrk and heap_start values are 0, as are the argv and envp values, which are only copied over when duplicating the stack,
  which adjusts all those values
- all those values are correct when copy_stack() runs (when CONFIG_MMU is disabled)
- fixing those values (just manually copying them over when CONFIG_MMU is enabled) fixed the problem with execbuiltin() not running.
  It will now run `ls` and `ps` correctly too (the ones built into the shell)

- I've removed the copy_stack when CONFIG_MMU is enabled, and set the copy-on-write flag when copying segments, and that all seems
  to be working now
- when I tried to change the other things with the code segments or switching to unallocated segments, it won't even start
  multitasking.  Not sure what's wrong, but it's likely something significant, perhaps something I forgot to switch over.  It can't
  be looping page faults because I've got a print statement when a page fault occurs, and that's not printing

- I decide to try tackling the _user_exit problem next
- it looked like I only needed to change the crt0 file with _start(), so I changed that and tried updating the compact flash card
  image, but that wouldn't boot because the flat binary loading is using the hardcoded 0x40000 starting address, but why the held
  did it produce flat binaries?
- I couldn't for the life of me find a time in the history when it was producing elf outputs instead.  I guess I had manually
  commented out the rule in Makefile.build which does objcopy (right after strip, which looks like objcopy overwrites).  Commenting
  out the objcopy made it work, but it still didn't boot
- it was having an exception on pid 1 and then pid 3, which seemed to be because a page fault occur right on the boundary between
  code and data.  Sure enough, I was using >= =< for comparing segments, and changing it to >= < fixed that problem, so it was
  booting now
- aaaaand it's still got the _user_exit exeception, so what gives?  It shouldn't be doing that.  Is it not using the right entry
  point?  The main() function appears before _start() in the file, but if it uses the elf entry point, it should be fine

- I thought for a second that the httpd binary might have been failing because it was a flat binary, but that's not the case.  It
  was however last updated in January, so it might have been before some breaking changes, which might explain why it wasn't working
- the latest build of it is also not working, but in a completely new way.  It's infinite excepting on a page fault for address
  11FFD4 which must be a kernel stack address.  No idea why that address is being used.  It happens right after the exec though
  It should also be killing the process, but it's not for some reason

- back to the exit problem, and aha!  it is actually working if I run the non-builtin executables (ie. /bin/ls).  The builtins don't
  have the problem fixed because they only call the main function, which is renamed using the C preprocessor

- found some code in the insert_heap_and_stack function that allocates the stack even in MMU mode.  Commenting it out, it still
  boots, but when I try to run any program, it has an infinite exception.  The problem seems to be in `sh` between the fork and the
  exec().  It forks, and then has 3 page faults that correspond to the data segment, and one to the stack segment, and then it has a
  null pointer exception

- it happens in the shell at address 0x208154, which works out to 0x1b54 into the code segment, which is the rts instruction in the
  fork() library function.  The first page fault occurs just after the fork() syscall ends.  That address is in the code read only
  segment, which is odd, because I'm loading all the code in the shell when it launches, so that address shouldn't fault...

- what if there's some code somewhere in the kernel that assumes the segment start and end are always "windowed", so writing to it
  causes corruption in other processes?

- playing around a bit, and get it back to what should be working, and it has the ntpdate timeout, and the shell comes up fine, but
  the exceptions that occur are actually weird addresses.  A not entirely valid code reference to _user_sigreturn, or possibly
  _user_exit which comes before it, for pid 6, ntpdate (which should have been updated to not have the exit problem), but then the
  second one, in pid 4, seems to be for E0C6 0020, but it's probably meant to be 0020 E0C6, which is an address at the top of the
  stack of that process
- so it's actually broken even with the minimal CoW enabled.  Adding more is just making it more frequent I think, but the problem
  lies elsewhere
- actually, out of 3 exceptions, the first is a builtin echo command and it's the exit, the second is pid 6 which is ntpdate, which
  shouldn't have the exit problem, but it's timing out, which might dispatch a sig alarm? in which case it truely is _user_sigaction
  that's executing and causing the termination there, so that might actually be ok, and then the 3rd is the weird one, but seems to
  be a misalignment issue?  or else corruption
- the fault is in pid 4 which is `sh /etc/rc`, at address 0x209ED0, which is 0x18d0 into the shell program
- that seems to be an RTS instruction in `sh_task` which is the normal main() for sh()... so the stack is corrupted?  This should be
  the version of the shell on disk


2026-01-01:

- I'm still stumped by this.  I figure it's either that MOVES doesn't work the way I expect, or there's something else copying data
  using the virtual address as if it were a physical address somewhere in the kernel, but I could be off base.  The third exception
  is definitely broken though
- I was looking through the shell code and it hooks two signals with custom handlers, SIGINT and SIGALRM.  On a hunch, I pressed
  Ctrl-C at the shell and it had an exception and was killed, so there's definitely a bug there.  Not sure if it's related to the
  erroneous termination above, I wouldn't think it would be, but I'll look into it for now
- Oddly enough, it's actually an address error instead of a bus error, but the fault address is 0x20CFCC which isn't an odd number,
  so it's not an alignment issue.  It's also a valid stack address.  Code offset is 0xa24, which is RTS in `handle_signal()` so that
  checks out.  The address is not the _user_sigreturn error though
- the code before the RTS is a puts() call that prints "^C" but I don't see that printed in the output here, as if it didn't
  actually run that syscall (or had the wrong address, and printed nothing?)
- Or actually, it could have checked the stack value to see if it was SIGINT (ie. the value on the stack was wrong) and it skipped
  the puts()
- well... I added a print statement to the signal handler runner, and reloaded it, and pressing Ctrl-C did nothing, no messages, no
  prompt change, no nothing.  The last time I tried this, I had run ls and /bin/ls.  I tried running /bin/ls and then pressed Ctrl-C
  again and it printed the message in the kernel signal dispatcher (but not the puts() in the custom handler) and then had a bus
  error on address 0, so it's not working normally on first boot, but not causing an exception either, and only after running
  another program does it cause the exception.  This really feels like stack corruption somewhere
- the exception occurs at the exact same code location (the RTS in the custom handler), even though the fault address is different
  this time
- wait a minute, maybe there's something in the signal context code that hasn't been updated given it needs to write to the user
  stack to put the argument on there...
- yeah, it does push a value onto the user stack, without using the MOVES code, which would definitely explain why the custom
  handler is not getting the correct signal number
- I saw the pushing of the _user_exit value onto the user stack here and tested what would happen if it was removed, and the shell
  crashes on boot, so I'll have to fix that later
- well... ok, so I changed the code that pushes _user_sigreturn onto the user stack (very hackish code that assumes MMU enabled atm)
  and now it's crashing on the _user_sigreturn address when it executes that RTS instruction, so progress?  That's very likely the
  cause of the problem here, but I don't see any existing code that pushes the signal number onto the user stack.  The rest of the
  code in the add_signal_context() function is dealing with the kernel stack.  I wonder if the signal number was working previously
  when USER_MODE is enabled

- From https://man7.org/linux/man-pages/man2/sigreturn.2.html
       Once upon a time, UNIX systems placed the signal trampoline code
       onto the user stack.  Nowadays, pages of the user stack are
       protected so as to disallow code execution.  Thus, on contemporary
       Linux systems, depending on the architecture, the signal
       trampoline code lives either in the vdso(7) or in the C library.
       In the latter case, the C library's sigaction(2) wrapper function
       informs the kernel of the location of the trampoline code by
       placing its address in the sa_restorer field of the sigaction
       structure, and sets the SA_RESTORER flag in the sa_flags field.

       The saved process context information is placed in a ucontext_t
       structure (see <sys/ucontext.h>).  That structure is visible
       within the signal handler as the third argument of a handler
       established via sigaction(2) with the SA_SIGINFO flag.

       On some other UNIX systems, the operation of the signal trampoline
       differs a little.  In particular, on some systems, upon
       transitioning back to user mode, the kernel passes control to the
       trampoline (rather than the signal handler), and the trampoline
       code calls the signal handler (and then calls sigreturn() once the
       handler returns).

- So it looks like linux actually does what I was trying to avoid, which is mapping some special code into each process which
  contains the sigreturn syscall, but it sounds like other systems might add an extra argument when registering the signal which
  gives the address of the sigreturn stub which I assume gets added to the stack by the kernel
- the latter idea seems like a workable solution maybe...
- So it looks like according to the linux manpages, the sigaction struct has some extra fields, one for an alternate signal handler
  function which takes more arguments than just the signum, and then at the bottom a field called sa_restorer field, which it says
  to not touch, and that posix doesn't define that field.  So it looks like it is indeed passing in another argument that is the
  trampoline/_user_sigreturn equivalent

- alright, so added that, recompiled the commands and uploaded them to the compact flash card, so that the sigreturn syscall is
  inside each application, and the tried again.  The third exception on boot is still there, so this doesn't change that, but when I
  try Ctrl-C after running /bin/ls, it didn't have an exception!  It still exited the process for some reason, and it didn't print
  ^C so signum is still not being put on the stack.  This must have been broken for quite a while
- So it seemed to print "Error: -1" which was interrupted by page fault print statements.  There's some code in read_input in the
  shell which prints that out if it gets an error that isn't EINTR.  I think it probably should get EINTR but it's getting -1
  instead.  Either way, I think it's passing control back to the shell after the signal is run
- I tried adding the signum to the user stack and it did print ^C!  It didn't print "Error: -1" either, but it did still exit the
  shell.  `Process 3 exited with 0` (instead of exited with -1 like the previous run

- Oh btw, I'm sure this will get lost in the noise of these logs, but I saw an old tab which confirmed that linux is using a memory
  map where the kernel is mapped into every process's address space in the upper part of memory, likely protected with a
  supervisor-like bit which prevents the user application from actually accessing that memory, but it's active when the kernel code
  is running, so it can directly access the user process's memory.  It was mentioned on a page talking about
  copy_from_user/copy_to_user.  I'm not sure if I maybe did the same thing with ruxpin, which might be why I could get away without
  user address translations.  I can possibly do the same thing on the m68k arch, but only if I use the long descriptor type, which
  has the supervisor bit to protect kernel pages from being accessed by the user
- I don't actually see anything like that in ruxpin on a quick look, so not sure, maybe not.  I seem to recall something about the
  aarch64 MMU treating upper memory as special or something though

- looking in the git history, it seems like it never put the signum on the user stack.  Before I added USER_MODE, and it only had
  one stack that had the sigcontext and such on it, it might have been able to get the signum from the sigcontext if it just so
  happened to be put in the right location, but that's really hacky if that's how it used to work
- that said, I think I might be getting away with not explicitly clearing it after the signal because the saved context has USP
  which will overwrite the old version.  The add_signal_context function doesn't write the usp back because it passes it to the
  function that adds the process context on top of it.  I should probably confirm that though
- hmm.... looking through the signal and scheduler code to see how it actually transfers control to the process running the signal,
  and I'm not sure it's actually doing the right thing here... it might be at least missing the arch_extended_context() switch,
  which would leave the MMU table incorrect.  I'm not sure it's setting current_proc either, which would be quite bad.  Not sure if
  I can confirm this somehow...
- well in the two cases that's occurring, the proc being signalled and the current_proc when cleaning up the signal afterwards is the
  same, but in both cases, the process being signalled might have been the current process anyways.  It was the timeout in ntpdate,
  and the Ctrl-C after a command.  Can I use kill to test another proc?  Maybe daemontest since it seems to leave a zombie process
- I couldn't get that to leave the zombie process.  It exited with an error of some kind.  So I tried killing pid 2, which caused a
  series of exceptions in the kill process.  I didn't see it try to send a signal though, so still not able to confirm that it's
  properly switching the current process
- the reason why this is particularly pertinent atm is that if we're switching to the process that will receive the signal anyways,
  we can possibly switch early and use the CRP/MMU/MOVES/memcpy_to/from_user to put values on the user stack, but if we aren't, then
  we might need to use the slower way of doing it
- it looks like reschedule_proc_to_now() is not actually rescheduling but putting the proc just after the current proc, so that it
  will run next, but it doesn't seem to set the "needs_reschedule" flag, so it might not actually switch to the task right away

- so... I'm kind of getting sidetracked.  I tried removing the pushes of _user_exit onto the user stack in the process context
  functions (to see if they're related to the exceptions I'm getting) but when I run it, despite the shell having the proper
  _user_exit syscall inside of them, the two shell processes that are launched on boot (pid 3 and 4) both terminate shortly after
  they start because they access the word just above the top of the stack...
- as I wrote that, I think I answered my own question about why this is happening.  The C code expects a return address, and when it
  looks for arguments on the stack, it's likely assuming there's a return address and going around them.  This would make sense
  since the code that causes the fault is at address 0x189e, which is just before calling `open`, where the argument passed to
  `open()` is the second argument in argv, ie. the script that it should run
- this definitely makes sense for pid 4, which is `sh /etc/rc`, but seems a bit odd in the second case?  Oh actually it's a
  different code address?  No it's the same one.  It's probably because argc is wrong because of the missing return address
- either way, this might be a red herring.  There's still the bug where it's likely putting the return address at the wrong address
  because it assumes the stack pointer is a physical address, but there needs to be something on the stack there
- I tried just adjusting the stack pointer without putting the actual value on the stack, and it's not actually changing any
  behaviours, so those contexts pushing a value to the user stack isn't alone causing the stack corruption

- what if it's the address being passed into these?  Maybe it's not the right address which can cause problems?
- Ah ok, so yes, the exec_ functions are what do most of the stack building.  They put the arguments onto the user stack before
  passing it off to the arch_context functions which put the return address basically, and everything else goes onto the kernel
  stack.  The exec_ functions normally take the stack pointer, map->stack_end, which is technically supposed to be a user virtual
  address, and then the exec_ functions treat it as if it's normal contiguous memory.  So that definitely needs to be fixed.  Not
  sure if that explains everything, or is just one of many problems, but I'll have to test it out properly after dinner

- well I very hackishly made the binary loader fetch the page and load the stack from the last stack page, but that didn't make any
  of the behaviour different for any of the conditions going on, so I'm not sure this alone explains what's causing these various
  issues.  I'm really disappointed.  I don't have much time in my break left, and while I got things done, I haven't actually gotten
  this MMU feature working, and I'm still stuck banging my head against everything
- I also don't have a proper clean solution for these user stack loading issues.  Only hackish solutions that use direct mmu calls
  to get the underlying page to manipulate.  I'm not even sure if MMU translation is working, or if the MOVES functions are copying
  data correctly, because it seems any attempt at using virtual addresses that differ from the physical ones results in a bunch of
  exceptions

- I think the signal problem might be minor or unrelated to the other issues.  The third exception after boot (well maybe it's the
  second one now, the one with the misaligned address), is still an issue, and then enabling more translation things, I haven't
  debugged much because it's such a catastrophic failure
- I should maybe see about consoladating the argument code such that I can more easily copy and pack it, ideally using an iovec so
  that it's easier to copy across the user/kernel boundary


2026-01-03:

- I'm kind of going in circles.  I had so many bugs in the new string_array code, and I've introduced a bunch more in adding it to
  the exec() code.  I think I need to step back and undo some of those changes to get back to a working system, and then try again

- So I committed the signal code, and stashed the exec arguments/string array code, and I thought while I had that done, and the
  kernel was booting again, I'd try removing the execbuiltin syscall, which shouldn't be needed because the shell already forks
  itself to run a command, so it can just call main() and exit.  That seems to be working fine
- in the process I found that init also has a call when it's compiled into the kernel, so I thought I'd test that, and I'm falling
  down that rabbit hole now.  I had to compile the kernel without net support in order to get it small enough to load.  I tried
  increasing the max size, but that's going to be harder to do and require a firmware update, so I'll hold off on that
- the kernel is not booting with init in the kernel, because it has a page fault.  Initially it was trying to load a page, which is
  so wrong, but I added a check to the page fault code to check that it's not trying to write to a read-only segment first, and
  raise EPERM, which is also wrong, it should be a segfault, but I'll fix that later
- looking into the code to find why the init process has a page fault on address 0x10E208 right at bootup, it seems to be writing to
  errno during the open() syscall.  I can easily just make the kernel segment that's inserted be read/write, but maybe I make two
  segments instead?
- it wouldn't be hard, but I would have to add another tag to the linker script, and this is a mode that is intended for testing, so
  I think I might just make it read/write/execute
- having second thoughts, maybe I'll just add it
- changed my mind again.  I tried adding it but was getting EINVAL because the segments were aligned, and the linker script can't
  seem to easily access the PAGE_SIZE, so I have a hardcoded 4k alignment at the end that I'd prefer not to copy in order to make
  the code and data segments align, which would make the kernel size problem more of an issue, so I'll just make one read/write/exec
  segment
- well, it doesn't work when CONFIG_MMU is enabled, but it does work without it, so I'm going to go with that.  There's definitely
  some bugs lurking in the MMU code still, and I could try to find it though this setup, but... I kind of don't want to if it could
  be a red herring...

- ok what in the actual fuck!  I restored the stashed changes that had the string array code, and compiled it (while it still had a
  printk that showed the syscall number) and it worked and I could run ls and such, so I removed the syscall print and compiled it
  and ran it again, and now it's got an infinite exception in pid 6 at some non-sensical address!  Why would the print statement
  change that!?!
- this fault address doesn't look dissimilar to the the fault address when compiling init into the kernel, although it's certainly
  not the same address.  I wonder if it's related to issues with the user stack construction still, which is partially changed by the
  string_array stuff, or if it's an unrelated bug somewhere (that is also affecting the case where init is in the kernel)
- the error I'm getting now is the same I was getting last night before I separated the commits.  It's an error in strlen() where
  the address passed in is totally wrong.  It's likely a bug in the string_array code since it uses strlen()

- something's a little funny here.  I'm not sure if the execbuiltin removal has affected something inadvertently, since it seems
  like it's launching something unexpected after the ntpdate timeout occurs.  It's behaving differently this time compared to last,
  where it's got an error in pid 3 now instead of in pid 6 (ntpdate)
- after the timeout, it prints this:
      starting exec()
      exec(): /bin/sh
      arg 0: p␁NANr
      arg 1:
      arg 2:
      arg 3:
      arg 4: /bin/sh
      arg 5: /etc/rc
      arg 6: init
      page fault @ 2F62696E in pid 3
  which is definitely a bug in the arguments code, but I'm not sure where exactly, since it shouldn't be doing this...  maybe it's
  even related to the signum I'm putting on the stack now??  corrupting things after the signal handler runs?  I didn't add that
  change to the sigreturn commit I made earlier
- yup! that was exactly it.  So adding the signum is causing the problem.  I think it might be because it's not removed from the
  stack.  I wasn't sure where the removal should be, but I think it might be the sigcontext restore code that then has the problem,
  so in order to put the signum on the stack, it has to be removed before the sigcontext?
- no that's not right, because the sigcontext is on the kernel stack, not the user stack...
- I tried adding some code to remove it by fetching the usp, adding the size of signum, and then setting it, but that didn't seem to
  work.  It's still getting this error in pid 6 or something
- oh, hmm... I put that change after the context was dropped, so maybe I need to put it before?
- that doesn't make much sense though because dropping the old context should change the value of usp back to the old value before
  the signal was dispatched, so this should break in the same way... but it doesn't seem to?
- man this is weird.  It does seem to work now with the removal of signum before the context is dropped

- I went down to lunch, and tried it again when I got back, and now it's got a fault at address A18220C8 in pid 6 during boot.  This
  is with the change to add and remove the signum argument.  So it may be unrelated to the signal stuff

- I had turned the computer on, and loaded the kernel from before lunch, and it went into the infinite exception in pid 6 error.  I
  recompiled the kernel, and loaded it (without turning the computer off), and it booted fine, and then I turned the computer off
  and had a quick shower and turn it back on, loaded the exact same kernel, and it had the infinite exception on pid 6 error.  So it
  seems to be unrelated to the code changes I'm making.  It's some kind of random bug
- it does seem to occur more reliably after turning the computer off and back on, I think
- this is still the error where strlen is given the wrong address, occurring at 10D382
- ok looking at the stack output, there's a return address to 10D05C, which is inside vsnprintf, which calls strlen with the broken
  address (A182 A0C8), but the rest of the trace is stack addresses, probably arguments or local variables inside vsnprintf
- dumping more of the stack from the monitor, I'm seeing 106BA6, which is console_putchar_direct (the start of the address) so I
  don't think that's a return address.  Kind of odd to see that function on the stack
- I'm also seeing 10CA30 (vprintk) a couple times, 10D754 (appears to be in read only data), 10CA7E (printk) so that's the call that
  starts this all.  Then there's a big buffer on the stack.
- the string on the stack is then: `arg 0: pdate` so it's trying to print the arguments, but it's got some messed up string because
  that should be `ntpdate` not `pdate`
- some return address to 10CA92 a couple of times, which is also printk
- the next address I can find is 100A4A which is right at the end of the do_exec() function... so I think this might be related to
  the code that prints out the arguments (which makes sense given the above calls)
- Oh shit, it's printing out the user arguments, not the buffered list, so that might explain the problem...
- modified code didn't print out the right thing but didn't crash either
- ok now it seems to be working somewhat reliably

- now that I have that bug fixed, I tried removing the code that removes the signum from the user stack, and it doesn't seem to have
  any affect.  I can have signum added to the stack and then not remove it after, and it's not causing any problems, which is how I
  thought it should work.  The errors were because of that argument printing code

- so I went back to the function that builds the user stack during an exec() call, where I have the new string_array struct passed
  in, and which then uses an iovec to copy it into user memory.  If I make it use a user buf with iovec (and don't add the
  mmu_table_switch() call), it hangs during boot.  If I add the mmu_table_switch() call (in binaries.c before it calls the exec
  functions) it seems to work fine.  It *should* always be the case that when exec() is called, it will be operating on the current
  process, and there's actually already a hack in the exec functions to call the extended switch since it's changed the memory map,
  so I guess it's not that bad to have it like this.  Doing this means it should copy the stack data into the right memory location,
  without needing to do weird things with pages
- looking at the code that calls the exec function, it was actually passing in the actual page as the stack, instead of using the
  user stack address (map->stack_end), which maybe is not actually correct...
- I changed that to use map->stack_end again, and it also seems to work fine, so that's good
- there's still a few locations that do user stack manipulation, namely the context.c code

- hmm... not ideal, I tried rewriting the exec functions a bit so that rather than skipping the extended switch, it produces an
  error, and it's failing of course, because the init.c functions call exec without being the current process

- bunch of debugging and refactoring, trying to get everything into the iovec including a new push_back function for easily creating
  a stack
- I'm stumped on a problem where making the user exec function use a user buf instead of a kernel buf makes it except, but on pid
  2440 which makes no sense.  I've seen this error before but can't remember the cause.  The pid was the same even number even, 2440.
- I thought I'd found the error when the stack passed in by alloc_kernel_process had a page added to the start of the stack address,
  but that's not fixing it.  It's not even calling the user exec function yet!!!
- scratch that, didn't look closely.  The kernel task is allocating a single page for the stack
- also it is actually calling the user function, which I can tell from a unique print statement
- it doesn't work for the initial process because the current_proc variable isn't set yet!
- I moved the extended switch outside the conditional and now it's working.  So CRP wasn't set which was causing the MMU to mis-map

- tried anonymous memory for the stack again, and I'm getting a error -22 when creating init task *frustration*
- I'll come back to it tomorrow.  I should be close but I'm not


2026-01-04:

- ok, so that bug where it fails to init, which I did get in the past but didn't dig enough into, was caused because I had a
  hardcoded MMU_FLAG_WINDOW in the flags, which combined with the SEG_ANONYMOUS flag made the type be 6 which is invalid, so it
  would return an EINVAL error from the mmu mapper
- and trying to fix it is resulting in the same error, it seems because there's no default type when a mapped segment doesn't
  specify... so it's not so easy to solve I guess, which explains why I gave up before and looked for other causes
- I had to add a SEG_FIXED to the init.c maps
- well, fixed that and now it's booting but getting into an exception loop in pid 6 with a weird addresss
- it tries to load a page, which I think is where it goes wrong?
- the exception itself is in memcpy_from_user, called by do_sendto, in ntpdate.  Is it trying to load a page that's already loaded?

- omg, the last issue there was that since it was ntpdate using udp, it used the sendto and recv_from syscalls which take an extra
  array passed in for the address buffer, and while I was copying the contents of the address buffers into/out of kernel space, I
  wasn't actually using `get_user_uintptr()` to fetch the address of the address buffer, or the length of the address buffer, so it
  was returning garbage which caused an exception!  It's actually running with anonymous (not-preallocated/fixed) stack pages!
- I'm going to have to be careful going through everything to make sure I'm actually copying everything properly user<->kernel
- also, I should add a check like linux does to make sure a user address is valid before dereferencing it, and also maybe copy
  functions that won't cause a fault, but can still invoke the page loader code if a page is missing?  Not entirely sure what linux
  does there

- alright, so it's working for the stack pages, but it's still crashing when I try to make it load the executable file's pages on
  demand.  It sort of works a bit, and it seems to be loading the correct pages, but for some reason, the SSW word in the page fault
  exception says it's a write operation, but it should be executing a branch to main() in the init process.  It loads the first page
  that contains _start and writes to a location in the data segment, and then faults on address 203900 which is the first page of
  the executable, and the data seems to match the code, the instruction looks good, it should be a branch, but for some reason,  the
  write flag is set, so it fails and is terminated
- I should note, I have an exception in the binaries.c file which will preallocate if and only if there's a RELRO segment in the
  binary
- I commented out the check for the write flag, and it got much further, but both the shell processes that are launched, pid 3 and
  4, crash with an access to address 0, so something's not quite right

- just to reiterate, when it says pid 2440 has an error, it's likely that current_proc is NULL and it's getting the pid from an
  invalid memory location
- I just got that error again after removing the hackish code in binaries that allocates a page on the stack for arguments before
  calling exec_initialize_...  I need to replace that code somehow


2026-01-05:

- oh my fucking god!  So last night, I tried enabling the page loader code, and it crashes early on because something that should be
  a read cycle appears to have bits set for a write cycle.  So I ignored that, and then later on pid 4 (and sometimes 3) would
  crash because it tried to read from a page that was already loaded.  Looking through the manual it mentioned the FC (function
  code) and I thought maybe it was a different FC which was why it seemed to be a repeated cycle.  I tried ignoring the read to
  existing page as well, skipping the attempt to load another page and just return instead.  It fucking boots!  It boots fine!  What
  in the actual fuck
- it even has the same FC for both repeat cycles, so that's not it
    ```
    bus error: w fc=1 addr=20C3FC ssw=709 mmusr=404 pid=4 pc=208484
    ...
    bus error: r fc=1 addr=20C3FC ssw=B649 mmusr=204 pid=4 pc=207EFE
    ```
- So the first access has the I bit set in MMUSR, which means the table entry is invalid, but the second fault only has the M bit
  set meaning...??  The manual says "This bit is set if the page descriptor has the modified bit set".  So it just causes a fault no
  matter what, just because that bit is set?  Why would you want that?

- It's now the evening, and the first day back to work is done, so I can fully turn my attention back to this, after testing that
  change first thing in the morning
- I've had all day to think about this in the back of my brain, and search random things on the web.  I didn't turn up anything on
  the web.  I thought for a brief time that maybe I had the wrong SSW location, but it's definitely the SSW I'm looking at
- I was just reading through the manual again after taking another look at Linux's m68k page fault handler.  There's a comment in
  the linux code that says "if an instruction fault and a data fault occur at the same time, then map in both pages".  It can have
  two faults at the same time??
- so that got me thinking and reading the SSW section again, and it does say, in one sentence:
    "The least significant half of the SSW applies to data cycles only."
- the RW and RM bits are in the lower half of the SSW *facepalm*  So they only apply if DF is set, otherwise you must ignore them
  and assume it's a read-only instruction fault cycle

- talking to thorpej, and he mentioned the figuring out the fault only from the MMU SR bits.  You really only need to know if it was
  a write protect error, which is its own bit in MMU SR. The invalid (I) bit would mean you should load the page if possible.  And
  there's a privilege bit too
- that said, I could also use the SSW value, now that I know how to interpret it

- I understand the case where it reads an instruction, but the write flag is set.  But I don't understand the other fault, where it
  seems like a read, but the page already exists, and the mmusr shows only the M fault(?) occurring
- the line is: `bus error: r fc=1 addr=20C3FC ssw=B649 mmusr=204 pid=4 pc=207EFE`
  So it's only an instruction fault, not a data fault
- wait... do they need to be fetched separately?  Since instruction and data are separate spaces (and the ATC entries have the FC
  included in them) then it needs load the page twice for instruction or data, so... only copy-on-write if DF is set and write bits
  are set, and instruction fetching a page that's already loaded just returns

- Haha! I thought it was working, and then I ran the `ps` command and it's infinite excepting with a totally messed up address of
  4CDF0C04, which is some kind of incorrect value
- you know, there is the PVALID instruction which can be used to validate an address before dereferencing it, but also, you need to
  make the exception handler actually kill the process


2026-01-06:

- tracing the stack of this `ps` error, it's a fault in printing to a string, which comes from the procfs's `get_data_stat`
  function, which makes sense given it's in `ps`
- one look at that function, and I can see it's indexing into proc->map->argv, so I'm almost certain that's the source of the fault.
  It's a userspace address, so the kernel shouldn't be accessing it directly

- I went and made generic mmu copy functions, but then I made them only defined when MMU is enabled, and added the to procfs, but
  forgot that since it's I used the user copy functions, they won't work and crash all the time, but they shouldn't use them at all
  since it looks up each page instead


2026-01-08:

- I think there's some kind of corruption somewhere.  One of the clues is that when printing the idle task's args, it prints a weird
  character instead of the last digit and newline, but it only does it then
- at the same time, dumping the stack after it boots, based on the address that ps prints, the args look fine, but not the envs, or
  just something is weird with that stack
- the other addresses printed by ps show what look like valid addresses, but the on those stacks is definitely not arguments.  It
  looks like code (4e75, 660C, etc opcodes), so something is getting a page wrong, or a page is being erroneously freed and reused,
  or somethings written past its own memory, or an address calculation is going awry


2026-01-10:

- trying various prints to trace the memory allocation, I wasn't able to find an obvious problem causing the ps command to fail to
  fetch the correct command names.  I printed the stack traces sometimes, and was getting the wrong data, but the more I tried to
  inspect it properly, and found bugs in my assumptions when printing stack data, I found that it actually does seem to be correct
- there was one minor/sorta bug, where the idle task was allocating a stack on the kernel heap instead of as a page, so it wasn't
  page aligned, which threw me off.  I changed that since it won't be able to make it out properly, even though it doesn't use the
  stack at all
- I think the actual bug might be that the proc filesystem data functions are not actually getting the right pages somehow, since
  the data is definitely wrong, like it's clearly 68k instructions and not a stack.  Hmm... need to verify again with a different
  process

- yup, looking at pid 4's stack, and also pid 6's when it was first created, and again with dump (when that page was not otherwise
  reused after being freed and filled with code instead), the stack looked more or less fine, and the addresses still matched the
  virtual address despite copy on write and such
- ah I see a problem.  The generic_memcpy_from_user seems to work fine, and the pages it fetching look fine, but when it copies
  using generic_strncpy_from_user, it gets a totally wrong address

- well, there was definitely a problem in the memcpy version, which was indexing into a `page_t *` type, so instead of being a byte
  offset, it was a number of pages offset, which explained why the data was so weird.  I've fixed that
- and then once that was fixed, I'm seeing parts of names, but they are cut off, and seemingly not in a consistent way, but
  something a bit more complicated, so I'm sure I have some off by one errors in the complex pointer calcs.  I'll have to fix that
  tomorrow though, since it's already really late, and I have an ear infection that is causing me immense pain


2026-01-11:

- it turned out that I was both incrementing the `i` and `page_offset` index values for each character being copied, and also adding
  the total chunk size to both indices at the end of the chunk, which is why it stopped early.
- it still wasn't working for the idle task, and that turned out to be because the stack wasn't mapped into the idle process's
  memory map
- that got me thinking that maybe I should make the idle task truely be a kernel thread, where the memory map is shared with the
  rest of the kernel.  The possible issue there is if there are some functions that might get called that might try to add or remove
  segments from the map, which shouldn't be done for the kernel areas


2026-01-12:

- I added a bunch more usercopy functions, which can be defined arch-specific, so that it can use the generic versions if needed,
  and passes in a map, but instead of referring directly to the generic versions that use the memory_map (mmu), it will use the
  version that only works with current_proc, if the map passed in matches current_proc.  It may be able to optimize out the
  selection when passing in current_proc->map, but I'm not positive, and not sure if it can optimize even more complicated but still
  static references to current_proc
- either way, it should be the best of either, and since it's defined as just memcpy/strncpy when no-mmu is selected, it shouldn't
  add much more overhead.  That said, there's a growing amount of bloat in the kernel.  I'm sure some part of it is unavoidable
  though
- well so much for that idea.  The .bin takes up 216 more bytes when I use the _to/_from_user_map versions and pass in
  current_proc->map than when I leave it as _to/_from_user in the COPY_USER_STRING macro, which is used in 17 places in syscall...
  so it's not zero overhead, maybe because -Os? but honestly it would be smaller to optimize so maybe it just can't


2026-01-13:

- slowly puttering away at cleaning up the usercopy code
- trying some rough optimizations to cut down on the size of code, but no real gains tbh
- I tried O3, and intially it didn't work, but it turned out to just be the monitor that wouldn't compile.  It turns out, while the
  -Os version takes 61160 bytes at the moment, the -O3 version takes 105232 bytes, which I was shocked by the size increase in the
  name of performance, but I then thought to checkt what no optimizations would do, and it turned out to be 104360, so the -O3
  version isn't really much bigger than the unoptimized version, but the -Os version is *tiny* by comparison, almost have the size!
- for completeness, the -O2 version is 75936, and the -O1 version is 68480.  That's so weird that the -O1 version is almost as small
  as -Os, but waaay smaller than -O0
- I got the monitor's -O3 working by adding fread and fwrite that just reads/writes to serial.  Not sure why I didn't implement that
  earlier.  Maybe I had intended to allow it to target other serial devices, but *shrug* I can add that later

- I need to add error checks to everything using usercopy functions, including all iter operations
- should you add a mode to iter to copy to a specific user process's memory, now that you have functions that can do that? or should
  always use the user_map functions when selecting the user_buf option?
- should you actually have an iter mode specifically for other user processes besides the current one, that always uses the no-fault
  method of reading data from pages, and also make usercopy functions specifically for that case, so when you actually want to force
  it to use the manual method, you can.  The thinking here is that sometimes you might know that the current process's state is
  invalid, and even though proc->map == current_proc->map, the MMU register is not set, or you cannot service an exception or
  something???

- actually, now that i've walked down two flights of stairs and am away from my computer, I just thought, about the place in exec
  where it's manually loading a page, and the need for this extra iter mode (which might still be useful), I can probably just force
  the CRP switch and use the faster usercpy functions, which would be better anyways, right?


2026-01-14:

- now that I've done all that work on the generic usercopy functions, looking at Linux, it has nothing like that, but it does have
  various means of using the iovec_iter with either an iovec-like struct, or folioq or others that can write to disjoint memory (ie.
  pages), so maybe instead, I should think about a function that can create such a struct from pages from the page table, which
  could be faster overall to fetch the pages (all at once) and then it doesn't need to do all the complex setup each time an iter
  function is called, which for the places I want to use it (exec, context) which is stack manipulation and pushing small values
  onto the stack, it could be a lot of overhead

- I guess I really went different with iovec, but am now kind of going closer to what linux does.  I started adding an iovec-like
  struct, but the previous def assumes it's a user buffer, so I made a new one call kiovec, then kio_vec, then kvec (<- same as
  linux), and that basically means 4 different impls, but doesn't have simple buffers.  It only has vectors of buffers.  So maybe I
  should do the same and cut out two impls.  It might be a bit slower, but if there's only one buffer in the vec, it should be about
  the same, maybe with a couple more instructions to check that there's only one buffer, and then the normal index, single memcpy,
  etc.  Most of the `generic_*_to/from_user_map` functions would turn into iter functions.  The creation of the iovec separate from
  the iter would also be an extra step, decl, etc, which might be a bit cumbersome.  Maybe I can find a way to simplify that with
  preproc macros?


2026-01-16:

- I was trying to rewrite the generic usercopy/iovec/kvec stuff and tried running it after committing a bunch of work in progress,
  in small bits, but now I can't get it to run.  Either it compiles to a bit over 62230 bytes and then doesn't load fully because
  the kernel tries to boot (and fails), or it boots fine, but as soon as you press any key, it either hangs or just doesn't respond.
  I'm not sure what's causing it
- I did a clean and then reloaded it, and now I can enter a command, but it says "command not found"
- fuuuck, stashing the changes to get to the latest commit seems to not work either =(  I've broken it somehow, and all the commits
  are probably broken too


2026-01-18:

- I finally have the tangled mess sorted out.  I've committed a bunch of things in stages, and in the process found a bug with
  SEG_FIXED where it would try to copy the pages of the segment and remap them copy-on-write, but then it wouldn't be able to
  convert them because the memory_ops would be set to NULL.  The ops are still NULL but segments marked SEG_FIXED will not have
  copy-on-write enabled (no write protect bit set)
- I had also got program memory loaded on demand enabled, which I've since disabled
- the only thing left is the iovec changes, but I've now commented out everything, and the very last thing I tried commenting out
  was the extra fields I had put in iovec_iter, which turned out to be what's causing the input to be wonky, and to get "Command Not
  Found" most of the time when trying to run anything.  It's not reading in data correctly from the tty, but why would one extra
  field in the iter cause this problem
- it seems to happen no matter what order they fields are in.  I previously had an `nsegs` field inside one of the unions, so I
  commented that out and put in the `num_segs` field I had at the top (but not the `cur_segs`, so the struct size was identical),
  and it worked fine.  If I uncomment the `cur_segs` field though, the size increased by 4 bytes, and that causes this weird
  behaviour!!!


2026-01-19:

- hmm... it does seem to be related to being inside the union or not.  I have num_segs uncommented, cur_segs commented out, and the
  old nsegs in the iovec union commented out.  If I uncomment cur_segs, whether it's before the union or after it, it causes the
  problem, but when I put it inside the iovec union, it works fine
- removing the union doesn't fix the problem.  Adding an array of 20 does fix the problem.  Adding an array of 3 does fix the
  problem.  Adding an array of 2 does not fix the problem.  Replacing the type enum with an int does not fix the problem.
- when the struct is 9 ints in size (36 bytes), it works.  When it's 6 ints (24 bytes) it works.  When it's in between that, it
  doesn't work
- I finally took a look in tty_68681 instead of just tty.  The tty driver creates an iter and then passes it into dev_read and
  dev_write of the tty_68681 driver, but the tty_68681 driver uses the copy_uint8 functions.  I thought maybe those could be the
  source of the problem, so I modified the read function to use memcpy_into_iter instead, and that didn't work, but when I modified
  the write function to also use memcpy_out_of_iter, it seems to be working!
- I'll try restoring the read function and leave the write function modified to see if it breaks with just the write.  It works with
  copy_uint8_into_iter(), but doesn't work with copy_uint8_out_of_iter()
- the only unusual thing about these functions is that they don't have a bounds check, which is a problem of course (potentially, if
  length is not checked), but I wonder if it's the kernel buf or user buf impl that's causing the problem.  Maybe I can modify it to
  only use one somehow?
- but why would the length of the struct affect it?

- so I very hackishly just removed the get_user_uint8() call in copy_uint8_out_of_iter() and it's obviously not working correctly
  because it's reading the wrong memory locations from user memory, and printing out garbage characters, but it kind of seems like
  commands are running.  I can't be certain it's not printing "Command not found", but it does print different things for different
  commands instead of always printing the same thing, so there's that.  Perhaps it's the get_user_uint8() that's a problem
- there's nothing obviously wrong about get_user_uint8() though... given that it's getting the right characters, and printing the
  right things out

- ok, more evidence.  I've put things back to normal but with the extra field to make the bug occur, and then I modified both
  copy_uint8_*_iter() functions to use memcpy_to/from_user instead of get/put user uint8.  With that, it works fine.  I then
  restored the original impl of copy_uint8_out_of_iter() to use get_user_uint8() and it's broken.  I then put that back to
  memcpy_from_user(), and modified copy_uint8_into_iter() to use put_user_uint8(), and it works fine.  So it seems like only
  get_user_uint8() is broken, even though it seems symetrical with put_user_uint8
- this problem is caused by the iter struct being bigger too, which is odd, and it's reading characters, not writing characters,
  that's breaking, so why is only the write function causing the problem?

- I'm now dumping the iter struct during these two functions, and there's definitely some weird things going on.  Only looked at the
  broken version so far, but the cur_segs field and num_segs fields seem to be modified unexpected sometimes.
      ```
      struct iovec_iter {
              int type;
              size_t offset;
              size_t length;

              int cur_seg;
              int num_segs;

              // NOTE this is simplified, it's actually a union
              char __user *buf;
              size_t nbytes;
      };
      ```
      ```
      <I think this is before it's printed the Command not found message>
      11FFAC: 0000 0001 0000 0001 0000 0001 0000 0000
      11FFBC: 0000 0000 0020 B867 0000 0001

      Clength of 11FFAC is 1 offset 0 28
      <it's printed one character, and is now printing the second?>
      11FFAC: 0000 0001 0000 0001 0000 0001 0000 0101
      11FFBC: 0000 0001 0020 B867 0000 0001

      <I think this is reading a character>
      11FFAC: 0000 0001 0000 0002 0000 0002 0020 B924
      11FFBC: 0020 B9A4 0020 9263 0000 0002
      ```
- so sometimes cur_seg has `0000 0101`, and sometimes cur_seg and num_seg have address, `20B924` and `20B9A4`, which doesn't seem
  right at all
- I switched copy_uint8_out_of_iter() to the working version, and tried again, and while it does work, the iter structs appear the
  same as above with these weird fields
- Now I've switched both to the working version, and it still has the weird numbers in the iter struct!?  wtf.  Is something else
  messing up the iter struct, or are these values carry overs?  I guess I'm not zeroing those values in the struct so that would be
  misleading... I should do that

- ok that makes more sense now.  Those fields are initialized, and they're now the initialized values with the working version, and
  they're also the init'd values when using the broken version, so I don't think this is the source of the issue

- I tried copying the inline asm from the working memcpy_from_iter into get_user_uint8, and it's producing a bunch of garbage
  characters now, but it kind of seems like it's working despite that.  Not sure why the garbage characters
- but in the process of changing that, I noticed the `result` variable that the inline assembly is supposed to copy into is actually
  a uintptr_t instead of a uint8_t.  I thought "surely that must be the problem", but alas, no.  It seems to still be failing with
  that switched to a uint8_t

- I tried again to switch the inline assembly, this time with uint8_t instead of uintptr_t, which would have explained the garbage
  characters from the attempt before, and now it does seem to work.  No idea why the original doesn't work though.  Here are the
  two:
    Original (broken):
    ```
        asm volatile(
                "moves.b        (%1), %0\n"
                : "=d" (result) : "a" (src) :
        );
    ```
    Replacement (working):
    ```
        asm volatile(
                "moves.b        (%1), %%d0\n"
                "move.b                %%d0, (%0)\n"
                : : "a" (&result), "a" (src) : "d0"
        );
    ```
- the thing that especially confusing me is why adding an extra field to the struct causing it to break, when there's no reference
  to the iterator here.  It's given a pointer into the iterator buffer, which is `src` in these snippets
- Here's the assembly that's generated in tty_68681_write:
    Broken:
    ```
      106e1a:       4e7b 0000       movec %d0,%sfc
      106e1e:       206a 0014       moveal %a2@(20),%a0
      106e22:       d1c1            addal %d1,%a0
      106e24:       0e10 0000       movesb %a0@,%d0
      106e28:       0280 0000 00ff  andil #255,%d0
      106e2e:       2f00            movel %d0,%sp@-
    ```
    Working:
    ```
      106e1e:       4e7b 0000       movec %d0,%sfc
      106e22:       206e 0014       moveal %fp@(20),%a0
      106e26:       d1c1            addal %d1,%a0
      106e28:       0e10 0000       movesb %a0@,%d0
      106e2c:       1880            moveb %d0,%a4@
      106e2e:       102f 0023       moveb %sp@(35),%d0
      106e32:       0280 0000 00ff  andil #255,%d0
      106e38:       2f00            movel %d0,%sp@-
    ```
- the working code here seems kind of broken, with that weird `moveb %sp@(35),%d0`, but it seems there's a `lea %sp@(35),%a4`
  further down in the code which then conditionally jumps to above the given code, so I think %a4@ and %sp@(35) are pointing to the
  same memory location, even though it seems like weird indirection

- I have to figure this out, so I'll include longer snippets
  Working:
  ```
  int tty_68681_write(devminor_t minor, offset_t offset, struct iovec_iter *iter)

  106d7a:       598f            subql #4,%sp
  106d7c:       48e7 383e       moveml %d2-%d4/%a2-%fp,%sp@-
  106d80:       242f 0028       movel %sp@(40),%d2
  106d84:       2c6f 0030       moveal %sp@(48),%fp
  ...
  106e12:       222e 0004       movel %fp@(4),%d1
  106e16:       2801            movel %d1,%d4
  106e18:       5284            addql #1,%d4
  106e1a:       2d44 0004       movel %d4,%fp@(4)
  106e1e:       4e7b 0000       movec %d0,%sfc
  106e22:       206e 0014       moveal %fp@(20),%a0
  106e26:       d1c1            addal %d1,%a0
  106e28:       0e10 0000       movesb %a0@,%d0
  106e2c:       1880            moveb %d0,%a4@
  106e2e:       102f 0023       moveb %sp@(35),%d0
  106e32:       0280 0000 00ff  andil #255,%d0
  106e38:       2f00            movel %d0,%sp@-
  106e3a:       2f0a            movel %a2,%sp@-
  106e3c:       4eba feb8       jsr %pc@(106cf6 <tty_68681_putchar_buffered>)
  106e40:       5383            subql #1,%d3
  106e42:       508f            addql #8,%sp
  106e44:       4a83            tstl %d3
  106e46:       66ac            bnes 106df4 <tty_68681_write+0x7a>
  ...
  106e62:       2602            movel %d2,%d3
  106e64:       49ef 0023       lea %sp@(35),%a4
  ```
- so I think `%fp` has the value `iter` on the stack, the pointer to the iterator
- %fp + 4 == offset, so it increments the offset using %d4, but saves the old value in %d1, which it then adds to the pointer to
  the buffer, which is taken from %fp + 20, and put into %a0
- the address stored in %a0 is then dereferenced in the `moves.b` instruction and put into %d0, which is then moved into %a4
- the only place that %a4 seems to be set is down near the bottom, but it's kind of an odd value.  It must be a local variable, but
  it's also literally an odd number (35).  The instruction that uses it is a byte operation though, so it should be ok.  It might be
  the `subql #4,%sp` at the start, which allocates a 4 bytes space on the stack for a local variable (the stack has to be word
  aligned at the very least)
- so really it's just putting the value it gets from %a0 into a local variable, pulling that same value off the stack into %d0
  again, and using that to pass into tty_68681_putchar_buffered

- Now the broken version with the extra field, and without the extra field that causes the problem:
  Working version:
  ```
  106d7a:       48e7 383c       moveml %d2-%d4/%a2-%a5,%sp@-
  106d7e:       242f 0020       movel %sp@(32),%d2
  106d82:       246f 0028       moveal %sp@(40),%a2
  106d86:       4878 0018       pea 18 <SHORT_CONTEXT_SIZE>
  106d8a:       2f2a 0004       movel %a2@(4),%sp@-
  106d8e:       2f2a 0008       movel %a2@(8),%sp@-
  106d92:       2f0a            movel %a2,%sp@-
  106d94:       4879 0010 e738  pea 10e738 <CSWTCH.26+0x748>
  106d9a:       49f9 0010 d0f2  lea 10d0f2 <printk>,%a4
  106da0:       4e94            jsr %a4@
  ...
  106e0e:       222a 0004       movel %a2@(4),%d1
  106e12:       2801            movel %d1,%d4
  106e14:       5284            addql #1,%d4
  106e16:       2544 0004       movel %d4,%a2@(4)
  106e1a:       4e7b 0000       movec %d0,%sfc
  106e1e:       206a 0010       moveal %a2@(16),%a0
  106e22:       d1c1            addal %d1,%a0
  106e24:       0e10 0000       movesb %a0@,%d0
  106e28:       0280 0000 00ff  andil #255,%d0
  106e2e:       2f00            movel %d0,%sp@-
  106e30:       2f0b            movel %a3,%sp@-
  106e32:       4eba fec2       jsr %pc@(106cf6 <tty_68681_putchar_buffered>)
  106e36:       5383            subql #1,%d3
  106e38:       508f            addql #8,%sp
  106e3a:       4a83            tstl %d3
  106e3c:       66b2            bnes 106df0 <tty_68681_write+0x76>
  ```

  Broken version:
  ```
  106d7a:       48e7 383c       moveml %d2-%d4/%a2-%a5,%sp@-
  106d7e:       242f 0020       movel %sp@(32),%d2
  106d82:       246f 0028       moveal %sp@(40),%a2
  106d86:       4878 001c       pea 1c <SHORT_CONTEXT_SIZE+0x4>
  106d8a:       2f2a 0004       movel %a2@(4),%sp@-
  106d8e:       2f2a 0008       movel %a2@(8),%sp@-
  106d92:       2f0a            movel %a2,%sp@-
  106d94:       4879 0010 e738  pea 10e738 <CSWTCH.26+0x748>
  106d9a:       49f9 0010 d0f2  lea 10d0f2 <printk>,%a4
  106da0:       4e94            jsr %a4@
  ...
  106e0e:       222a 0004       movel %a2@(4),%d1
  106e12:       2801            movel %d1,%d4
  106e14:       5284            addql #1,%d4
  106e16:       2544 0004       movel %d4,%a2@(4)
  106e1a:       4e7b 0000       movec %d0,%sfc
  106e1e:       206a 0014       moveal %a2@(20),%a0
  106e22:       d1c1            addal %d1,%a0
  106e24:       0e10 0000       movesb %a0@,%d0
  106e28:       0280 0000 00ff  andil #255,%d0
  106e2e:       2f00            movel %d0,%sp@-
  106e30:       2f0b            movel %a3,%sp@-
  106e32:       4eba fec2       jsr %pc@(106cf6 <tty_68681_putchar_buffered>)
  106e36:       5383            subql #1,%d3
  106e38:       508f            addql #8,%sp
  106e3a:       4a83            tstl %d3
  106e3c:       66b2            bnes 106df0 <tty_68681_write+0x76>
  ```
- So, the entry point seems to be the same.  It saves a bunch of registers on the stack, and then fetches the first argument in
  %sp+32, and then the third argument (iter) at %sp+40, which goes into %a2, so that's our iter pointer
- it has a `pea` instruction which is different, and that seems to match the size of the iter struct, but not sure if that's
  actually what it's for.  Maybe it's actually just an argument to printk
- Ah yes, that's actually the printk in the iovec_iter_length() function which contains the iter size as the last arg to print
  `printk("length of %x is %d offset %d %d\n", iter, iter->length, iter->offset, sizeof(struct iovec_iter));`
- these two functions are identical except for `moveal %a2@(16),%a0` where the offset is different because of the added field

- I've looked at the whole fucking function for both side by side, and there's no difference between them except for the size in the
  printk call, and one offset used to index into the iter struct, which are correct in both cases.  I have no idea why one works and
  the other doesn't.  The change here is just that added field in the struct

- what the heck, now it seems to be just entirely broken again.  Can't get it to work with the different inline assembly.  Maybe
  this was a red herring all along


2026-01-19:

- well when I tried it again, with cur_segs in iovec_iter, and the two instruction moves/get_user_uint8, it's working.  If I comment
  out `cur_segs`, it won't accept input at the shell, so I don't think the problem is related to that change.  I think it's related
  to the size/alignment of having one extra instruction or something.  Maybe a bad pointer or reused memory address, or buffer
  overflow or something


2026-01-23:

- have I not had this hanging problem before, and it went away after changing something that didn't actually fix the problem, or
  else the problem is something inherent that can happen if the conditions are right, and I don't have the right conditions?

- am I reading this right?  printk's `buffered` argument seems backwards.  `printk_direct` calls `vprintk()` with buffered=1, which
  makes the version of putchar used be `console_putchar_buffered`

- what if moving the location of tty.o and tty*.o within the binary changes whether it breaks or not
- put print statements in both read_input in tty, and suspend/resume in tty* to see which of them is running or not


2026-01-24:

- I tried rearranging the order of object files and tried using libc-68k-none instead of libc-68k-os (which ran fine) but it's still
  doing the command not found thing, and typing seems to echo two characters
- it kind of seems like it either reads the character in twice, or none, instead of once, which implies something to do with lengths
  or indicies.  I noticed in the ringbuffer impl, it uses `volatile` for the indicies, which makes me wonder if there's some
  optimization thing happening that's breaking some assumption somewhere, and I came across this in the past, but didn't write about
  it in these logs

- for that printk thing, it was all the way back in 2021 that I last touched the buffered/direct stuff where I renamed printk to
  printk_direct and printk_safe to printk, and switched it from being a direct flag to a buffered flag, but it was the same
  orientation (safe == direct, <none> == buffered), and it was back in Sept 2020 that I added that flag in that orientation.  So I
  guess it's been wrong since then?  It seems to work fine when using either only direct or only buffered *shrug*
- printk_direct is not used anywhere

- now I'll try the print statements in the various tty input functions again.  I'll make them really short to minimize interference
- only put them in the read functions and the tty_read_input function (3 places)
- and of course even that change was enough to make it work *eye roll*
- it's gotta be some weird volatile/optimization related thing

- Output from pressing `l` and then `s`
      - starting
      ```
      % r512
      ```
      - pressing `l`
      ```
      t1 R
      r512 t1 R lt1 % r512
      ```
      - pressing `s`
      ```
      t1 R st1
      ```
- it's weird that it prints an improper `\n% ` or something.  I guess since it also prints an extra couple `r512`s that it's
  returning from the tty_read call when it shouldn't.  The `r512` is from a read syscall on the tty, trying to read the whole input
  buffer coming from the shell.  The `R` is reading a single character in the read_input function.  The `t1` is the 68681 driver
  reading a single character

- alright, slightly different prints, this is the first character printed after booting, which seems to be broken, and then after
  that it works (when these print statements are there).  `s` is suspending in the tty_read syscall, `t` is reading a character in
  the 68681 driver.  `R` is reading a character in the tty read_input function.  `w` is waking up (resuming the syscall proc) in the
  tty read_input function.  The character I actually typed was `a`
      ```
      % s t R t R
      w t R at % s
      ```
- so the first `% s` was were it was waiting, and then pressing `a` made it print two `t R` sequences (so it read in two characters
  instead of just one).  One of the characters it read caused it to wake up the process, and then it reads the actual `a` character
  whith `t R at ` and I think the `%` that gets printed happens at a different time kind of out of sequence compared to when the
  process actually wakes up (ie. the `t R at` gets printed before the process has time to call printf("% ");)
- interesting to note: it's the print statements in the read syscalls that seem to cause it to "work" (as in, it fails on the first
  command after boot but then seems to read in the correct commands and run programs correctly after that).  I can have the prints
  in the read_input function and it will always give "Command not found"

- so I'm now trying to replace the copy_uint8_into_iter function in tty68681_read, and I started by splitting it to use an
  intermediate byte, which even a volatile on it didn't get it working.  Now I'll try replacing it with memcpy.  My hunch here is
  that it's writing data incorrectly into the iter, and possibly overwriting something in the iter struct?  I don't expect this to
  actually change much though
- as expected, that didn't actually change anything.  It's still waking up the process when it shouldn't, and it's still printing
  command not found when I press enter.  I did have it totally lock up when I fumbled and pressed a `/` character.  It seems to
  happen whenever I press slash and then any other character
- So my suspicion now is that it's something in read_input, specifically its use of iter, and by extension, something in the iter
  functions it uses there (and not in 68681, or in the memcpy/copy_uint8 into_iter functions)

- next thing, I replaced the `char ch` in read_input with `char buf[4];` and then copy the first character into `ch` for the rest of
  the function, but the kernel buffer in iovec_iter is the buf and not ch in case it overflows.
- when I wasnt' initializing it, it did seem to have a different value as buf[3] sometimes, but after initializing it to 0 before
  calling iovec_iter_init_kernel_buf, it seems to always be 0 (except the first character which is what's being read in), so I don't
  think there's a problem with the iter part of it.  It is working like this except for right after boot.  The first character read
  in is always `\n` which is not being pressed

- now I'm trying to actually print the byte that's read in from the serial.  I wonder if it is actually the ringbuffer impl that's
  wrong, and has some index issue
- I press `l` and I get:
    ```
    13 10
    w 108 lerror
    ```
  where `error` is the dev_read() function call in `tty_read_input` returning 0 because it has no character, so it might not just be
  a `\n` character but `\r\n`?  It's definitely wrong, and it's definitely in tty_68681 that the erroneous characters are coming
  from.  `tty_68681_getchar_buffered` is producing these characters
- so the code of `tty_68681_getchar_buffered` has a busy loop of some kind.  It had a putchar commented out inside of it but
  uncommenting it (and fixing it to actually compile) doesn't print anything so it's probably not hitting that busy loop
- there's a lock/unlock in this function, and then the only other things are ringbuffer functions...
- so I guess the next question is, is it mistakenly reading the ringbuffer, or does the ringbuffer erroneously have extra characters
  in it
- I changed the variable that assigns the output of _buf_get_char() to be `volatile` and now it's working... so weird.  When I say
  working, it runs commands, but it still has an erroneous `\n` that gets read in, but it doesn't seem to cause a problem
- what if I remove the `volatile` keywords from the ringbuffer struct?  (and leave it off the getchar var like it was originally)?
  It's broken like it was before (Command not found always).  Putting volatile in getchar but not in ringbuffer and it also is
  broken, so it's only if both are volatile that it works (well works-ish, still not entirely right)
- making getchar a normal function instead of a static inline, it seems to always work (still an extra `\n` but always runs
  commands).  With or without the volatile inside of it.  It does produce a slightly different function based on the volatile
  though.  Oddly enough, it seems like the non-volatile version is the one that's a bit longer *shrug*
- my shitty attempt at printing inside the interrupt in 68681 didn't work so well.  Trying again but just printing a single
  character instead of trying to print which character was just read... so we'll see if it actually reads in two characters when I
  press one.  It does, every time I press one character, it prints two `^` characters.  And interestingly enough, it's a sliding
  window.  It's still printing the byte read in from 68681's read function.  It prints two `^^` characters, and then prints two
  decimal ascii values.  The first `l` pressed does `^^10 \n 108`, then `s` does `^^108 l115 s` (so it reads in the `l` a second
  time, and then reads `s`), and then `^^115 s 112 p` when I press `p`
- this is really weird.  I kind of wouldn't have been surprised if it was reading twice from the ringbuffer, but it seems like it's
  actually reading twice from the 68681 chip, which is what doesn't make sense here.  This is not a hardware issue...

- reading through the code for the 68681 driver, there's something weird (possibly unrelated) with the ISR flags.  I have them
  defined twice, `ISR_CH_A_RX_READY_FULL` but then also `ISR_S_TX_READY`, and it's the latter one that I'm using in the
  handle_channel_io function, which implies that if channel B's ready flag is set, it might think channel A has data to read?  If
  this is true though, it should totally not work properly, shouldn't it?  Like channel B (the slip line) should just totally not
  work.  I guess last time I tried it, it didn't work, but it did work at one point, so did I break it since then?  Did it only work
  as a fluke before?  So bizzare
- it seems like it was back in December 2020 that I made the change that both added those _S_ flags (and the _CH_ flags), as well as
  the function that handles io, so it's been around for quite a while
- aaand changing it to use a variable for the flag makes it hang after begin_multitasking...  It does seem to be switching, but
  nothing gets printed, so it must be wrong
- what if I make them both be CH_A like it was before, but just with the indirection?  It does seem to boot... so it's not the
  indirection, it's the actual flags/bits
- actually I guess I can easily see if it's the fact that it checks A and then B in the ISR that's causing it to read in two
  characters instead of just one.  Nope, it looks like it is actually reading two characters from channel A (minor number is 0 both
  times)
- oh my god!  what trickery!  I see what's going on now.  I have a conditional shift at the start of the function which I'm using to
  allow me to not have to use a separate flag in the channel, and instead have the hardcoded check...  that's why it worked with
  those weird flags
- so that was just a red herring.  I'm pretty sure I've made this mistake before
- you know... oddly enough it seems like the version that has the shift + hardcoded both does the wrong shift (ASR instead of LSR)
  but also is longer than the one that uses the flags in the channel struct...  The one that uses the channel flags uses an extra
  int on the stack, and maybe has more memory read/writes or something, but it has 32 fewer instruction bytes...
- just to note, using an unsigned char for the ISR register value instead of a signed char will make it use the correct LSR
  instruction instead of ASR, which could alter the meaning if there's an input change interrupt at the same time
- I've changed the few places that used char in the tty driver to uint8_t

- so after recompiling again, with the modified ISR with the flags instead of the shifting, and the uint8_ts added everywhere, not
  sure if this is different than before, or I just didn't try this before: I tried entering characters, abcdefgh... to see the codes
  printed.  `a` prints `^97 a`, then `b` prints `^^^10 97 a98 b`, then `c` prints `^99 c`, then `d` print `^^^98 b99 c100 d`
- so it's every other character reads in 3 characters, and then reads in 1 character, etc

- tried a bunch of things, sometimes adding a volatile will fix something, like volatile to iovec_iter->offset, but then adding the
  cur_segs field will break it again
- when it works correctly, the ISR's status is read once as 0x0D, and then reads as 0x0C and returns, reading one character at a
  time, except for the very first time which reads a `\n`
- when it's broken, it reads every character twice, returning 0x0D twice in a row, and reading the same value... which makes it seem
  like a timing issue, or hardware issue
- if it's a timing issue, will adding a delay to the loop fix the problem?
- i put a 1000 times spin look in, and that didn't do anything
- honestly it would be really weird if it was a hardware issue, when it almost always works otherwise

- looking at the code again while downstairs, the handle_channel_io is called from handle_serial_irq which was recently changed to
  not be called directly anymore, and it's possible this bug has been lurking since then.
- but that said, it's still true that that loop in handle io definitely runs twice when it's broken, not two interrupts or anything


2026-01-25:

- so much back and forth
- at one point, I noticed that sometimes the elf would have two program segments instead of just one.  I've adjusted the linker
  script to add alignments after each segment, which I think has fixed that, at least so far I haven't noticed two program segments,
  but that didn't seem to have any effect on whether it was working or not
- I've got a change, where if I make the `char ch` in tty_read_input volatile, it will work perfectly fine, and if I then add a
  field to the iter, it will break again.  I was about to say the code is identical, but a diff says otherwise... will inspect in a
  second, but I assume it's just some minor address changes

- it looked like the diffs were mostly just offsets that were incremented by 4 in the broken code.  I then changed the extra field
  to be an array of 2, so two new ints, and it's working again
- also, putting the field at the bottom of the struct didn't change anything
- if it's as single char instead of an int, it's still broken (when no additional field is working)
- 2 chars is broken, 5 and 6 chars is broken
- 8 chars (same as two ints) in working.  9 chars is also working (as is 3 ints)
- tried 2 to 8 additional ints, and all of them work.  It's only 1 int that causes the problem

- with weird behaviours like this, my inclination is to suspect hardware (unlikely here), hard-coded memory addresses (but there
  nothing obviously different between working and not), or assembly code (where the funny business tends to happen)
- looking at the syscall entry, I notice that when an interrupt occurs, it's switching to the kernel stack, but if it's already in
  the kernel when the interrupt occurs (not that there seems to be an inconsistent difference in this case), it's possible it might
  use the wrong stack value, since the current kernel stack isn't being saved before that.  It could clobber the stack
- this may be unrelated, but I think this could be a latent bug of its own.  I could see it not being triggered often at all in the
  current tests I tend to run
- oh wait, I forgot that it already checks if it's in the kernel or not, and bypasses the stack switch, because yeah, that would
  likely cause a major issue, and I seem to recall now that it did in the past, which is why it's been fixed wit that
  kernel_reentries value

- I tried adding the frame pointer to the irq stack in addition to the irq number.  I had a print statement in the do_irq function
  but that slowed things down to a painful crawl so I removed the print and just have the extra stack element.  It's breaking a bit
  differently now.  It doesn't recognize the <enter> press.  The first `l` is read as `\n` rather than `\n` + `l`.  The `s` is read
  as just an `s`, but then enter is only `\r` and not `\r\n`, so it doesn't run anything.  Changing the interrupt stack changes
  it???  It still runs the serial interrupt handler...
- adding a 3rd number to the irq stack puts it back to the normal breaking, with "Command not found"
- hmm.... I tried adding the frame pointer to the handlers, passing it from do_irq, and it now prints the first `B` in `Booting...`
  at the start, and the stops.  I'm not printing it or anything, just passing it through... what gives
- I noticed some possible weirdness when adding the argument though, so maybe that's related?
- ok wtf, I can't get it to go back to where it was before...  It's still doing the `B` thing with those changes undo (I think)
- oh, it was because I had tried out two instructions to remove the args instead of one that adds 8 to the stack in the
  syscall_entry code, so the stack was getting corrupted.  Fixed that and trying again

- Ok, so it was breaking before, and I'm now printing the stack frame pointer, which is always the same when the serial interrupt
  fires
- removed the extra field to make it work again, so now it's working, and the stack frame pointer in the interrupt is exactly the
  same every time, 0x202BF8, so it's not related to the stack?
- the weird thing I came across was that syscall was still calling arch_set_irq_handler, but that was just setting it to
  `enter_syscall` like the arch-specific init code was doing, so I've removed it
- the code is actually behaving a bit differently, where enter only seems to receive `\r` and not `\r\n`... Wait, this was supposed
  to be working!

- just adding the frame pointer to the irq stack makes it break.  Removing the syscall interrupt set code didn't change anything
- adding the field to the struct doesn't change anything once it's broken because of that extra irq stack value
- adding NOP(); macros after each register write in the tty 68681 init didn't change anything
- not sure if this is the same as before, but it's entering the isr twice.  It's printing the status byte too, which is E0, and then
  doesn't read a character.  This is when it's working, but I think it does the same when broken too.  I had assumed when I saw it
  earlier, it was because of transmitting, but it does have the receive bit set which gets into the while loop in handle_channel_io
- it's as if it's behaving a bit differently now.  When it works, it reads in once character, two runs through the loop, and also
  two runs through the ISR.  When it's broken, it's as if it's reading in 3 charaters, 4 runs through the loop, and two runs through
  the ISR
- according to the ISR register, it's always 0x02 when it enters the rx check loop, so the tx interrupt is never occurring at the
  same time.  The extra pass through the ISR can't be explained as being related to tx, but it might be related to channel B since
  the status byte is 0xE0
- disconnecting the second USB cable to /dev/tty1 doesn't change the behaviour, but the ISR register is now 0x42.  This is after a
  reset and booting the same image
- oh yeah, looking at the code again, the second run through the handle_channel_io function is because it's a loop in the parent
  function with the same value of ISR, so it checks both channels every time in separate calls.  The second run is checking channel
  B and it reads the status byte, which is 0xE0, where the channel B ready flag is 0, so it breaks at the first conditional

- the wasn't an rx reset, and the kernel interrupt config happened after the device interrupt config, but changing both of those
  didn't change the behaviour
- I also tried disabling the channel B interrupts, and that didn't change anything.  It's of course still doing the second ISR run
  too since there's only a shared bit in ISR for either channel A or B's rx ready
- removing the volatiles from the serial isr stuff didn't change anything, it's still broken (currently in broken state because of
  the extra field in the iovec_iter struct)
- I tried printing the address of the status register, in case it was changed after boot or something, but it's always the same, and
  the second isr call is definitely for channel B with a different register value
- I tried adding a RESET instruction to the start of the tty init, and that also didn't seem to change anything
- I had the flags wrong for channel B.  Maybe they got undone when I was undoing other changes or something, which caused those
  <0xE0> statuses, but it seems to be only running through once now.  It does actually have separate ISR bits for channel A and B

- oh my fuuuucking god.  Before I went to dinner I thought, "I guess I should try disabling the MMU to see if there's any change.
  It'll probably work because it will be so different", and then there were a few mistakes that needed to be fixed to get it
  working, so I left it until after dinner.  One of the mistakes that prevented it from compiling was that the memory map for the
  kernel wasn't being created.  Since it still uses memory_map without the MMU enabled, I decided to fix the mm/init.c file that
  creates it, so that it's still created when MMU is disabled.  There was actually an incorrect flag, MMU_FLAG_SUPERVISOR instead of
  SEG_SUPERVISOR being passed into the second segment that's mapped.  The incorrect flag's value was the same as to SEG_READ so no
  problem there.  It would have just been ignored.  And when I ran it, it did indeed work.  But then the thought occurred to me:
  the second segment being mapped has the cache inhibit flag set.  What if that's related?!
- I haven't explicitly enabled the cache.  I might not have the explicit dip switch to disable it set.  If the memory mapped I/O for
  the serial port, which is located at 0x700000, is actually being cached, that could definitely explain the problem I'm seeing, but
  if the cache isn't enabled or working, then it might be a moot point, mistaken path
- I was about to say "well the compact flash card/ata driver that accesses 0x600000 would break if the cache inhibit flag wasn't
  set" but this board doesn't have one onboard.  It's on Tom Storey's VME card, which is mapped to 0xFF00_0000, which does have the
  cache inhibit set, so this could be consistent...  This is all a bit speculative though.  I need to just test this
- I keep running into errors when mapping various segments.  I always forget that the second argument is the length and not the end
  address.  I'm surprised it still booted somewhat without the upper segments

- aaand it doesn't change anything from the looks of it.  I'm not surprised, but I am disappointed because that would have explained
  this behaviour somewhat, although sometimes the status byte is 0x01, then 0x0D, then 0x0C.  If it always read it as 0x0D 0x0D 0x0C
  that would have made more sense
- also the cache disable jumper is actually in place, so it shouldn't have been the cache anyways.  Ah well, I guess it's something
  else

- I tried making it use 4K pages and that failed during init, so I'll have to come back to that and fix it another time
- oh actually I didn't change CONFIG_PAGE_SIZE, only CONFIG_PAGE_SIZE_4K
- I tried removing the memory block device, with no change
- I tried changing the stack sizes, both CONFIG_KERNEL_STACK_SIZE and CONFIG_USER_STACK_SIZE, to be much bigger, and for some reason
  it's now writing characters sooo painfully slowly.  I had this same behaviour earlier, but I can't remember what it was now...
- that was when I had a print statement in the interrupt.  That doesn't make sense that the stack size would cause this.  I wonder
  if this is perhaps important
- I'm not sure if this is even going to finish booting...

- I tried increasing the stack sizes, which didn't work, slow, wouldn't boot
- then I tried changing binaries to use on-demand loading instead of fixed memory, but that also booted really slowly
- without those changes it boots normally.  What's making it be really slow to write to tty
- when I let it run, it very slowly printed three and a half bus errors after begin multitasking, and then it printed
  bubububububububu...  as in it was having too many bus errors
- it's possible all the startup messages are printed into the buffer, and then it's already booted by the time the buffer is written
  out to the tty.  The bububu is probably a bunch of bus errors occurring, printing out that message, and having no space in the
  buffer left.  This is probably directly related to the fact that I'm using buffered printing now instead of direct printing... I
  guess that's why it was set that way =P
- that's exactly what was the matter.  Changing to direct printing now prints all the bus errors.  It's still broken on tty input
  like before, but it at least boots successfully

- setting the user stack size to 32k non-stop bus errors for the same address in init as soon as it boots
- 16k stack size works more or less as normal, but doesn't fix tty problem of course

- 32K is causing a bus error for copy on write for the same address, which oscillates swapping out between two page addresses. I
  don't know why it doesn't resolve the first time and reoccurs
- oh, I see the problem here.  I printed out the mmu table after each bus error, and the memory at the end has 3 large pages, and
  since they're early termination large pages, they can't be swapped out by a single page.  I think this is a recently introduced
  bug because I think it previously forced individual pages
- odd, setting the get_table flags to 0 in mmu_table_get_page doesn't fix the problem...

- configured networking out of the kernel but that didn't help.  So it seems like it's not really a size thing.  MMU disabled does
  fix the problem, but everything else I tried in the config didn't fix it or change it other than discovering new bugs that seem to
  be unrelated


2026-01-26:

- Ok so I'm trying to step back a bit:
    - it works with MMU enabled, only the num_segs field in iovec_iter, and the single instruction `moves` in get_user_uint8()
    - it works with MMU enabled, cur_segs+num_segs fields are in iovec_iter, and the double instruction `moves` in get_user_uint8()
    - it works with MMU disabled (which doesn't use get_user_uint8()), and either the one or two fields in iovec_iter
    - it doesn't work with MMU enabled, only num_segs, and the double instruction in get_user_uint8
    - it doesn't work with MMU enabled, cur_segs+num_segs, and the single instruction in get_user_uint8
- the MMU disabled changes so many things, it's hard to know what might be responsible
- I'm trying to use memory_region, but I accidentally left MMU disabled, and it won't compile with the partial code I have going on
- I've converted the insert_heap_stack function to use a preallocated chunk of memory, and switched back to copy_stack so that it
  works when cloning without copy on write.  It's still breaking in the normal ways, and working in the normal ways, more or less
- so it's not the copy on write code, or anything.  What about disable page frees?

- well well well... something different and equally bizarre, and also the same tty issue
- for some reason, pid 5 tries to access 0x42A80020, which causes it to be fatally terminated.  It's the echo command in shell,
  which forks itself and then calls the command function in the same binary.  It must be related to that, like the data or something
  is not cloned or copy-on-write enabled? or maybe there's actually some bug.  The really odd part about this is that it happens
  because the page free functions do nothing now.  That shouldn't change any behaviour.  The fact that it does kind of indicates
  that some memory is being used that shouldn't be, right?  Or can the data segment alone explain this?
- the address of the fault is exactly the rts instruction after the fork, so that makes sense if the stack is not being copied, but
  it should be

- So after chasing this a bit, it's the mmu_table_free_inner function which is causing the problem, when the two single page frees
  are commented out.  The stack address when the fault occurs is 0x2127A4, but it says the physical page is 0xA4.  The contents of
  either address don't match the fault pointer contents.  Like it should be a return value on the stack, but that address is the
  kernel stack and not the user stack.

- as a note, I've left it such that the page_free_singles are commented out, and the insert_heap_stack and fork code is allocating
  a chunk and copying the stack instead copy on write


2026-01-28:

- this is so bizzare.  Today it seems like if I comment out the `movec` instruction in get_user_uint8(), which is used by
  tty_68681_write, or if I replace that function (actually copy_uint8_out_of_iter()) with memcpy_out_of_iter(), it seems to work
  fine, with either iovec_iter fields
- below is two versions of the tty_68681_write function, which seems to be causing this break

  Broken tty_68681_write
  ```
  106bf8:       222a 0004       movel %a2@(4),%d1
  106bfc:       2801            movel %d1,%d4
  106bfe:       5284            addql #1,%d4
  106c00:       2544 0004       movel %d4,%a2@(4)
  106c04:       4e7b 0000       movec %d0,%sfc
  106c08:       206a 0014       moveal %a2@(20),%a0
  106c0c:       d1c1            addal %d1,%a0
  106c0e:       0e10 0000       movesb %a0@,%d0
  106c12:       0280 0000 00ff  andil #255,%d0
  106c18:       2f00            movel %d0,%sp@-
  106c1a:       2f0b            movel %a3,%sp@-
  106c1c:       4eba fee0       jsr %pc@(106afe <tty_68681_putchar_buffered>)
  ```

  Working tty_68681_write
  ```
  106bf8:       202a 0004       movel %a2@(4),%d0
  106bfc:       2200            movel %d0,%d1
  106bfe:       5281            addql #1,%d1
  106c00:       2541 0004       movel %d1,%a2@(4)
  106c04:       d0aa 0010       addl %a2@(16),%d0
  106c08:       2040            moveal %d0,%a0
  106c0a:       0e10 0000       movesb %a0@,%d0
  106c0e:       0280 0000 00ff  andil #255,%d0
  106c14:       2f00            movel %d0,%sp@-
  106c16:       2f0b            movel %a3,%sp@-
  106c18:       4eba fee4       jsr %pc@(106afe <tty_68681_putchar_buffered>)
  ```

- there's more different than just the movec instruction, but it seems functionally the same at least
- I've compiled it with the movec instruction, tested that it fails (technically a bit different in that it returns endless <E1>
  status bytes, but very similar) and then gone in and replace the two words of the MOVEC instruction with NOP instructions, and
  it's failing in the same way, so that one instruction is not the problem.  It's something caused by it being there
- this seriously is feeling like a compiler bug now

- it's probably a compiler bug =(
- I installed gcc-14-m68k-linux-gnu, instead of the default gcc-15, changed the commands in Makefile.defaults, ran make clean &&
  make, and then tried running it.  It works fine.  I changed the commands back to gcc-15, and it has the <E1> repeating.  I need to
  try a few more combinations with gcc-14 to be sure

  Working tty_68681_write with the code that was breaking above
  ```
  106dea:       222a 0004       movel %a2@(4),%d1
  106dee:       2801            movel %d1,%d4
  106df0:       5284            addql #1,%d4
  106df2:       2544 0004       movel %d4,%a2@(4)
  106df6:       4e7b 0000       movec %d0,%sfc
  106dfa:       206a 0014       moveal %a2@(20),%a0
  106dfe:       d1c1            addal %d1,%a0
  106e00:       0e10 0000       movesb %a0@,%d0
  106e04:       0280 0000 00ff  andil #255,%d0
  106e0a:       2f00            movel %d0,%sp@-
  106e0c:       2f0b            movel %a3,%sp@-
  106e0e:       4e95            jsr %a5@
  ```

- this snippet is nearly identical, but of course it was completely different code where the problem manifests (not in
  tty_68681_write, but in the interrupt handler), so not surprising.  It's somewhere else that the code is different, but for some
  reason changing the write function affects it...
- version of gcc that breaks it is Debian 15.2.0-7 (2025)
- version of gcc that works is Debian 14.3.0-8 (2024)

- maybe it's not a compiler bug after all, since I have the junk/investigations/tty-read-double/tty-extra-iter-field-or-not where
  the only difference is indirect offsets in instructions, so it can't be a compiler bug can it?
- maybe I should compare them with the raw instructions to see if they're different too?
- I went through the raw instruction diffs, and besides a spot where the diffs look weird, but actually are just differences in
  indexes, all the opcodes are the same as well between the two versions

- something to try: change all page_t * references to uintptr_t or physical_address_t and see if it behaves differently, might
  uncover some bad offsets


2026-01-29:

- I tried changing all page_t references, and that didn't change anything
- Tholin in the homebrew discord suggested it could be crosstalk, and that maybe changing the voltage by 10% might affect it
- Tom Storey mentioned the compiler sometimes optimizing byte instructions into word instructions, which caused problems

- I tired adjusting the voltage up to 5.3V and didn't see a changed, but then I tried reducing it to 4.7V.  It wouldn't run at 4.7V,
  but starting running again at 4.8V.  I then loaded the broken tty-extra-iter-field image, and while it's printing some garbles
  when I run `ps`, it seems to actually be working!!
- Oh no, spoke too soon.  I uploaded the wrong image.  The investigations one doesn't work
- I put it up to 5.5V and no change, still broken


2026-01-30:

- somewhat interesting, if I comment out dev_write in tty.c, and leave `written` as uninitialized, it crashes a bunch.  Probably not
  unexpected, but maybe it shouldn't crash so easily in that case?
- if I comment out the dev_write() call in tty.c, which should prevent get_user_uint8 from being called, it seems to still be broken

- As far as I know, the only call to get_user_uint8, and thus that broken moves instruction is in the code but not called, and it's
  still broken
- I removed the moves instruction, which changed quite a bit of code, and made the function go from ending at 0x106ea0 to 0x106e8e,
  and the code worked
- I put the instruction back in but replaced it with a regular move of the some type, the function then ended at 0x106e9e and the
  screenful of <E1> happened
- I added a nop which brought it to 0x106ea0, and it still happened
- I have the old code in place, but added an extra nop, still happening
- 2, 3, 6, 12, 24 nops still cause the problem, before or after the moves

- I removed an ATA_WAIT() call from the ata driver, and now the tty_68681_write function ends at 0x106e90 (16 bytes shorter) and it
  works... but removing the mem driver which also comes before didn't seem to work?  I guess I'll try it again
- removed it, and tty_68681_write ends at 0x106d50 (0x150 bytes less?), and it's broken
- removed just some minor code from mem.c, so the end of tty_68681_write is now 0x106e8c, still a problem
- added nops to bring it to exactly 0x106e90, and it's broken, so it's not directly related to the size
- switching to the two instruction moves from the one instruction version increased the end of tty_68681_write to 0x106eac (12 bytes
  longer for some reason)
- removing the alignment from each section of the linker script doesn't change anything, and setting it to 32 bytes also doesn't
  change anything
- I'm not sure the linker script alignment was working as expected, but I changed it to make each section fall on a 256 byte
  boundary, and that didn't change anything

- size and alignment don't seem to have an effect, but maybe the placement of data within a section does?  Could it be reading code
  as if it were data, because it has a wrong address somewhere?

- I tried going back to the commit where I got demand paging working, and specifically moving the num_segs field inside iovec, which
  is inside iovec_iter, plus the cur_segs field was the minimal change to get the input to hang.  I couldn't go farther back without
  a conflict with iovec.h

- I tried the previous commit, which required saving strarray.h/.c which wasn't in the history of the earlier commits, but that one
  change to iovec was enough to cause the problem!!
- commit 45175277d03e60a77baee3e52f4a150b3bafbd6a is not running, the init process crashes on boot
- commit b26bea29b3a37b5bad426eb88adaa2522ffafe7a has the problem with that change

- ok, so removing the union in the iovec_iter still fails with cur_segs in the struct, but works with cur_segs removed.  This is
  with an additional nsegs in iovec for padding, in all cases
- it's not even related to the size of the iter struct, because the unionified version is bigger, but still works or doesn't work
  based on one field
- this is bullshit


2026-01-31:

- looking through the git logs to see if I can find mention of this tty bug in the past, but I can't find anything related to "tty"
  or "serial" or "uart", but I'm certain I've had a case somewhere in the past where I've seen the status byte be incorrect for some
  reason, and I'm *pretty* sure it wasn't hardware related
- I should put the scope on the serial port just to confirm that, but it will take a bit to set up
- I'd like to also try printing addresses, since my best hunch at the moment is that there's an improper deref or address somewhere
  that's looking at the kernel code segment, which is causing this somehow.  I have no idea how that would relate to the tty status
  byte being incorrect, but I don't have much else to go on atm

- I added print statements to get/put uint8/uintptr, and it's not printing any addresses outside of the 20xxxx range, but it's also
  working now instead of breaking, so those additional prints are enough to throw off whatever's causing the bug
- it seems that any print statements in those 4 functions causes it to work instead of break
- this kind of makes sense though, if these functions are not *causing* the problem, but instead are making some data be present
  at a certain address which then causes the actual bug to read a different value?
- hmm... a print statement in the copy_uint8_out_of_iter() function that prints the address does *not* make it work.  It's still
  failing (currently, it's not inputting anything).  None of the addresses printed are suspicious, but again, the problem might not
  be caused here.  I would kind of expect that the same print in get_user_uint8 would not cause the problem
- a single simple print in get_user_uint8 does still cause the problem
- hmm... putting more than just the address in the print, making the format string "get_uint8: %x\n" makes the problem go away...
  but that should only change the data segment

- I don't believe it...  And I'm not convinced it's the cause of the problem, but it's certainly suspicious...
- I looked at the objdump of the rodata section, since it seemed like adding printk format data was breaking/fixing the code, and
  there's not much in the rodata section.  It's mostly the serial/tty_68681 channel info structs.  The addresses looked right for
  one channel, but the other channel didn't look right.  It's not aligned to 4 bytes, but to 2 bytes
- the broken version has this:
    ```
    Contents of section .rodata:
     10de3e 00000000 00000000 00000000 00000000  ................
     10de4e 00000000 00000000 00000000 00000000  ................
     10de5e 00700011 00700013 00700013 00700015  .p...p...p...p..
     10de6e 00700017 00700017 02000070 00010070  .p...p.....p...p
     10de7e 00030070 00030070 00050070 00070070  ...p...p...p...p
     10de8e 00070100 52525344 5a                 ....RRSDZ
    ```
- I added a 3 char array to the end of the struct since there's a single byte field at the end which would otherwise need to be
  aligned.  That shifted the addresses for channel B to be long word aligned
    ```
    Contents of section .rodata:
     10de3e 00000000 00000000 00000000 00000000  ................
     10de4e 00000000 00000000 00000000 00000000  ................
     10de5e 00700011 00700013 00700013 00700015  .p...p...p...p..
     10de6e 00700017 00700017 02000000 00700001  .p...p.......p..
     10de7e 00700003 00700003 00700005 00700007  .p...p...p...p..
     10de8e 00700007 01000000 52525344 5a        .p......RRSDZ
    ```
- I only now noticed that it's actually channel B that was correct, and channel A that was misaligned, which makes even more sense,
  although I don't know why the compiler decided to put channel A second when it appears in the correct order in the source file...

- that said, and like I said, I'm not convinced.  Adding a longer format string previously made the problem go away despite this
  code coming before where the format string would appear

- I added the extra format string in there, and the problem seems to be occurring, when it didn't before...
- ok, so I removed the padding, and added in the format string, and then compiled, and it wasn't working... when it was previously
  working like this.  I then did a make clean, and make again, and then that binary worked, problem is not occurring, and the tty
  registers struct is word aligned instead of longword aligned, so I don't think it's related, but also, why did it require a
  full recompile?
- sometimes an incremental compile won't produce the same broken/fixed result
- ok this is so bizzare, I have the code in a broken state, with a print in get_user_uint8 that only prints the address.  I compile
  it, and it doesn't work.  I modify the print to print `get_uint8:` and then the address, and incrementally compile.  It still
  doesn't work.  If I modify tty to have the padding, and incrementally recompile, then it works.  If I removed the padding and
  incrementally recompile, then it still works (because the extra format string is there).  If I remove the extra format string, and
  recompile (without padding), then it doesn't work again.
- changing the format string recompiles a lot of files, but changing the tty doesn't recompile much, and it's the second one that
  tends to cause the difference, even though the first one also includes the same files recompiled in the second one
- but yeah, working vs not-working code has this serial struct aligned different, so it's not the common problem here
- that makes sense too because with the tty-extra-iter-field-vs-not binaries, a working version vs broken version is identical in
  size, but different in offsets only

- ok, so I have the padding in, no printk statement in get_user_uint8, and it's working, and now I've removed the iovec_iter field
  to see if it breaks again, and no, it's still working...  How can I break it again with the padding in the tty struct
- I thought maybe putting that old two instruction code into get_user_uint8 would make it break, so I did that, and it actually is
  using the uintptr_t value still in get_user_uint8, so that two instruction code is printing garbage instead of printing the
  correct output, since user mode printing is going through that function
- I wasn't able to get it to break input again though, even with the tty padding removed
- what gives... I can't reproduce the problem anymore, with and of the old code...
- a full clean and recompile has it broken again

- I've added a new set of binaries to `investigations/tty-read-double/incremental-compile-vs-clean-compile`, where the only
  difference is that the working version and the broken version had the same code, but the working version had a working
  configuration that was compiled, and then modified to the broken configuration and only incrementally compiled, which still
  produced a working binary.  And then I did a clean and full recompile, and then the broken image was produced, which has the
  broken behaviour
- the two binaries are actually wildly different, all throughout them
- the differences start in the _kernel_start code, the entry point, where the addresses to the BSS section are different by 16
  bytes, since they're supposed to be aligned to 16 bytes according to the linker script.  So some kind of data is different?
- the `__bss_end` label and the `boot_args` labels are both at different addresses
- the working version is 16 bytes further than the broken version (0x112418 for the working version 0x112408 for the broken one)
- analyzing the .rodata/.rodata.str1.1/.data sections, dumped by objdump from the elf files, they are nearly identical, but all the
  addresses are shifted by 16, so the code itself is a different size between the two versions, and the only data that's different
  is the addresses at the end of the .data section, where they addresses (some of them) are 16 bytes further
- the addresses are fairly sequential (not as a rule), and the first few are identical, with the first (and earliest) address that's
  different between them being 0x106f5a, which is 0x106f4a in the broken one.  It occurs at data segment address 0x10ebd8.
- so the code size is different after that point, which I'm pretty sure from memory is in the tty code
- so that address 0x106f5a, corresponds to the tty_68681_init function.  The earliest point of identical addresses is <drumroll>
  tty_68681_write.  So the incremental compile, even though it did recompile the tty 68681 code, somehow still ended up with the
  wrong code?  Is this related to the built-in.o file not replacing the original code?
- the working version of tty_68681_write has a `subql #4,%sp` instruction at the start (allocating a local variable), and the broken
  code doesn't have that, and the rest of it is different from there, mostly in small ways related to offsets, but some different
  instructions too
- the broken code in tty.o, built-in.o, and the binary, are all 198 bytes long, and the working code is 212 bytes long
- adding 7 nops, which should increase the size by the same amount, doesn't affect the problem (it's broken whether the nops are at
  the start, end, middle, in a function after, etc)

- changing the write function in the tty_68681_driver struct to NULL makes it jump to the firmware during boot, which is maybe a bug
  that should be fixed, or at least a check that refuses to add a driver with a NULL write?
- replacing the write function in the driver struct with a nop version that just returns the length seems to work (I can't see
  what's written, but it's giving bus errors as if a program is loaded and running when I type commands, when previously without
  that one change, it was reading in no input
- putting the nop as the read function reads nothing, but I guess that's expected...
- I added a putchar, or alternatively a printk to the nop write function, that just prints a ! to show it's being called, and it
  works with that.  I had to move it a bit downward, below the putchar for that version to work, so it's below putchar, above
  tty_68681_process_input(), and it's working, the problem is not occurring
- I then put the old tty_68681_write pointer back into the driver struct, and left the nop function in the code, and it's working
  like that as well, so it's the existance of this function which is somehow fixing it?
- removing it and it breaks again
- no matter where I put this nop function in the file, it ends up being put between tty_68681_write, and tty_68681_set_leds.
- if I remove the printk, it goes back to not working (when it's not added to the driver struct either way)
- I moved it back to the top, and replaced the putchar with printk, and it's back to not working, but at the same time, the function
  is being put between the seek and process_input functions in 68681
- ok, going back a bit, I have the nop function, not being used but compiled into the code, which is between the write and set_leds
  functions, and the input is working.  Then I moved it to the end, and made sure it was at the end of 68681, between
  console_putchar_buffered and devfs_release, and it's still working
- if I try to put it anywhere near the top, it always ends up in the same spot between write and set_leds

- I have the write_nop at the end and it's not working, just turned the computer on
- full recompile and it's working again
- removed the write_nop and incrementally compiled, and it's not working
- readded it and incrementally compiled and it's still not working
- I've saved the two versions of the kernel source tree in investigations/junk/incremental-compile-not-always-producing-same-results
- dumping the built-in.o files seem to show them having the same size for tty_68681_write, and they both have the write_nop
  function, as well as the kernel itself having both
- the two versions of the load scripts are identical... and loading the supposedly broken one now works... so there's something
  strange going on here, as if it is actually a hardware problem, or perhaps a hardware initialization problem?

- I loaded a kernel that doesn't have write_nop in the binary, and the tty input is broken.  I then loaded the supposedly broken
  incremental compiled version (literally the saved one in the investigations dir) and it's still broken.  I then reset and loaded
  the exact same kernel a second time, and it worked the second time
- the rom on this k30 card doesn't have a move or compare command, so I'm not sure if it's identical.  I guess I can use the dump
  command though.  But I wonder if it's running it twice or loading it twice that causes it to get fixed in between
- I dumped the supposedly broken but actually working kernel, then loaded and dumped the truely broken kernel, and then loaded the
  supposedly broken kernel to dump it, but it was working, and now when I try it again, it seems to work
- power cycle and try again
- I'm only dumping the start of the kernel anyways, so I doubt this will do anything.  The monitor wasn't happy dumping 8000 hex
  words, so I'm dumping 0x4000 words instead
- at least for that, the truely broken version is definitely really different; offsets are 0x20 less than the working version, but
  the identical supposedly broken images loaded separately, where one was working and the other wasn't, are identical (at least the
  part that I compared).  I would think that either the stacks are different, or memory elsewhere is different
- well... this is interesting.  It seems like it's always broken if I reset and boot again without reloading.  It's the reloading
  that fixes it...  I need to dump the latter part of the image
- there's definitely some data that's different, right at the end, which I think is where the data section is located...

    ```
    3776c3776
    < 10EBE0: 0101 0010 6F4A 0010 67D0 0010 6812 0010
    ---
    > 10EBE0: 0001 0010 6F4A 0010 67D0 0010 6812 0010
    3826,3828c3826,3828
    < 10EF00: 0010 960C 2781 E494 000E A58E 0000 0048
    < 10EF10: 7C55 81C1 0000 0000 0010 EBE2 0010 EBA4
    < 10EF20: 0010 EB84 0010 EB64 0000 0000 0001 000F
    ---
    > 10EF00: 0010 960C 41C6 7EA6 0008 AE1F 0000 0010
    > 10EF10: 7C55 8189 0000 0000 0010 EBE2 0010 EBA4
    > 10EF20: 0010 EB84 0010 EB64 0000 0000 0000 000F
    4052,4057c4052,4057
    < 10FD20: 0000 0000 0000 FFFF FFFF 0020 5CDC 0020
    < 10FD30: 5500 0012 3200 0000 0004 0000 0004 0000
    < 10FD40: 0004 0000 0000 0000 0003 0000 0004 0000
    < 10FD50: 0004 0000 0000 0000 0000 0000 0000 0000
    < 10FD60: 0000 0000 0007 0000 0006 0020 B980 0000
    < 10FD70: 0000 0020 BA98 0020 BA20 0000 0000 0000
    ---
    > 10FD20: 0000 0000 0000 FFFF FFFF 0020 9DDC 0020
    > 10FD30: 9600 0012 1850 0000 0004 0000 0007 0000
    > 10FD40: 0007 0000 0000 0000 0003 0000 0007 0000
    > 10FD50: 0003 0000 0000 0000 0000 0000 0000 0000
    > 10FD60: 0000 0000 0000 0000 0000 0000 0000 0000
    > 10FD70: 0000 0000 0000 0000 0000 0000 0000 0000
    4060,4061c4060,4061
    < 10FDA0: 0000 0000 0000 0000 0000 0000 0000 0020
    < 10FDB0: 6D0E 0000 0002 0010 0000 0020 7CB4 0000
    ---
    > 10FDA0: 0000 0000 0000 0000 0000 0000 0000 0000
    > 10FDB0: 0000 0000 0000 0000 0000 0000 0000 0000
    4072,4073c4072,4073
    < 10FE60: 0000 0000 0000 0000 0000 0000 0000 0020
    < 10FE70: 6D0E 0000 2000 0010 0000 0020 7CB4 0000
    ---
    > 10FE60: 0000 0000 0000 0000 0000 0000 0000 0000
    > 10FE70: 0000 0000 0000 0000 0000 0000 0000 0000
    4091,4093c4091,4093
    < 10FF90: 0000 0000 0000 0000 0000 0000 0000 0000
    < 10FFA0: 0000 0000 0000 0000 0000 0000 0012 0000
    < 10FFB0: 0000 0000 0012 31A4 0000 0000 0000 0000
    ---
    > 10FF90: 0000 0000 0000 0000 0000 0000 0000 7C55
    > 10FFA0: 8186 0000 0000 0000 0000 0000 0012 0000
    > 10FFB0: 0000 0000 0012 5C88 0000 0000 0000 0000
    ```
- the first diff is just some flags
- 10ef08 is actually the start of the BSS section, so most of these differences are bss data
- then a random seed, some time data, the driver table looks identical, and then the data after the NULL doesn't have a symbol, but
  maybe it's just unused buffer space, so the value isn't important?
- the rest of the changes after that, there's no symbols with 0x10fxxx addresses, even though the bss doesn't end until 0x112424
- I looked through memory for FD\d\d and found one thing that looked like an address.  It is actually the init_proc() function
  that's writing to 0x10FD42 to zero that memory.  It's an entry in the process table (the init loop was unrolled)
- I can't seem to find address-like things in the code anywhere else

- so if there's no data section changes that seems to account for the difference, and if the bss section is properly zero'd out on
  boot, which I should verify, then the only other comparible difference is the stack segment, *possibly* some allocated pages (but
  I kind of doubt it), or it truely is a hardware-related issue such as an initialization issue of the serial port, or crosstalk
  or something
- after a power cycle, loading the supposedly broken kernel, it didn't work.  I didn't try booting again before loading the same
  image over serial, which worked the second time

    ```
    $ diff stack-dump-from-broken.txt stack-dump-from-working.txt
    1777c1777
    < 11EF00: 0020 6200 0011 F190 0011 155C 0000 3143
    ---
    > 11EF00: 0020 5E00 0011 F190 0011 155C 0000 3143
    1792c1792
    < 11EFF0: 0000 000A 0020 BAFC 0010 CFC0 0011 F210
    ---
    > 11EFF0: 0000 000A 0020 B6FC 0010 CFC0 0011 F210
    1794c1794
    < 11F010: 633D 3120 6164 6472 3D32 3042 4146 4320
    ---
    > 11F010: 633D 3120 6164 6472 3D32 3042 3646 4320
    1819,1820c1819,1820
    < 11F1A0: 0000 0000 0000 0000 0000 0000 0020 BAFC
    < 11F1B0: 0020 4E00 0020 BA00 0000 0001 0000 0077
    ---
    > 11F1A0: 0000 0000 0000 0000 0000 0000 0020 B6FC
    > 11F1B0: 0020 4E00 0020 B600 0000 0001 0000 0077
    1822,1825c1822,1825
    < 11F1D0: 0012 3200 0010 5528 0020 3500 0020 BA00
    < 11F1E0: 0000 0100 0000 0001 0011 F1F6 0020 BAFC
    < 11F1F0: 0020 57E8 0010 0020 5700 0800 0011 F27C
    < 11F200: 0010 2E6E 0020 3500 0020 BA00 0020 4E00
    ---
    > 11F1D0: 0012 3200 0010 5528 0020 3500 0020 B600
    > 11F1E0: 0000 0100 0000 0001 0011 F1F6 0020 B6FC
    > 11F1F0: 0020 57D8 0010 0020 5700 0800 0011 F27C
    > 11F200: 0010 2E6E 0020 3500 0020 B600 0020 4E00
    1827,1828c1827,1828
    < 11F220: 0000 0100 0020 BAFC 0010 CFC0 0010 4DE2
    < 11F230: 0012 3200 0020 BAFC 0000 0001 0000 0004
    ---
    > 11F220: 0000 0100 0020 B6FC 0010 CFC0 0010 4DE2
    > 11F230: 0012 3200 0020 B6FC 0000 0001 0000 0004
    1831,1833c1831,1833
    < 11F260: 0000 0000 0011 F31C 0020 BAFC 0011 FBCC
    < 11F270: 0012 3200 0020 BAFC 0011 FFCC 0010 4976
    < 11F280: 0000 0040 0000 0000 0020 BAFC 0000 0001
    ---
    > 11F260: 0000 0000 0011 F31C 0020 B6FC 0011 FBCC
    > 11F270: 0012 3200 0020 B6FC 0011 FFCC 0010 4976
    > 11F280: 0000 0040 0000 0000 0020 B6FC 0000 0001
    1836c1836
    < 11F2B0: 0020 BAE8 0011 F31C 0011 F7C0 0000 0014
    ---
    > 11F2B0: 0020 B6E8 0011 F31C 0011 F7C0 0000 0014
    1839,1841c1839,1841
    < 11F2E0: 0020 4E00 0020 7B08 0020 BAD8 0000 0000
    < 11F2F0: 0010 FA84 0010 2A1A 0010 FA84 0020 BADC
    < 11F300: 0020 7B08 0012 3200 0020 BA00 0011 F31C
    ---
    > 11F2E0: 0020 4E00 0020 7708 0020 B6D8 0000 0000
    > 11F2F0: 0010 FA84 0010 2A1A 0010 FA84 0020 B6DC
    > 11F300: 0020 7708 0012 3200 0020 B600 0011 F31C
    1844c1844
    < 11F330: 0020 BA00 0000 0100 0012 2AA8 0012 1850
    ---
    > 11F330: 0020 B600 0000 0100 0012 2AA8 0012 1850
    1846c1846
    < 11F350: 0010 FA84 0020 BB00 0020 7B08 0011 F7B4
    ---
    > 11F350: 0010 FA84 0020 B700 0020 7708 0011 F7B4
    1848c1848
    < 11F370: 0020 61B0 0000 0000 0020 7B08 0011 16BC
    ---
    > 11F370: 0020 61B0 0000 0000 0020 7708 0011 16BC
    2001c2001
    < 11FD00: 0010 D894 0011 FDB7 0020 7988 0011 FDB7
    ---
    > 11FD00: 0010 D894 0011 FDB7 0020 7588 0011 FDB7
    2006c2006
    < 11FD50: 0020 7988 0000 0000 0011 FF8C 0010 CF74
    ---
    > 11FD50: 0020 7588 0000 0000 0011 FF8C 0010 CF74
    2008c2008
    < 11FD70: 0020 B8FC 0010 CFC0 0011 FF8C 6275 7320
    ---
    > 11FD70: 0020 B4FC 0010 CFC0 0011 FF8C 6275 7320
    2010c2010
    < 11FD90: 6164 6472 3D32 3042 3846 4320 7373 773D
    ---
    > 11FD90: 6164 6472 3D32 3042 3446 4320 7373 773D
    2012c2012
    < 11FDB0: 643D 3320 7063 3D32 3037 3938 380A 0000
    ---
    > 11FDB0: 643D 3320 7063 3D32 3037 3538 380A 0000
    2030,2047c2030,2047
    < 11FED0: 0000 0000 0000 0000 0000 0000 0000 2711
    < 11FEE0: 0000 0001 0000 0002 0020 B9A4 0000 0000
    < 11FEF0: 0000 0000 0011 09A8 0011 09A8 0000 0000
    < 11FF00: 0000 0000 0011 FFC8 0000 0000 0010 FA84
    < 11FF10: 0010 3A10 0011 0514 0010 3B82 0010 FAC8
    < 11FF20: 0020 4DE0 0000 0018 0010 FA84 0000 0001
    < 11FF30: 0011 074C 0010 3BFA 0010 FA84 0000 0001
    < 11FF40: 0000 0001 0010 3864 0020 4DE0 0010 6618
    < 11FF50: 0000 0001 0000 0000 0020 B92C 0020 B924
    < 11FF60: 0020 B9A4 0000 0000 0000 0000 0011 16AC
    < 11FF70: 0020 9500 0020 9640 0000 0000 0010 945A
    < 11FF80: 0000 0000 0000 004A 0011 FF9C 0010 6B7C
    < 11FF90: 0010 6A8E 0011 09A8 0000 0000 0000 0000
    < 11FFA0: 0000 0000 0000 0000 0000 0000 0000 0000
    < 11FFB0: 0000 0000 0000 0000 0000 0000 0000 0000
    < 11FFC0: 0000 0000 0000 FFE1 0010 4976 0000 0040
    < 11FFD0: 0000 0000 0000 0000 0011 09A8 0011 09A8
    < 11FFE0: 2000 0010 471A 0100 0010 DE24 0010 DF84
    ---
    > 11FED0: 0000 0000 0000 0000 0000 2711 0000 0001
    > 11FEE0: 0000 0002 0000 0001 0000 0002 0000 0000
    > 11FEF0: 0011 09A8 0011 09A8 0000 0000 0000 0100
    > 11FF00: 0011 074C 0011 FFC8 0010 4976 0000 0040
    > 11FF10: 0000 004C 0000 004B 0070 0005 0010 6AFA
    > 11FF20: 0000 0000 0000 0000 0000 0000 0000 0000
    > 11FF30: 0000 0001 0000 0002 0010 6BC2 0000 0000
    > 11FF40: 0000 0000 0000 0000 0000 0000 0000 0001
    > 11FF50: 0011 FFA0 0011 FF9F 0000 0000 0000 0100
    > 11FF60: 0000 0000 0010 6062 0000 0000 0000 0000
    > 11FF70: 0011 FFA0 0000 0002 0000 0003 0000 0003
    > 11FF80: 0000 0000 0000 0000 0010 EF38 0000 0000
    > 11FF90: 0000 0000 0011 11DD 0010 3AE2 0000 0000
    > 11FFA0: 0010 68D0 0000 0002 0000 0000 0000 0201
    > 11FFB0: 0000 0002 0000 0000 0000 0101 0000 0001
    > 11FFC0: 0000 0004 0010 EF30 0000 0000 0010 1BE8
    > 11FFD0: 0000 0000 0000 0000 0000 0000 0000 0000
    > 11FFE0: 0000 0000 0010 471E 0010 DE24 0010 DF84
    ```
- the diff is less than I thought it would be, and the majority of the differences are related to specific page addresses, namely
  0x20B600 vs 0x20BA00, and 0x20B700 vs 0x20BB00, but the chunk near the end is probably the most different.  It's offset by a long
  word, and some of the things that are addresses in one are NULL in the other, and such.
- there's a buffer on the stack starting at address 11FD70 which is a bus error message.  It's the last one before printing the
  prompt, so most of the stack is fairly recent, or left over data from previous deep calls.  It's the last part of the stack
  mainly, that part that has all the differences, which is actually where the kernel stack pointer was probably pointing to when the
  reset button was pressed, just before dumping the stack data
- the addresses at the top of the working stack where that last diff block is, has addresses for handle_serial_irq, tty_68681_read,
  tty_process_input, resume_blocked_procs, and main() near the top, among others
- a quickish power cycle, and reloading that same kernel, was not enough to cause the problem.  It worked first time loading
- I'm also not so sure the 0x207B00 vs 0x207700 difference is meaningful, since this last run is working, and it's using 0x207B00 at
  stack address 0x11F370

- I put a scope on the headers for the serial, and I'm only reading one characters.  I haven't connected the logic analyzer to pins
  on the back to inspect the serial controller accesses from the cpu.  It's a pain in the ass to hook up.  It might be a lot of
  board space, but it'd be nice to have some inspection headers.  A backplane hookup won't solve this because it's an onboard
  peripheral
- also, tried to get the X56 programmer (TL866) in wine, but it won't be even start, and it wasn't detected in windows for some
  reason
- after installing the updated guest additions (since I had to remove virtualbox and reinstalling through apt-get), the usb was
  detected, so I can program a new rom now, but I'll test it first
- oh I also changed the cable, not sure if the one it came with just doesn't work.  It doesn't fit smoothly into the socket like my
  other usb-b cables do


2026-02-01:

- it's a hardware problem, isn't it...
- I hooked up the logic analyzer to the serial controller, IOSEL, WR, DTACK, A1, A2, A3, A4, and the "truely broken kernel" is
  working fine... unable to reproduce problem
- I disconnected IOSEL, WR, and DTACK, but it seemed to still be working.  I then disconnected all except one address line, and
  after booting, I was able to get it to not input anything.  Reconnecting the address lines and it still seems to be broken, which
  is good in this case because it's reproducing the problem
- I kind of need IOSEL to distinguish what's the serial port access and what's another address being read, but hooking it up, it
  seems to still be reproducing the problem (I'm sure it's a dtack problem if it's hardware, because it's always a dtack problem)

- *sigh* I don't entirely trust the logic analyzer's data values, but it seems to be returning 0x56 when the status byte is read,
  and it's in that "hang" situation, where no input is received, nothing is printed, but it's actually reading in the status byte
  repeatedly.  But that doesn't make sense because that would mean the receive buffer is full but the ready bit is not set, and the
  code seems to be reading in 0xE1 instead
- that said, the issues is much less consistent with the analyzer probes connected.  It seems to work fine sometimes when it would
  otherwise fail, and resetting and booting again (without loading again) will often get it to break, with the same image


20226-02-02:

- So, it's doing all sorts of weird things, and the program doesn't make sense relative to the logic analyzer, and I'm not sure
  which one is wrong because I don't trust the analyzer's wires
- currently, I'm back to the behaviour where it reads in multiple characters instead of just one, but when I look at the logic
  traces, it just doesn't make sense what's going on.  It seems to be writing to address 0x2 (the command port) with byte 0x80,
  which is not a valid command
- it also seems like the status byte often is 0x02, which doesn't seem valid either, since the FFULL flag (0x02) shouldn't be set
  unless the RxRDY bit (0x01) is set too
- I've removed the short extension card from the system board and moved the flash card and system board to the first two slots, with
  the extension on the k30 in slot 9, so there's plenty of space on the board to probe signals.  It seems to work too, OS booted
- next time I order boards, I should get a full length extension

- I checked the DSACK1 signal as well, and it's DSACK1=H, DSACK0=L.  I was thinking maybe the port size might be wrong, and it's
  making it read in a different part of the 32-bit data bus, but that doesn't seem to be the case.
- the value printed as the status byte is often 0x01 or 0x0D

- after redoing all the wires, and only connecting the data bus wires, untangling them though, it's reading different numbers...
  Some are the printing out of the status byte, which I need in order to corroborate the signals, but I'm not seeing any 0x80s for
  example
- the < and > characters that surround the status byte are 0x3C and 0x3E in ascii

- took a trace with nothing going on.  There's some blips on D29 and D30 when the serial interrupt is running, but otherwise not
  much going on, which leads me to think those are broken

- the accessed to register 0x2 are now 0x04 which is the enable tx command, so that makes sense.  It's followed by a access to 0x3
  which is therefore transmitting a character
- i don't know what is the writing and what is reading in the character.  It seems to act all like writing.  It also seems to not
  service irqs a bunch of times in a row, just exiting and allowing it to re-enter.  Is the buffer full???
- a write to the LEDs regs would be easier to detect than a print statement

- I think I know what's wrong.  I've been wrong in the past about that, so I could be wrong now, but I was looking through a trace
  where I only received a character, technically echoing it, but not printing inside the ISR, but also writing to the leds register
  with a bit indicating the fault.  And it seems like it reads in the status byte, goes to read in a character, but can't because
  the buffer is full!  I thought I had checked that before, but it's hard to see with print statements
- an inline debugger would help here of course, much better than print statements.  I should look into how I can get a debugger
  working on this platform...


2026-02-03:

- the thing that's bugging me about this is that previously, I don't think I was hitting the "failed reading" message, which I
  recently added when it overruns the new loop limit.  It might have been the same problem as when <E1> was the status byte, but I
  can't be sure, and it may just be an artifact of the code thinking there's an infinite number of characters to read.  It still
  comes back to the status byte being wrong
- maybe I should put the status bytes into another buffer which can be printed outside of the ISR to confirm they are what they
  seem?

- it really is returning status 0xE1 continuously, and nothing seems to clear it, when both CMD_RESET_ERROR and reading the byte
  should clear it, but it doesn't.  It's always E1
- I had a probe on the serial line, and it definitely transmits a clear character when pressed, but it's not broadcasting a break
  continuiously.  After the character is sent, it pulls the line high
- the only alternative thing I could try would be put the probes on the serial chip instead of the CPU to see if the value is
  different, and maybe also put the serial probe on the chip as well, to make sure it's seeing the same thing

2026-02-06:

- i was just about to go to bed, and knew i should have left it til tomorrow, but i tried putting a resistor across the dsack0
  signal and it didn't seem to change the fact that nothing was being input. I wasn't sure if it was actually connected across
  the pins of the resistor network, so I tried the resistor across the cpu pins and now it won't boot. I tried swapping the cpu
  and that didn't change anything. Tried a few other things, but couldn't get it working at all. I've given up for today at least,
  but feeling just blank, empty, spent.


2026-02-07:

- mentioned this problem in the discord and TechAV said he had a similar problem, that turned out to be the CPU, CPLD, and SRAM chip
  all getting fried at once.  He said the CPLD verified correctly, but when he erased it, it wouldn't reprogram or verify after
  that.
- I had tried a verify only and that was ok, but I'll try an erase at some point
- I've tried looking at the halt signal, and also the reset.  The halt is 3.3V or so when reset button is not pressed, because of
  the LED, but when it's pressed, it's above 1V still, which is odd.  The reset signal is the same.  Both are fed by different
  inverters in the same 74x05.  It's possible that chip was what got fried, since it shouldn't be like that, although when the CPU
  is out, it seems to get pulled down just fine.  I've been too scared to put a jumper on it though to see if it just needs a bit
  more pulldown power.  When the reset button is pressed, it draws about 250mA more than after.
- The current draw is sorta normal around 0.88 on startup, but 0.75 after a reset, and 0.72 without the CPU.  I definitely think
  it's not resetting correctly
- I suppose it could be the 74x05 and the CPU at the same time
- when I have the analyzer on AS, DS, and A2-A5, there's a blimp when it first powers on.  I think it doesn't change when the reset
  is pressed.  The blip is weird though.  It doesn't really seem like a normal cycle.  Way too long.  If it didn't reset on boot,
  maybe it's just wonky in a normal way?


2026-02-08:

- Coming back to this again today.  I see I actually had the ground pins of the analyzer disconnected previously, so the previous
  readings were invalid, but hooking it up today only makes some of the spikes less frequent.  It's still not working of course
- the AS/DS signals start low, I powered it on while capturing, they went high and then low again (AS/DS/DSACKx/A2-A5 all low in
  sync).  And then a few microseconds later, the A2-A5 signals go high and then low again
- I then pressed reset a 3 times while still capturing, there's a bunch of weird activity on DSACKx which doesn't make sense, and
  AS/DS don't change state at all during that time.  Not positive it's not interference of some kind (I also do have a very long
  ground wire).  There's some random blips on the address lines, and all while AS/DS remain low, and then all signals go high.  I
  think that might have been the moment I pressed reset

- I improved the ground wire, but now I'm getting a bunch of weird signals.  The AS/DS signals are changing a bunch, but not what
  I'd expect relative to each other, and the DSACK signals are hardly changing at all.  The DSACKs are generated by the CPLD
- it has the weird flurry of signals both when turning on, and less so when turning off
- I'm looking at that reset again, and the sysreset from the backplane goes through a different 74x05 gate on the same chip that
  directly pulls down on the CPU reset, and that also can't pull it down below 1.2V or so, same as the others, so definitely
  something wrong there.  I worry if I put a jumper on there, I'll short something

- I think I'll try erasing and reprogramming the CPLD because that will at least confirm if that's working or not
- well that worked fine.  It did draw about 1.25A while erased, which is a bit much, but it programmed it and verified it after
  explicitly erasing it, so I think it's fine.  I can rule that one out (probably)

- back to the reset (not the halt, but reset).  I had the CPU in, and it would be 5V.  When pressing the reset button, it would pull
  down to 1.2V or so
- I removed the CPU and it rides at 5V.  When I press the reset button, it pulls down to 0.  When I put a 1K ohm resistor into the
  reset pin, and connect it to ground, the steady state is about 2.2V, which doesn't quite make sense given it has a 10K pullup,
  which did show as 9.7k on the multimeter, so I'm pretty sure it's ok.  I would have expected the 1k to pull down to about 0.5, as
  a voltage divider with the 10k
- if I put a jumper into the reset pin in the socket, and jump it to ground, it only pulls down to like 2V or so.  It's about the
  same as with the resistor.  It doesn't seem to produce much current, which is also a bit odd.  I tried putting the scope probe
  into the ground pin I was using to jump to, and it's definitely grounded.  I also tried putting the scope pin onto the uart
  header, in case the wire I was using to ground the probe was a problem, and that didn't change anything
- if I put the jumper from the reset pin to the uart ground pin, it jumps around a bunch and causes the status LED to come on, which
  is the weird part.  I wonder if it's shorting somehow
- the STERM pin is pulled high by only a resistor, and has no other gates going into it, so I should be able to jump that to ground,
  right?
- OMG what!?  Ok, so I had STERM with a jumper and scope probe on it, and tried touching the uart ground, which pulled it to ground
  but it was very noisy, same as the reset signal.  If I touch it to a ground in the CPU socket, it only goes down to like 3 or 4V.
  If I touch it to the ground of the nearest bypass cap, it goes solidly to ground!  It's as if the ground pins on the board are not
  actually pulling to ground!!!  Do I have a fried trace on the board?
- I wasn't using the same ground pin either, so it's not like it's just one pin.  I don't see any damage on the board
- I'm getting 11K of resistance between any of the PGA socket pins and other grounds around the board, and 20K of resistance to 5V
- even though there's ground planes everywhere, there's only one trace on the inner layer of the board that connects all the ground
  pins on the socket, which are all the inner-most pins of the socket.  So those pins are all connected to each other, and they're
  all measuring 0.4 ohms between them (which is probably from the multimeter leads, which is to say, they're probably actually near
  0).  The inner and top and bottom layers hold all those ground pins tightly together.  But that ground pour in the middle is
  only connected to ground through one trace that goes on the inner layer to a bypass cap near the socket (C16).  I didn't connect
  more grounds to it because I was worried about making a ground loop, so it's only connected in one place, and that's the spot
  that's reading 11K between C16's ground and any pin in the center ground.  The other layers' pours don't fit between the pins of
  the socket itself

- I soldered a bodge wire in and it's booting fine, loaded the OS, no problems (except for the one I was originally trying to figure
  out).  So it was definitely that trace that fried.  The bodge I put it went from one of the capacitors on the bottom side of the
  board to the capacitor used by the crystal of the serial port controller, which was the closest through-hole trace to the CPU.  It
  goes up through the address pins and over, so it's less likely to interfere with clip on leads when debugging

- so now I'm back to the serial issue, and I put all the leads back on, and it wouldn't boot at all.  I panicked for a brief second,
  but unclipping the dsack0 lead got it working fine again, so definitely something there
- if I clip it on either the CPU or on the 68681, it won't boot.  Which seems a bit weird.  That's different than how it was before.
  It was working sometimes or flakily before when I did the same

- I was once again playing around with a resistor on the DSACK0 signal and now I can't get the OS to boot.  The monitor will run,
  and it can load the whole image in (seemingly) but when it booted, it would lock up when after the 3rd bus error for copy on
  write.  It was possibly accessing the CF card at that point.  The CF card's LED was left on and the status LED was on which I
  think means a VME bus access was in progress.  It was also taking like 1.88A which is way more than usual, but in line with
  accessing the VME bus
- now it won't even boot past the mem0 line, which the thing that comes after that is the ata0/ata1 log lines, and then it mounts
  the minix filesystem.  So something very wrong here
- this was definitely working before dinner and booted fine.  And I don't think the DSACK line connects to the bus at all, so it's
  unlikely that the CF card is broken.  The CPLD might have an issue after re-writing the program to it, but like I say, it was
  working fine before, and I haven't changed the CPLD logic since.  All I did was connect probes, and then a 1k resistor to DSACK0

- Ok, ran it again after being off for a while.  It once again went to 4 bus errors after "begin multitasking", and it's hanging
  here with the OP6 LED lit, and the status LED lit, drawing 1.85A.  The CF card's "disk act" was not lit this time.  It seemed to
  be stuck on a read.  I only caught the upper part of the data bus, and not the address bus, but it had 0xE0 on the bus.  Probably
  not significant
- without reloading the kernel, just booting again, it's now stuck after the mem0 log line, but before the ata0 log line, drawing
  2.03A, status LED on, OP6 off.

- I had taken the probes off, and tried again, and it was still doing this.  I left it off for a few minutes, and turned it back on
  and now it's booted and the serial is working
- I pressed the reset button, and then booted again without loading and it hanged after the slip0 log line, but before the bus error
  or begin multitasking log.  The status LED is lit

- I swapped the CPU again, and then it wouldn't even boot.  I tried yet another CPU and that wouldn't boot either.  Like no serial
  output except the NUL when turing it on and off.  I put the original CPU back in and that wouldn't boot either!  I think I pressed
  it in really hard, and then also put it in a slot without the riser, and it wouldn't start on power up, but then a reset got it
  starting the firmware, and printing the welcome message.  Something's really wrong with this thing.  Maybe there's another fried
  trace somewhere?  Or something got damaged other than the CPU
- ok, that time I tried one of the new CPUS, pushed really hard when inserting, and it started on first power up
- I loaded the OS and it booted first try, serial worked.  Reset button, boot, and it started up again, serial didn't work, but it
  booted fully.  So maybe that other CPU is a bit fried on the DSACK line, and the board is no longer strong enough to overcome it?

- reading the logs for retroverse, searching for "clock", I see a lot of stuff that's similar to what I experienced here.  Issues
  with serial not reading correctly (although no explicit mention of 0xE1), and it hanging when accessing the CF Card.  It could be
  related to the clock speed, and also the DSACK pullups.  When trying to fix the CPU clock speed at 25MHz, I added the 2.2k
  pullups.  I wonder if those are too much for the DSACKs?

- Something to try tomorrow, try changing the CPLD logic to reduce the clock speed and see if that affects the serial working or not


2026-02-09:

- actually it seems to be the riser board that's causing the problems.  It was working fine before, but I tried booting with it
  after yesterday getting it to work without it and a new cpu at the same time.  It did the same thing, where it hung when mounting
  the minixfs partition.  I tried different slots, but it still seems to just not want to boot with it.  There's no visible damage,
  but it was working entirely fine until I shorted that pin.  It could still be other damage on the k30 board that becomes more
  marginal when using the riser, but yeah, I guess I can't use the riser anymore
- I also tested with 1k in parallel for DSACK0 without the riser, and while I did get a bit of garbled text at one point, it seemed
  to work after a reset.  It booted the OS but still had the serial port issue, so I'm not sure a 1K pullup is going to do much for
  that particular problem
- I'm starting to wonder if that messed up clock signal is actually partly responsible, since it's very very dirty, at least at
  50MHzCPLD/25MHzCPU.  I'm very curious now if lowering the clock will make everything work

- Tom Storey mentioned needing to go down to 560 ohms once to get respectable rise times, and TechAV mentioned using 1K ohm pull ups
  on his 68030 board.  He also mentioned that the 68882 FPU drives the DSACK high for one clock cycle instead of letting it float
  high, which might actually be a good idea on the CPLD, because the 68681 seems to have respectable rise times, but the CPLD isn't
  doing that

- I was looking at the clock signal again, and it's really not that great.  The schmitt trigger hex inverters I'm using are can
  switch at between 7 and 13ns transition time, which is 142 to 76MHz.  A 50MHz signal is switching twice at 100MHz per switch,
  which is right in that zone.  The signal on the scope looks rounded as such, even though it's coming out of a couple gates.  I
  need to use a faster version, perhaps an AHC or AHCT, which is a *bit* faster, or go to a PLL like the ICS501, even though it's
  kind of a specialty chip, but maybe I can make the clock itself semi-modular (ie. so a mezzanine board can be attached with an
  alternate clock)
- one thing that was peaked my interest looking at the timing of the datasheet was that it's dependent on voltage, so higher
  voltages make it go faster.  I did turn the voltage up and down when testing for crosstalk, so it might be a bit lower now than it
  was before, which might affect the quality of the clock?

- I was going to try dividing the cpu clock by more, down to 12.5MHz to see if that changes things, but I can't get the virtual
  machine with ATMISP working, so even though I've got it compiling again (after needing to clean before building), I can't load it
  into the CPLD, even though I had this working a couple days ago.  No idea what could be wrong, but I also don't want to reboot my
  computer because I have a lot of stuff open


2026-02-10:

- I loaded the same previously build image of the bus logic into the CPLD, and loaded the OS, and everything worked as expected.
  Then I changed the CPU clock to 12.5 MHz, rebuilt it, programmed it into the CPLD, and loaded the OS, and it had a fatal error
  when accessing the CF Card (not the first time it was accessed, but just before begin multitasking, which I think is the first
  time a bus error occurs that would trigger a page copy
- it seems to be the F line emulator (ie. it tried to execute an instruction opcode that started with 0xFxxx), at address 0x105A84
  but there's no F opcode there of course.  It's the same image that was working before.  It must have read in the wrong value due
  to a clock trip-up.  I wonder if the logic generated is different due to tools changing since I last built it
- the logic is definitely different between the new build with 25MHz clock, and the old one
- Aha!  Changed back to 25MHz, rebuild the logic and loaded it with ATMISP, and it's also breaking in nearly the same way, but a
  couple log lines earlier, so it's something to do with changes somewhere.  I'm assuming it's yosys because the fitter is the same,
  and the atf15xx_yosys toolchain only had a couple minor tweaks different with it, but yosys was probably updated with an apt-get
  at some point
- I'm glad I saved the old logic jed

- TechAV once again had a great thought.  I explained the issue and they asked if the bus pulls high, which the VME backplane does.
  That might be the source of the F opcode, if the wrong address was put on the bus, or timing issues meant there was nothing on the
  bus when it was read.  Although that said, I would have expected it to not be reading code from the VMEBus, only data, so maybe
  that's not relevant... hmmm...
- I loaded the monitor with those special `a` and `v` commands.  The `v` command runs successfully.  It accesses 0xFF80_0020, and
  prints `5` as the output, which I think is reading a register on the CF card.  But `a` causes the same 0x2C exception (exception
  11, which is F-line)


2026-02-11:

- this is so bizzare!  I guess the `a` command wasn't working properly before
- Oh ok, I guess the analyzer hooked up to the vmebus is causing interference with the normal bus cycles.  It was flickering the
  status LED and taking a long time to fetch the full data, and when I tried booting the OS, it would fail during boot, but
  disconnecting the 60mm VME riser which had the analyzer pins connected to it seems to have everything working as expected again
- so I downloaded yosys, 0.33, 0.47, and 0.55, and used the Dockerfile inside it to build an image.  I then used another Dockerfile
  to make an image based on them with wine installed inside of them, and then started a container with:
    `docker run -it -v /media/work/retroverse:/retroverse yosys-wine bash`
- building an image with 0.33 seems to work fine, but I suspect 0.47 doesn't work, and 0.55  was the one that's broken

- backing up a bit, I found that I had the old build files (edif, fit, jed) for the previously working build of the bus logic, which
  had a note that it used yosys 0.33 to build.  That's the one that was working from June 2025.  But the latest version I have
  installed is 0.55, and even though the verilog, hoglet67/atf15xx_yosys, and the fitter are all the same, the yosys version must be
  causing the difference here
- I put the analyzer on it and took a trace, and when it tries to access the VME bus, it can do it a number of times, but at some
  point gets tripped up and the CPU's AS goes low, the VBR0 and VBGIN0 of the VME bus seem to work as expected, but VAS doesn't go
  low for like 8ms.  It's just sitting there.  Eventually it goes low, and a cycle completes, and then the next cycle does the same
  thing.  There's one point where the VAS signal flickers on and off a bunch of times before the cycle stays on and the VDTACK
  signal asserts and the cycle completes.  It's definitely something with the logic
- It might be the case that something is marginal how I have the logic, and the slight difference in build output from yosys, with
  some optimizations, is causing a race condition or something.  Maybe a missing synchro or the need to make something clocked
  instead of combinatorial.  Clocking the logic more might fix it but also make it slower

- interestingly enough, with 0.47, it boots but then fails after the 5th bus error (or 4th log after begin multitasking), with an
  F-line exception, which is the same spot where I was getting a failure when the CPU wasn't booting due to the 60mm riser... so the
  logic error might have been present in the old 0.33 version, but some optimizations means the race condition that occurs is less
  likely to occur on the older version compared to the newer one

- getting back to the original problem for a sec, since I have the 0.33 version of yosys running, I tried changing the clock speed
  for the CPU down to 12.5 (divide by 4 instead of divide by 2) and then tried running the kernel.  I can't seem to get the serial
  problem to occur.  I tried booting the same image a couple of times.  This is a kernel build which previously was always going
  into the 0xE1 problem on boot, usually first thing.  So a slower clock definitely seems to fix it.  That seems to imply it is
  actually a hardware problem, and not a software problem (or a logic problem), which makes sense given I couldn't find anything
  software-wise that could be causing this behaviour from the 68681.  But besides a possible logic race condition, I'm not sure if
  there's anything else that could be responsible such as the poor clock signal or crosstalk

- man, either having the riser in an empty slot with no board in it, or having any logic analyzer channels connected to the VME
  backplane seem to prevent the OS from loading and booting correctly.  It doesn't seem to matter which pins.  I thought it was
  working before, so I do kind of wonder if there's some damage, either on the riser, or on the k30 board.  It will be interesting
  to see if the U3Pro32 I ordered has the same thing, given they have grounded fly leads (assuming I can ground them well enough)


2026-02-12:

- well... of course it seems to work now.  All of that effort to try different versions of yosys, and I guess all I needed to do was
  press the system reset button instead of the CPU reset.  It boots the OS fine after that with no errors.  I guess it was the CF
  card that was in a weird state, and when the CPU card tried to read from it, it would lock up.

- So where that leaves me is: the serial problem I originally had happens reliably at 25MHz CPU speed.  It seems to go away entirely
  at 12.5MHz

- I tried adding some logic to dsack to drive it high, but it wasn't working at all.  But from looking at the analyzer traces, I
  think it might be better to prevent a chip select going low until the CPLD is ready for the next one, so that another CPU cycle
  doesn't start too early.  That might be causing invalid numbers on the bus as I'm seeing with this serial problem

- I tried adding a condition to not start the next cycle's addressing until the dsack is gone, but now it doesn't work reliably,
  especially VME accesses.  It's very fragile, the bus logic


2026-02-13:

- thorpej mentioned using CY62158 memory chips, which are 1M x 8bit.  I thought they were cheaper than the Alliance ones, but I just
  checked and that was for the 3.3V chips, which are only about $5.  The 5V chips are ~$10.  They have multiple memory chips, with
  the others being AS7C4098A and IS61C3216.  I think those latter ones are twice the speed of the 8Mbit ones, but I'm not sure I
  need the speed, at least at the moment.  That said, 50MHz is 20ns, so maybe if I wanted to push the CPU up to that speed, I'd need
  to go for faster SRAM


2026-02-14:

- I was trying to get a better shot on the scope of when the clock seems to drop out, but I didn't manage to catch anything that
  looked like it.  The scope shots look a lot better when I'm using a short ground, so maybe clock distortion isn't as big of a
  deal.  It at least looks square, even though there's almost a volt of overshoot on the clock low part of the signal, which could
  possibly be causing false ticks, even though it doesn't seem too likely.
- I think it would be a bit too difficult to rework a series resistor in there, and while it should help a bit with ringing, I'm not
  sure the risk is worth it.  I'll maybe see once I get the iron out though
- I'm thinking of possibly replacing that DSACK pullup resistor pack to 1K instead of 2.2K, since the only reference I have to this
  problem occurring previously mentions going from 10K to 2.2K as the fix, but I don't have much else to go on here.  The traces I'm
  getting seem perfectly fine
- It's possible there's some kind of timing issue, given reducing the CPU clock fixes the problem, but I'm not sure if that's
  because the div by 4 means there's longer for signals to stabilize, or because the combo logic has more time to propogate, or
  because there's some unsynchro'd signals still
- I seem to have had a yosys 0.55 logic build on the board and it wasn't booting the OS, and so went back to a 0.37 build which is
  not quite identical to the 0.33 build, but seems to work fine as well.  I had thought the problem with 0.55 went away when I fully
  power cycled the system, but maybe I was confused and had a docker container open for 0.37 that I had built the logic for thinking
  it was using 0.55
- also, tried the riser in slot next to CF card again (sysboard in slot 1, CF in slot 2, riser in slot 3, CPU in slot 9), and it
  won't boot with it inserted still (failed to create init task -1, stopping)
- I seem to have a lot of "return -1" in the code, so not sure where that exactly came from, although there's hardly any in the proc
  or mem code besides init functions, and a bunch in the file system code, so I would guess it came from the file system code, and
  maybe is some bad data loaded from the CF card?  It's not giving error -14 which it was doing when failing to read from the CF
  card earlier, when I had the logic wrong, so it's probably getting data back, but it might well be the wrong data as sometimes
  happened when running the `a` command from the monitor

- tried putting an unconnected jumper onto pins 4 and 12 of row B of one of the slots, and it hung when accessing the ATA card.
  Same thing when I just had pin 4 connected.  Oh actually, maybe that's preventing the BGIN signal from propogating, with the
  special self-switching DIN41612 connectors I'm using here...
- also I forgot to sysreset after the first error
- it boots fine when it's in after the CPU card, so yeah that was the problem there
- put the unconnected wires into DTACK and AS on the backplane in slot 4 and no problem booting
- riser in slot 11 works fine
- it almost booted with the riser on the CPU card.  It got to just after the ntpdate timeout, but now it won't even get past the ATA
  stuff at the start, even after a full power cycle and/or system reset

- one thing I was looking at last night was possibly serial controller timing issues.  The 68681 has some requirements for how long
  the chip select line is held for, but it notes that it's not required for proper functioning, but instead for assuring that DTACK
  will be asserted.  If it's not held for at least that time, then DTACK might not be asserted.  I'm pretty sure that's not my
  problem here according to the traces I've seen
- I can't seem to get a lead connected to the DSACK0 line of the card to the new U3Pro32 analyzer I have without it refusing to
  boot, so I can't correlate the DSACK to the other signals.  This was even with the fly lead's ground connected
- but I did try looking at the IOSEL and RAMSEL signals, and there's no discrepency between on states between them.  A possible
  cause of the behaviour I'm seeing is that another device is selected at the same time, and the data is getting corrupted, but that
  doesn't seem to be the case, at least not with the RAM

- I have the scope on DSACK0 again, and I noticed there was a weird glitch where the DSACK held above 1.8V for an extended period
  after it should have dropped, but not sure if that was just a slight bump as I was capturing the first touch rather than holding
  it.  I didn't have any hands free to press the button
- the rise time of the DSACK I'm seeing is often 60 to 100ns, which might be too slow
- I have nothing else to go on than just this hunch that it's related to the DSACK.  I think I'll try replacing the pull ups just to
  see.  I hate when it's easier to modify the board than figure out exactly what's wrong, and see a truely glitchy thing happen that
  I could possibly trace to the logic

- I feel like I should try debugging the logic a bit more before soldering the board, so I rebuilt the logic with 0.49, which
  produces the problem.  Building an image for 0.43 which I can try after, and 0.37 did seem to work
- well, this is very interesting.  I have probes on AS, ROMSEL, RAMSEL, and IOSEL, and I don't believe it should be accessing ROMSEL
  at all because I'm running the monitor from RAM, and then accessing the ATA device, but when the `a` command is running, it does
  actually briefly assert ROMSEL while AS is high.  Sometimes that overlaps with RAMSEL, but often it's the moment that RAMSEL goes
  high, ROMSEL goes low for a clock cycle or so
- yosys 0.43 seems to produce a binary that has ATA 14 error when accessing the CF card
- built an image with 0.37 and it definitely doesn't do that thing with ROMSEL
- the address decode is the only place where romsel is set (in an assign), and it's conditional on cpu_as, but looking at the top
  level module, it's actually getting cpu_as_fifo[0], so the synchronized AS.  cpu_as goes high before cpu_as_fifo, but doesn't go
  low until after cpu_as_fifo goes high.  Should all the address decodes be conditional on both being low?
- that did seem to fix it... it still has one access to ROMSEL, but RAMSEL is not low during that AS cycle, so it might be a legit
  ROM access?  Not sure what it could be doing though
- definitely won't boot the kernel though, bus error at address=2F0A2F11, pc=103476.  Odd, that's during memory_map_print_segments.
  The first fault is addr=1F7C0C0D ssw=715 mmusr=0 pid=2440 pc=105A14, which is ata_read_sector, so that makes sense
- it should actually be accessing ff800004, and the dump of the code matches the disassembly, so it's not a mistaken in transfering
  the image over serial
- according to the SSW, it's not an instruction fault, but a data fault, during a write, for what I think is a byte operation, in
  address space 5 (which is probably supervisor data).  It's weird because it should be writing the sector count to the ata register

- I'm going to try the same mod in yosys 0.37 and see if it does the same thing
- it does indeed do the same thing, how weird
- 0.33 is causing an ata error 14 now, with the extra `cpu_as_fifo[0] | cpu_as`

- I think it might have to do with the fact that only the request_* signals are inputs to the other modules, rather than each of
  them selectively including the cpu_as input
- what I have should be the same logically as I had before, but with an extra input into the address decode module, so not sure why
  it still doesn't work...


2026-02-15:

- adding cpu_as to the request_rom signal worked, although it did leave the "in-use" led lit on the CF card after reading.  It
  didn't seem to have issues reading the ATA though
- adding cpu_as to the request_ram signal did not work, causing it to not detect the ATA.  Looking at the code, there's a clocked
  process for controlling the ram DS signals, so that might be why

- I finally just replaced the pullups with 1k resistors, and it didn't fix the problem.  It's still hanging when typing characters
  in, and still has the same error leds, which is to have OP5 lit, but not 4 or 6.  That corresponds to "failed to read" where it
  loops 100 times trying to get the latest character, and not buffer full, since it will not put invalid characters into the buffer
  anymore
- ok, so it's not a pullup issue.  Is it worth it to install the old pullups?  Maybe with these ones it won't be so sensitive to the
  analyzer probes?
- well, I tried putting the oscilloscope probe on it, and then the input started working, and now I've power cycled it and tried
  again, and it still seems to be working.  I can't seem to make it fail again.  The probe is not on
- looking at the timings though, the DSACK0 peaks are much higher, getting almost to 5V before the next cycle starts.  It maybe
  takes 20ns or less to get above 2.2V instead of taking 60ns or so with the 2.2k pullups


2026-02-17:

- tried sending data on channel B and that didn't seem to cause the debug LED to light up, so it must only be affecting channel A?

- I tried setting the align flags, `-falign-functions=4 -falign-labels=4 -falign-loops=4 -falign-jumps=4 -malign-int -mstrict-align`
  It failed to boot initially, with a bus error where the address waas displayed wrong, so I suspected the exception frame struct
  among others might need the `packed` attribute.  It turned out to *only* need that one, and then it booted fine, but still had the
  problem

- I tried swapping the serial port controller, MC68681, after Tom suggested it.  I didn't think it would do anything, but the broken
  image I was using worked fine and I couldn't restart it to make it break.  I tried other images I had saved, and two of the images
  still do the double character thing, but two or three of them work, and both the with the field and without seem to work


2026-02-18:

- I tried swapping the serial controller back and it definitely did the 0xE1 thing, and putting the working chip in and it worked
  fine
- I then cleaned up and committed what I had, and then unstashed my previous changes, which were the minimum changes that broke it
  earlier, and I was getting the double character thing again, so that explains why some images were still doing that.  It's a
  separate problem
- I cleaned up and stashed the changes again, and it seems to work reliably without the changes, breaks with then
- I tried undoing the tty_68681 changes only, and it was still giving double characters/command not found
- Undoing everything pretty much (it was already mostly commented out), while only having the two extra fields in iovec, and with
  the int nsegs (duplicate) removed from the iovec union in inovec_iter, and that still gave double characters
- then I think I removed one of the two extra fields in iovec_iter, and it worked fine
- So then I readded the extra field causing the trouble, and added the machine alignment args to gcc, and that also worked, albeit
  with the permissions flags wrong due to alginment issues previously caused by adding those flags.  So either this particular issue
  is an alignment issue, or it's hidden by the alignment change?
- alignment flags with one or two fields in the iovec struct works, so at least those changes that would sometimes cancel eachother
  out seem to not being cancelling, with the machine alignment args (-malign-int -malign-strict

- I swapped the CPU back in, the original one, and it seems to be working, so it might have only been the serial controller that was
  broken, and the other CPU that wasn't working as well, was just a bit more marginal on DSACK or something, such that the broken
  serial controller was less functional?


2026-02-19:

- it's specifically the -malign-int that fixes the problem, not the -malign-strict
- it turns out there's a lot of differences between these two, one with align-int, and the other without.  Most of the addresses
  seem aligned before and after, but they're like 0x20 to 0x30 bytes different.  The working version has the lower addresses, as if
  it packed it better or moved some data around between them, which is odd.  I would have thought alignment would add padding that
  would make the aligned version use more memory
- I didn't see a difference between the .rodata sections where the tty addresses are.  I have a feeling the alignment difference
  that makes or breaks this is actually on the stack (just a hunch)
- the broken version has an extra addl/subl in tty_write when calculating the address of rdev in struct device?  That seems weird.
  It's possible the problem is actually in tty and not tty68681
- I'm not seeing any addresses that are really misaligned, and the structs in tty and tty68681 seem to not have an odd number of
  bytes across their fields

- I found some misaligned data in the .data section.  May or may not be related since a lot of stuff is changed between these
  builds, but yeah, 10ecb8 vs 10ec88, which is `proc_files`
- it seems like adding __attribute__ ((aligned (4))) to struct tty_device will fix the problem, but adding it to the tty_68681
  structs won't change anything
- ok so this is so far pretty definitively a problem, but I still don't know if it's actually here, but elsewhere and affected by
  here.  The struct tty_device has two fields at the top, one of which is a device number, and one which is a pid number.  I guess
  when I had originally made this, the pids were 16-bits, but I think I changed that at some point to be 32-bits, and so now the
  everything in that struct is off by 2.  Adding a padding byte instead of the alignment tag also fixes the problem
- adding the padding to the end of struct tty_device also seems to fix the problem, but adding it to struct serial_channel in
  tty_68681 doesn't fix the problem
- according to the symbols table, there's virtually nothing in memory that comes after the tty_device's `devices` list
- there's another devices struct that I was getting confused by, in slip, which also is misaligned, but adding padding there doesn't
  change anything.  It only seems to be the tty struct

- actually, padding at the end kind of makes sense too because it's an array of 2 elements, so the second element will be misaligned
  if the struct is not the right size, which might be where the effect is coming from?
- trying 3 elements doesn't fix the problem, and changing the buffer size rather than adding padding fixes it, but I don't think
  either of those things tells me much
- what about putting the odd one out `rdev` field to the bottom instead of the top?  that doesn't fix the problem?  That doesn't
  really make sense to me
- changing the tty devices to 1 instead of 2 fixes the problem, without padding... that also doesn't make much sense
- iiinteresting... I changed the #define size of the array of tty_device to 1 so that it would only process one tty in the loops,
  like tty_process_input, but still kept the array size at 2, so that it had the same size in memory, and that's working!  which
  means the problem is not with the struct in the data segment.  It's with calling tty_process_input on the second array that's
  somehow messing it up?
- only having the process_input loop do one iteration (without any other changes) makes it work...
- changing the rdev to 0 in dev_read in the tty_process_input function makes it segfault with a weird address, 60700000 which seems
  weird, but also maybe related?
- maybe not, I tried adding the padding that should fix things, and it's still got a segfault on 607E0000 (slightly different) so
  this might not be related
- I modified things enough to put the tty_68681_getchar_buffered function call directly into the tty_process_input function instead
  of going through tty_68681_read, which involves the iovec_iter, and that worked fine!!! so it's likely something related to the
  iovec_iter that's a problem?  Maybe that inline assembly instruction actually was the problem all along, but it's an alignment
  issue due to the instruction used?  Well no, because it was using memcpy_into_iter instead of copy_uint8_into_iter, and both are
  broken


2026-02-20:

- Ok, I've found another way of making the bug happen while using all the strict flags in gcc, which had previously fixed the
  problem.  I had narrowed things down to adding an attribute onto struct tty_device, but that was while using the iovec_iter
  functions to transfer the character.
- Replacing the call to dev_read->tty_68681_read, which expects an iovec, I made a duplicate function called tty_68681_getchar,
  which did the same thing but took an old style buffer, and if I replace the iter_init+dev_read in tty_read_input with the getchar,
  it has the bug all the time, no matter whether the alignment attribute is on struct tty_device, or all the alignment args.  It
  seems to always have double characters or delayed reading of a character
- the problem is indirect, obviously.  Changing the function that tty_read_input calls is what has an affect on whether the bug
  occurs or not.


2026-02-21:

- I'm playing around with various versions that work and don't work, trying to compare differences, and honestly it doesn't make a
  heck of a lot of sense
- in 2026-02-21-iter-vs-getchar I have two broken and two working versions:
    broken -> with tty_68681_getchar instead of using the iovec iter through dev_read
    working -> using dev_read, and with `__attribute__ ((aligned (4)))` on struct tty_device
    alsobroken -> using dev_read, without alignment
    alsoworking -> using tty_68681_getchar, but with the loop removed, and returning the char as int instead

- I still can't conclusively prove that more or less code is changing things, since some of the addresses are less than, and some
  are more than, without any correlation to working vs not working
- going through old images to find the ones that are still broken after changing the serial controller:
    - 2026-01-25-tty-extra-iter-field-vs-not is still broken
    - 2026-01-25-tty-char-volatile-or-not is still broken
    - 2026-01-28-gcc-15-vs-gcc-14 is still broken
    - 2026-01-31-incremental-compile-vs-clean-compile is working
    - 2026-02-19-align-int-vs-not is still broken
- so it seems like they're all still broken except for that incremental compile one.  I thought more of these were working again the
  last time I tested them, which is a bit unnerving if that's not the case.  Either I didn't test them properly and had mistaken a
  broken one for working, or there's some inconsistent behaviour of some kind

- I tried changing tiny things about the tty_process_input and tty_68681_getchar functions to get it from broken to working, and I
  had changed everything to make it like it was when working, removing the loop from getchar, removing the size arg from the
  function, returning the value as the int returned, even added the `ch = error;` line after the function in tty_process_input, and
  none of those things worked.  It wasn't using that last `&ch` argument at all, but just having it there was causing it to always
  break.  Removing that arg made it start working
- the arg was actually still present in the def, but the decl and call had it removed.  Removing it from the def and adding it back
  to the decl also causes the problem.  So it seems to be having that arg in the function call that breaks it
- changing the arg passed in (the one that isn't used) to be "test" or NULL both work.  The problem doesn't occur.  It's so far only
  occurring with `&ch` passed in
- changing it to `(char *) &error` also fixed it, so it's only that char???
- I changed `char ch` to be `int ch`.  I had to also cast it when passing it to write.  It works but prints NULL instead of the char
  when echoing, which is to be expected given I'm not properly adjusting the address or casting
- I have the broken code back in place, with `char ch` and `&ch` passed into the read function, but I have dev_write() call
  commented out and replaced with printk().  It's not printing the character properly either, but the commands are running as if
  it's reading it in correctly... So it's something to do with the interaction of the echo dev_write() call and passing ch into
  getchar?
- I switched back to using the iter for reading the input char, but with the echoing dev_write call replaced with printk, and it
  works fine, so it's somehow the interaction between those two calls?
- I made it set ch to '!' before echoing it, and I'm not sure if it worked or not because the check for '\n' happens right after
  that call, so it never detected the newline
- changing it to use a completely different char variable for echoing, and making it const, the problem is still happening... that's
  very odd since it's a different memory location being manipulated
- in tty_68681_write, I replaced the copy_uint8_out_of_iter() call with memcpy_out_of_iter() and it's now working
- the really weird thing is that despite changing that function fixing things now, it was previously breaking/getting fixed by
  removing the &ch argument from getchar.  Why would not passing in an extra argument cause it to work if the problem was in writing
  the character later on...
- I replaced the copy_uint8_out_of_iter() call in tty_68681_write with a direct kernel buffer print rather than the switch statement
  that can copy out of user buffers, and it's printing garbage when printing the command output, but otherwise seems to work
- I've copied the switch statement from copy_uint8_out_of_iter into the write function, and it's not printing anything except bus
  errors now, but it seems like it's running commands given the bus errors
- I have no idea why this isn't printing anything out...
- I changed the default switch to print a message, and it's always hitting that, so it's not actually switching properly? wtf
- THIS MAKES NO FUCKING SENSE
- the consts are definitely 1 and 2, and the iter->type is definitely 1 or 2 depending on the call, but it's always running the
  default branch of the switch
- ho lee shiit.  I think I see the problem, and I'm... kind of embarassed?  I don't think this problem applies to the original
  function.  Only to me erroneously copying it into tty_68681_write.  I forgot the breaks given it's not returning.  The real
  copy_uint8_out_of_iter() uses returns instead

- alright, so for real now, replaced copy_uint8_out_of_iter() with the same code but directly inserted into tty_68681_write, and it
  seems to be working correctly...
- these two versions are nearly identical, but there are some small differences:
    - the broken version pushes one more register onto the stack when it enters the function, %a5, and because of that, the offset
      to fetch something on the stack is a bit different
        Broken
        ```
        106c92:	48e7 383c      	moveml %d2-%d4/%a2-%a5,%sp@-
        106c96:	246f 0028      	moveal %sp@(40),%a2
        106c9a:	4280           	clrl %d0
        106c9c:	102f 0023      	moveb %sp@(35),%d0
        ```
        Working
        ```
        106c92:	48e7 3838      	moveml %d2-%d4/%a2-%a4,%sp@-
        106c96:	246f 0024      	moveal %sp@(36),%a2
        106c9a:	4280           	clrl %d0
        106c9c:	102f 001f      	moveb %sp@(31),%d0
        ```

    - there's a jump to a different location than expected
        Broken
        ```
        106d02:	508f           	addql #8,%sp
        106d04:	50c0           	st %d0
        106d06:	601a           	bras 106d22 <tty_68681_write+0x90>
        ```
        Working
        ```
        106d02:	508f           	addql #8,%sp
        106d04:	70ff           	moveq #-1,%d0
        106d06:	60d8           	bras 106ce0 <tty_68681_write+0x4e>
        ```

- From the broken image, I went in and searched for `508F50C0601A` and switched it to `508F50C060D8` to change the location of that
  branch, and interestingly enough, the code works the same, no catastrophic failure, but it's still doing the double character
  thing, so that's not causing the problem, it would seem

- the diff of the two versions is just this.  I think this is the smallest diff I've found that breaks or doesn't, and it seems the
  only thing that isn't that jump instruction is the extra reg on the stack?
      2c2
      < kernel.broken.elf:     file format elf32-m68k
      ---
      > kernel.working.elf:     file format elf32-m68k
      9213,9214c9213,9214
      <       48e7 383c       moveml %d2-%d4/%a2-%a5,%sp@-
      <       246f 0028       moveal %sp@(40),%a2
      ---
      >       48e7 3838       moveml %d2-%d4/%a2-%a4,%sp@-
      >       246f 0024       moveal %sp@(36),%a2
      9216c9216
      <       102f 0023       moveb %sp@(35),%d0
      ---
      >       102f 001f       moveb %sp@(31),%d0
      9237c9237
      <       4cdf 3c1c       moveml %sp@+,%d2-%d4/%a2-%a5
      ---
      >       4cdf 1c1c       moveml %sp@+,%d2-%d4/%a2-%a4
      9250,9251c9250,9251
      <       50c0            st %d0
      <       601a            bras <tty_68681_write+0x90>
      ---
      >       70ff            moveq #-1,%d0
      >       60d8            bras <tty_68681_write+0x4e>

- I'm pretty sure the `st` and `moveq` instructions are identical, but I'll test that next.  Modified load script with both the
  alternate jump, and the moveq inserted, and the problem is still happening, otherwise seems the same
- I carefully modified it further to remove %a5 from the movem instruction, and insert %d0 instead.  The diff shows no %a5 being
  used, so I believe that's a compiler mistake that doesn't clean up after some optimization, so it shouldn't make a difference that
  it's not saved, but there needs to be something on the stack to make it get the right arguments without changing the two
  instructions with stack-relative offsets
- I'm still getting double chars on that one, so is the stack offset the thing causing this?
- what if I add a value onto the stack, and adjust the offsets?  That seemed to not print anything now?  But otherwise it seemed to
  work in that I'm getting bus errors when commands run
- ok, undid the offset and movem changes, and then went back and adjusted them to match the working binary.  That seems to work
- both versions seem to not use %a4 and %a5!?  That's very odd that the code generated is so wrong, but what if I try removing %a4
  as well as %a5, since removing %a5 got it to go from breaking to working
- it still works, where it doesn't save %a4 or %a5 and has those adjusted offsets.  Since the regs aren't used, I have a feeling it
  might be the offsets that are causing it somehow, or the extra value on the stack, since removing 1 or 2 values from the stack
  made it start working
- changing it to save %a5 instead of %a4, but otherwise keep the offsets that worked, and it still works

- Aha!  I thought, if the problem is an extra thing on the stack, then using the working code, but adding something that will add an
  extra value onto the stack should make it break.  I used the version of tty_68681_write which has the contents of copy_uint8
  pasted into it, and confirmed it was working, and then added a `volatile int test = 0;` to the top, and now it's broken.
- looking at the diff of the dumps is really weird.  The version that's breaking now has a lot of changes different, just with that
  added `int`.  It changed the movem to no longer save %a4 on the stack in the movem, but the offsets for getting the arguments are
  still larger by 4, like the broken version that uses `copy_uint8_out_of_iter`, so it's definitely using an extra stack value, but
  lots of other instructions are different too
- I tried printing the stack in that function, and it causes it to break when it should otherwise work just having the extra print
  in there.  I'm still not sure if it's getting the right value, but it seems to either print 0x11FF58, or 0x11FF54, depending,
  which is where the kernel stack should be, and seems a bit weird that it would be different sometimes, but also doesn't seem
  anywhere near where there should be other used data (no overflow)
- the same print code moved to tty_process_input is printing 0xFF88 which doesn't make sense, so I'm not sure my assembly is working
- I guess it was just a matter of adding the size to the instruction, so I guess it was sometimes interpreting it as a word
  operation?
- anyways, it's print 0x11FF88 which at first I thought was wrong, because it's a bigger number, but I forgot that that's expected.
  0x11FF54 is higher on the stack
- the problem is not happening with the print in the parent function, and no weird stuff in that 68681 function, but that at least
  tells me that the problem might be around that area
- there's no symbols around that area, but that doesn't mean there couldn't be a calculated offset that ends up in that area
- I tried putting some values onto the stack just before begin multitasking to see if there was a high water mark on the stack, but
  it seems to be using most of the stack between then and the input, so I don't think this is related to just overflowing the stack
  into something else

- it seems that the printk of the stack before the call to echo the char with dev_write makes it work, but moving the printk to be
  at the top, outside the loop in tty_process_input, it breaks again
- I tried disabling the networking, and it's still breaking under the same conditions, so I'm going to assume it's unrelated.  It
  will make it faster to load and test to have it disabled
- hmm... if I put the stack printing before the ISIG processing block, it breaks, but if I put it after that if statement, it works,
  so is it related to that

- if that block is in, it has the problem. If it's commented out, it works
- replacing the accesses to VINTR and VSTOP with numbers didn't make it work.  Removing everything except the outer if also didn't
  fix it
- removing the ISIG from `tty->tio.c_lflag & ISIG` seemed to fix it
- but adding the other if statements back after that seemed to break it again, so it's not just that...
- I restored most of that block piece by piece until it broke again, and it wasn't until putting `tty->tio.c_flag` in (without the
  ISIG) to get it breaking again
- changing the struct termios struct to use int instead of short for the flags fixed it.  Also adding alignment to that struct fixed
  it
- mother. fucker.  The difference between having a ref to c_cc vs c_lflag is that an extra thing is put on the stack in
  tty_process_input.  Also adding the `volatile int test = 0` line to the top breaks it.  Even commenting out the whole block, which
  worked previously, but adding the unused var and it still breaks!
- with the added var, the printing of the stack doesn't fix anything
- adding 4, 3, 2, or 0 volatile vars will fix the problem, but 1 of them will make it double char

- adding 1 only to the bottom half runner in bh.c, which calls tty_process_input, causes the problem.  Adding 2 of them fixes it
  again, just like if that variable is in tty_process_input, so it's definitely a stack issue.  It's like its overwriting the stack,
  or otherwise saving/restoring the kernel stack incorrectly.  That or it's related to the MMU code reusing that area erroneously.
- I tried a bunch of things, and settled on moving the kernel_stack pointer to kernel_stack_backup before writing any new value to
  it, and setting it to 0 when copying the value out of it into %sp.  I'm printing both pointers in the tty_process_input function,
  and it's always printing as 0, 0 for both.  There's no obvious sign of failure due to the stack pointer being 0 when it shouldn't
  be, and the backup is never not 0, which means it never overwrites a valid stack value.  Maybe it's still doing that since this
  print isn't catching every instance, but it's at least not happening on the most recent two entries into interrupt handler code
- I think that is likely to mean the problem is not with the stack pointer being invalid, at least not due to this.  That kind of
  points more to it being a memory addressing problem of some kind


2026-02-22:

- I'm trying to think how possibly could this behaviour occur.  I could imagine a whole bunch of memory issues with invalid pointers
  or indexing the wrong type to get the wrong memory location, but I don't suspect there are other processes running between these
  characters being read in, so it's likely there's a more limited set of possible causes
- it seems to happen in the tty code specifically, while the bottom half is running.  It might possibly be related to interrupts,
  but either way, it's as if the stack is being corrupted while these functions are being called, such that it can erroneously loop
- I put a single character printk into the loop of tty_process_input, and when I press one character, it's printing it 2 or 3 times,
  so it's definitely looping there, but I need to know in more locations and relative to each other
- I have ones in the serial irq handler, one on entry into tty_process_input, and another for each character ready by
  tty_process_input, and I press a char, and get irq -> bottom half -> entry -> char -> char, and then the character is echoed, so
  it's only entering the bottom half once, and looping twice
- I pressed enter (which sends two chars), and got some unexpected results.  It has two irq entries into handle_io_channel, then
  bottom half -> entering tty bottom half -> char, then there's a newline, which I think is the enter character that I pressed, and
  then bh -> tty bh entry -> char -> char -> char -> (echos `l` a second time) -> then bh again -> tty entry -> char
- what if it's the ring buffer getting corrupted, and when it goes to run the bottom half, there's more characters available than
  there should be?  I need to better test the irq vs the bh
- so I'm printng a '.' to the tty68681 buffer whenever it has found the status byte to say there's one char available.  When I press
  the first char, it prints 1 dot.  When I press a second char, it prints 3 dots.  That said, I don't know how accurate this is
  given it's using the buffered writer
- I switched to direct printing with tty_68681_putchar_direct, and it's still printing 1 and then 3 and then 1 and then 3, so once
  again (as was the case last time I was debugging at this point) it seems to be getting too many chars in this loop
- Ok, an `>` when entering the irq, a `x` if it was an error char, and a `.` if it put it into the buffer: same behaviour, 1 entry
  then 1 dot, 1 entry then 3 dots, etc, so it's not multiple irqs
- what if one of the other functions being called in the irq code is causing this, so I've disabled the ring buffer code that
  inserts the char, and commented out the TTY68681 bottom half running
- Aha!  Progress!  It's obviously not echoing or reading in chars, but I'm only getting 1 irq entry, 1 dot, each time I press a char
- I'll reenable the bottom half, even though a decent amount of stuff runs from that.  I'm sure it will cause the problem again
- Hmm... It doesn't.  That's odd.  I guess that's not entirely conclusive that it's not the bottom half involved
- disabled bottom half again, and re-enabled ring buffer.  Hmm... now it's happening, so it seems pretty certainly related to the
  ring buffer
- it seems like the only changes to ring buffer have been related to the addition of the iter functions, and nothing specifically
  related to the other functions.  I suppose I could comment out those ones to see if they're indirectly corrupting something
- that didn't seem to change anything.  It's still printing everything as expected, which is a bit odd to be honest, since I thought
  some user-requested calls to write are going through iter, which would have to copy to the ring buffer...
- I removed the volatiles from the fields and now it's working, but that doesn't make sense.  I'm not at all satisfied with this
  solution.  This seems sketch, like it's related to a compiler bug

- so it goes away when I remove the volatile keywords from the struct ringbuffer fields...  I have a feeling I had come across this
  problem before, years ago possibly, and added those volatiles to fix it back then.  I could imagine that adding them would fix
  such a problem, but I absolutely can't fathom why removing them might also fix this problem, unless it was actually a compiler bug
  of some kind
- Perfect!  It didn't fix it.  Adding an extra volatile var to tty_process_input caused it to break again, so it was just shifting
  the problem again.  Removing the volatiles must have changed the stack slightly so that the problem only occurs again once it's
  back to the special size where the fault occurs
- with the network stuff removed, there seems to only be the two serial channel buffers initialized?  That doesn't seem right.
  There's 4 buffers, so maybe it's not printing the first two because it's not initialized yet?
- I swapped rx and tx buffer inits for channel A, and it's still only printing two, which seems odd... threw a printk in, and it's
  printing that it completed preinit
- oh it's because the channel A ports wasn't initialized until after.  I printed the buffer addresses in preinit and they look ok
  a.rx 0x10DAC6, a.tx 0x10DCCC, b.rx 0x10DEE2, b.tx 0x10E0E8

- actually, the bottom half was still running, because the rx_ready flag was set, and the timer interrupt would have triggered it.
  I could tell because it was still echoing the letter entered.  I'll try disabling that, so the 68681 bottom half should still run,
  but it shouldn't schedule the tty_read_input (tty bottom half)
- and indeed that works, only one char entered.  It's also not printing the e and c debug chars, so really I should have noticed it
  earlier that it was still running that.  So it's adding it to the ring buffer, but not running the bottom half.  But this is so
  weird because the bottom half shouldn't run until after the interrupt is exiting, and by that point, the irq should have been
  serviced, so how could it have been looping 3 times within the handle_io function?
- also something I was thinking about was that irqs might still be happening while the bottom halves are running, which might be how
  the stack gets corrupted.  The kernel_stack_backup value is only checked/printed when a character is pressed, but the irq is
  entered multiple times for what should be quick events
- I put a check on the serial irq entry to set an LED if the kernel stack or kernel stack backup are not zero, but it's not setting
  the LED so I guess that's ok.  So that's unlikely to be the source of the corruption

- So back to debugging the bottom half and irq handler.  I have the 68681 bottom half enabled, but it's not triggering the tty
  bottom half, and I'm printing out the in/out numbers for the rx ring buffer using printk_buffered, and it's showing the correct in
  nummbers.  The out numbers aren't going down because the other bottom half is disabled
- I have the start of the bottom half for the tty enabled, including the loop and the reading of one char, and the results I'm
  seeing are a bit weird.  It's looping twice for each character entered.  I guess that's not *that* weird because it will read in
  one character, loop, and then try to read a second character to find there's none available, and exit.  The `c` debug chars are
  printed before calling dev_read().  There's only one dot printed
- without knowing why the stack size/contents triggers the problem, it's hard to know if it's still happening or not when it doesn't
  get triggered.  Is it not happening or has it just not been triggered because of some other code running?
- maybe, since the stack is almost always the same depth, and it seems to be after it calls the tty_68681_read function or write
  function, maybe I can print the stack location there, and use that to gauge whether to add or remove volatile vars to make it
  break again?
- in tty_68681_read, it's printing 0x11FF54 on entry
- adding the `continue` after the read, to cut off the rest of the loop, it's now printing 0x11FF60, so adding 3 more long words to
  the stack should get it to the breaking point?
- either the thing that causes the problem isn't running, or this crude approximation isn't correct.  I tried one less and one more
  than 3 and none of those are causing the problem.  How about uncommenting more code
- most of the function is uncommented, except for the ISIG block, but there's also a volatile var, and it's printing 0x11FF54, and
  also has the problem occurring, so I've tried to commenting out most of the big block at the bottom except for the echo, and now
  it's printing 0x11FF58 and it's not double-charring, so adding a var might make it fail?
- aha! there, adding one it's now printing 0x11FF54, and double charring with the three dots, so I think I can use the stack val
- ok, so I've commented out the write call.  It should still print our stack value, which is still 0x11FF54, but the problem is not
  occurring.  But I can't accept that it's "just the write" causing this.  Maybe without the write, the 0x11FF54 indicator is no
  longer accurate?
- what if I uncomment the write, and comment out the actual putchar in the write function?  It's still printing the debug stuff
  despite none of the user text showing, and it's still actually putting the chars in the buffer, and it's printing the stack as
  0x11FF54 (which I can't trust because it's in the read and not the write which has now been modified), but it's not doing the
  double char thing, and it's showing bus errors as if it's CoWing when I type commands, so the problem is not occurring
- I moved the stack print to the write, and tried it with the putchar enabled and disabled.  The problem occurs with it enabled.  It
  was printing 0x11FF58 outside of input (printing normal user text), but 0x11FF54 after a chars was input, when the problem
  occurred.  When putchar was disabled, it's printing 0x11FF5C and 0x11FF58, so maybe disabling putchar with an extra volatile var
  will cause the problem?  I'll put the var in the tty_process_input function instead of the write, just to be sure
- So now it's printing 0x11FF5C outside of input, and 0x11FF54 inside of it, which is a bit odd that it's different like that, but
  either way, the problem is not happening like this
- what if I move the var to the write function?  Ok, it's now 0x11FF58 and 0x11FF54 when it gets an input, so it should be the same,
  but it's not breaking (this is with tty_68681_putchar_buffered commented out).  So is it putchar that's causing the problem?
- the problem is occurring, but it's a bit different, and the stack values aren't the expected values because I didn't remove the
  var.  It's 0x11FF54 and 0x11FF50, and it has double chars, but doesn't print 3 dots, only 2 dots.  The first two chars inputted
  after an enter result in double chars, and then it's back to single chars from there
- I commented out the volatile var, and now it's behaving as before, where first char is 1 dot, second is 3 dots, alternating from
  there.  So it seems the problem occurs in two conditions and behaves a bit differently, depending very explicitly on the stack
  size

- there's not all the much in tty_68681_putchar_buffered.  The busy loop isn't triggered since I added an LED set op which isn't
  triggered, although now that I look at it, with that LOCK/UNLOCK, shouldn't the busy loop be outside the lock?
- with the _buf_put_char disabled, but the busy loop, locking, and enable transmitter command enabled, it's still got the problem
- hmmm... I moved the lock to after the busy loop and the problem went away.  This doesn't make sense...
- put it back to the way it was, and now I have buf put char and enable transmitter disabled, so it's just the busy loop and the
  locking left, and busy loop within the lock.  Problem is gone, and stack sizes look correct although that's no guarantee
- I tried 0, 1, 2, and 3 vars added to the putchar_buffered function, with the enable transmitter and putchar disabled, and the
  problem is not occurring
- with just the enable transmitter command commented out, it seems to be working
- with just the busy loop commented out, it's working (enable transmitter command is still there)
- and with the lock moved to after the busy loop (but everything else enabled), it's working
- that leads me to think it's not this, but elsewhere
- this function is a static inline, so what if I change that?  Seems to still be broken, so now I can inspect that function
- The enable transmitter command is the 3 asteriked instructions.  With them it fails, without it works, but it's not only that
  different that causes it to work/not work, so I don't know that it's these that are causing the problem
    ```
    106594:       3540 0208       movew %d0,%a2@(520)
   *106598:       206a 040c       moveal %a2@(1036),%a0
   *10659c:       2068 000c       moveal %a0@(12),%a0
   *1065a0:       10bc 0004       moveb #4,%a0@
    1065a4:       46ef 000e       movew %sp@(14),%sr
    ```
- I put in `*channel->ports->command = 0;` instead, which should be a no-op, and the problem is occurring now.  It's one instruction
  byte fewer than the other instruction that was causing this.  Neither the working nor broken versions seem to be differing in how
  much of the stack they use, so maybe once it's here, it's about the code size?
- the offsets look ridiculously big, of course, but that's because there's two 512 ring buffers in the struct being indexed
- what if I move the ports to before the ring buffer...  Still same behaviour
- tried moving the ring buffers to the end of the struct, added a byte of padding, none of that has fixed the problem, so not
  related to that struct or its layout I guess
- I compared the code bytes in memory after the problem occurs between the source, from 0x106500 to 0x1066F0, and there's no
  difference.  It's only a small chunk, so that's not definitive proof, but yeah
- bit of a hail mary but what if I remove the tx enable command from putchar, and put it into getchar below as a dummy 0 write?
  the problem doesn't occur, so it's not just a matter of the code size pushing it into breaking

- interesting... I changed the location of the stack by changing it in _start, and I tried also changing the location of kmem and
  pages, to be various places in the upper part of the 0x200000 addresses, and I got the same double characters behaviour in every
  case, so it would seem that it's not some interaction between pages memory/address or the stack location being modified by
  addresses outside of the code executing.  It does seem to be dependent on the stack size relative to other things on the stack,
  and there might be some interaction with global data somehow, or modified code.

- I stopped updating this for a while, I changed course and tried to get tracing working, which was easier than I thought.  It's
  still doing the problem with tracing enabled.  I can print everything, and see that it is indeed going through that serial loop 3
  times instead of 1.  I've copied the trace into a file and annotated it by hand, and then I added a comparison into the trace
  handler which checks for the status read instruction, and prints out the values of the regs at that point.  (The actual trace is
  just the first part, and I manually added the instructions in)
    ```
    Trace pc=106316 sp=11FFA4 (pc)=2069       moveal %a1@(8),%a0
    Trace pc=10631A sp=11FFA4 (pc)=1010       moveb %a0@,%d0
    Trace pc=10631C sp=11FFA4 (pc)=0800       btst #0,%d0
    a0=700003 d0=1
    Trace pc=106320 sp=11FFA4 (pc)=675C       beqs 10637e <handle_serial_irq+0xb6>
    Trace pc=106322 sp=11FFA4 (pc)=0C00       cmpib #15,%d0
    ...
    Trace pc=106316 sp=11FFA4 (pc)=2069       moveal %a1@(8),%a0
    Trace pc=10631A sp=11FFA4 (pc)=1010       moveb %a0@,%d0
    Trace pc=10631C sp=11FFA4 (pc)=0800       btst #0,%d0
    a0=700003 d0=1                                                                 
    Trace pc=106320 sp=11FFA4 (pc)=675C       beqs 10637e <handle_serial_irq+0xb6>
    Trace pc=106322 sp=11FFA4 (pc)=0C00       cmpib #15,%d0
    ...
    Trace pc=106316 sp=11FFA4 (pc)=2069       moveal %a1@(8),%a0
    Trace pc=10631A sp=11FFA4 (pc)=1010       moveb %a0@,%d0
    Trace pc=10631C sp=11FFA4 (pc)=0800       btst #0,%d0
    a0=700003 d0=1                                                                 
    Trace pc=106320 sp=11FFA4 (pc)=675C       beqs 10637e <handle_serial_irq+0xb6>
    Trace pc=106322 sp=11FFA4 (pc)=0C00       cmpib #15,%d0
    ...
    Trace pc=106316 sp=11FFA4 (pc)=2069       moveal %a1@(8),%a0
    Trace pc=10631A sp=11FFA4 (pc)=1010       moveb %a0@,%d0
    Trace pc=10631C sp=11FFA4 (pc)=0800       btst #0,%d0
    a0=700003 d0=0                                                                 
    Trace pc=106320 sp=11FFA4 (pc)=675C       beqs 10637e <handle_serial_irq+0xb6>
    Trace pc=10637E sp=11FFA4 (pc)=2F0E       movel %fp,%sp@-
    ```
- It really is reading in 1 as the status byte.  There are no instructions here that woulda account for this weird behaviour.  It's
  really reading in the wrong byte, or the ...  or the read byte is not getting executed?  But it is I think... (looks)
- I have to go to bed, so I can't check what the value it's reading in from that register is, but it's definitely reading from
  address 0x700007, which is the receive byte address... so it honestly seems like the serial port is malfunctioning.... or there's
  an electrical problem


2026-02-23:

- after talking about it with Helena, I mentioned that the slower speed seemed to fix it but I hadn't tested it since I replaced the
  serial controller.  She suggested I try it again, and I just did, and it works at 12.5MHz and not at 25MHz... So it's definitely a
  timing issue?  By why does a very specific stack size and such has this to happen?


2026-02-24:

- Aha!  I tried compiling it without MMU support, and it did work initially, but now that I know the stack thing that relates to it,
  and I still had the prints for the stack position, it was printing out 0x11FF5C, which means it should need 1 extra int on the
  stack to make the problem come back, and sure enough, adding an volatile var to tty_68681_write made the problem reoccur!  So it's
  not dependent on the MMU code, and I might actually be able to trigger it in the emulator, if I could get it working again.  It's
  been in pieces for like 2.5 years, in the middle of a refactor that I pretty much abandoned
- It also really seems to be stack related, even though I'm not sure how that might work.  Is it like an uninitialized variable on
  the stack that causes issues?  There's not much that explains why specifically the IRQ is affected though.  THIS IS SO WEIRD


2026-02-28:

- I finally got the emulator working (moa).  I had to recompile without 68030 support, and was able to confirm that it's still doing
  the wrong thing on the retroverse k30p with MC68010 enabled instead of MC68030.  In order to get the emulator working, I had to
  fix a bug in the decode for the EXTBL instruction, which was decoding to a LEA instruction incorrectly.  I also had to change the
  memory map addresses of course, since the k30 is different from the k30p.  I had to change the ATA memory map stuff since there's
  a slight difference in addresses between the onboard ATA in the k30 and the COMET card in retroverse.  I ended up recompiling the
  kernel with a different CONFIG_ATA_BASE address (0x600021).  I came across a problem with DIVUL where it was being interpreted as
  DIVS instead, and I haven't completely fixed the problem but I'm also working off an older commit because of the broken state it's
  currently in.  I was building on top of 9be996d2a1c8fed9f049109c97794601f176708b from March 2023.  Once I got it starting to boot,
  I ran into trouble with the disk image, because I was using the partition image that the build system generates rather than the
  whole disk image that includes the offset (1MB) and the partition table, but was able to grab the first 1MB from the raw disk
  image I've previously saved.  And then once the `init` process was created successfully, it was failing with an illegal
  instruction when restoring the context.  This was a MOVEC instruction that wasn't even decoded correctly in objdump, but which
  copied the USP register because the CONFIG_M68K_USER_MODE was still enabled.  I only had the VBR MOVEC instruction implemented,
  but the USP, SFC, and DFC registers are also supported on MC68010, so I added those to the emulator as well.  At that point it was
  finally booting and running the kernel again!
- so what happened?  It worked correctly.  I know it breaks (when recompiled with the alternate ATA address) on retroverse, and the
  same image (with the address changed) boots but does not have double chars when running in the emulator!
- I also tried pulling out the k30-SBC board last night, and couldn't get it working, so gave up, but took another look at it and
  read my notes previously, in computie's log.txt, and saw that connecting it to the monitor stand's USB port didn't work, but
  another USB got it working, so it was running fine after that
- I tried uploading the same image (with computie ATA) and... it worked fine!  No double characters!  I tried the same image just
  now (with COMET ATA) to retroverse and it's definitely got the double characters!  So it's positively a hardware problem on
  retroverse's k30p!  CONFIRMED!

- So where does that leave me?  Well it's likely something with the buslogic, or possibly something else like board damage from a
  previous short, but it's definitely not a software problem.  I'm a bit torn on direction.  I have the problem replicated well at
  the moment, but I also want to clean up what I have.  I could commit the mess that I have at the moment, and come back to it
  another time, or push through and try to figure out what the root cause it, but that will likely require a bunch of work with the
  analyzer and hooking up probes, and playing with verilog.


2026-03-12:

- I've been puttering along with this but also not getting much time to work on it.  I've mostly been trying to get the user
  copy/iter stuff working and improving the interface to it, while the CPU is still running at 12.5MHz to avoid that double serial
  problem.  At some point I'll get back to investigating that, but not until I get the MMU changes merged


2026-03-21:

- I'm trying to clean up the last bits of the iovec/iter stuff and convert the last few spots where I know there's an improper
  access to user memory addresses.  I fixed up most of the ones in exec.c and am fixing up the ones in context, which maniuplates
  the stack
- it works, but it does take more code size than I expected to have the iter for stack maniuplation, compared to the original code
  that just directly modifies the stack.
- I thought I'd try changing binaries.c to not preload, to test if it works now that the user stack manipulation has been patched,
  but it's constantly failing no matter what I do.  I've got it so that the only difference is using load_pages to init the kvec, or
  using a simple kvec that spans the whole fixed memory segment, and it fails when using load pages
- the segments in kvec look correct, so I dumped the contents of memory between the two versions, and I noticed that it looks like
  in the broken dump, the data is shifted one page up (ie. the data at 0x203700 seems to be loaded into 0x203800 in the broken
  dump).  I'm not sure how that could happen, but I also haven't tested the vfs_read function super well, with a kvec, so maybe it's
  from that?


2026-03-26:

- I *almost* have it working.  I fixed a bug with memory_map_load_pages where it didn't subtract the seg_offset from all of the
  lengths used, so it was exiting early and missing the last page
- I then came across an error where cat'ing /proc/3/stat or cmdline was printing garbage continuously.  There's two problems going
  on, it would seem.  One, which I haven't looked into yet, is why it isn't exiting when it gets an error return, and the other
  problem is that it's error-returning when it tries to load a page outside the current segment.  It's getting the command line from
  the stack, so it's right near the end, and fetching a page, or ever 32 bytes, will be beyond the end of stack in some cases (thus
  returning a no page error)
- a strnlen won't work either because it's user memory, and I need to load a page first.  Checking the segment size also requires a
  bunch of work, just to avoid hitting the end of stack

- I hackishly just rounded up the value, hoping that the arguments are not so long that they take up two pages of the stack instead
  of just the last page


2026-03-28:

- revisiting the 32k stack size, and it seems to be swapping back and forth between two pages, which are in pid 1 and pid 3.
  Neither process is advanced.  They're both stuck at the same code address.  I previously noted there were some large pages in the
  memory map, when I previously came across this problem, but that's not the case now, so it's not the cause.  All pages are 256
- oh maybe that is actually the case.  I was printing the memory map after the process was created, but at that point the stack
  isn't populated yet.  Printing it later shows many more entries, including a 14-bit page at the end for addresses 0x208000 to
  0x20BFFF (when the segment is 0x204200 to 0x20C200).  There are 256 byte pages for 0x20C000 and 0x20C100 though, and it's swapping
  out the last page, 0x20C100, so this doesn't really make sense
- actually maybe that's a table and not an early termination page descriptor...  It's not clear from the way it's printed
- Oh, there's a write protect bit set on the table, it seems.  There doesn't seem to be one set on the other tables.  I kind of
  wouldn't have expected that to be an issue, but maybe it is?
- that table is created without setting the write protect bit, so it must be set afterwards...  This bug might apply not only to the
  stack, but to anything where a segment beyond 16k is alloctated
- it definitely is during mmu_table_copy that the table's write protect bit is set, but it seems to be get_table where it's
  returning the table instead of pages because the segment length is long enough to include it all, and there's no check to see if
  it's a table or descriptor...
- I don't know what the solution here should be... It should be possible to make large pages I think
- it's this check that's passing: (bits <= PAGE_ADDR_BITS || (request_covers_whole_chunk && !(flags & GET_TABLE_RETURN_ANY_SIZE)))
  and that returns the table (ie. request_covers_whole_chunk is true which is how it returns), and then entries i=2 and i=3 are set,
  but i=3 on src table already has a subtable, which is ignored
- the problem is that the table entry is already set as a table entry, which then gets the WP bit set on it, but this is wrong...
  An easy way to fix it would be force everything to be a page, but if I want to support large pages, I need a better solution.  I
  feel like there's actually a bug here, as in it's not just an uncovered case, but this code is actually behaving incorrectly in
  other ways too.  That's just a hunch though
- I'm not passing in GET_TABLE_RETURN_ANY_SIZE so I don't think it should be returning early, but it is?!?  That's the bug, isn't
  it?
- only mmu_table_get_page seems to ever set GET_TABLE_RETURN_ANY_SIZE, but it's checking !GET_TABLE_RETURN_ANY_SIZE... it's like the
  logic here is backwards, and/or I'm missing a GET_TABLE_RETURN_ANY_SIZE in a get_table call


2026-03-29:

- well I found the problem with the stack size and GET_TABLE_RETURN_ANY_SIZE.  It's finding and returning an empty entry, but then
  it loops on that table and finds another table in the element after the one that get_table returned with, which is why the return
  conditions in get_table weren't affecting the results
- I'm not sure how I want to resolve this.  It might be harder to get large pages working, so maybe I should force small pages? But
  then it would still be able to error out if the two tables aren't fully populated, since I'm not passing
  GET_TABLE_CREATE_IF_NEEDED to the source table, but even if I pass that in, it would trip up if the mapping function added a large
  page somewhere, and that was copied in the copy function


2026-04-03:

- I keep uncovering bugs.  I was almost ready to commit the signals fix, which was to re-set the return value during the signal
  cleanup function, which required getting the signal number from the stack in order to compare the process's signal config, and I
  found a bunch of issues in the httpd demo
- pressing enter while it was running caused the syscall LED to flag repeatedly, until Ctrl-C was pressed, which then cause a crash
  if I tried also printing out the syscall (it was trying to run syscall 52, which is the `select` syscall
- I then tried changing the page size to 4k, since I had to rebuild the userland programs, and every process crashes during boot,
  starting with pid 3 (sh).  It doesn't even have a bus error, which seems odd.  I know it using fewer pages, so fewer page faults,
  but I still think it should have at least one page fault
- I had a bad sleep last night and am having trouble focusing.  I don't know where to even start looking
- oh the last run, it errored on pid 1 first instead (when I enabled the 0x40000 process starting address instead of the SEG_FIXED
  segment) and it fails with an 11 (F-line emulator), and then pid 2 (idle) crashes with 14 (format error), which implies some
  memory is not right, copied to the wrong location or something?

- it looks like the user stack is full of zeros or at least invalid data which then causes the rts at the end of the fork syscall
  stub in the user process, which has happened before, and it was caused by corruption to the user stack
- one possible explanation for that might be that it's not copy-on-write write protecting the pages, possibly due to changes I made
  recently to the get_table function.  I have no evidence yet of that though

- I re-enabled the non-FIXED program segments that instead use preallocated data, and it now errors on pid 1 at `_start`, so right
  away, even though the memory looks correct, it matches the init.elf binary, and the MMU table correctly points to the real memory
  page that contains that data, and the write protect bit is set for that segment, flags look right.  This is before any fork
  occurs.  It's only init.c which created the two first processes...

- actually, the address with the fault, the 11 f-line emulator fault, is executing 0x40160, which *is* in the table, but if the
  table is not set correctly... then address 0x40160 is an address in rom, which is filled with 0xFF (empty)


2026-04-04:

- back to this, and while the CPR register is set correctly, I'm not seeing the `mmu: tcr` lines that should print at the beginning.
  Is the MMU not enabled for some reason?  That would certainly explain why it's executing the wrong location in memory despite the
  tables being set up and definitely being in user mode when the exception occurs
- Oh!  yeah it failed during mmu init and didn't stop.  The problem happens before it enables the MMU, when it's setting up the
  tables for the kernel space.  I didn't notice the error message until now.  It's the I/O space that has the problem

- So yeah, the problem was that it was mapping too much.  It would find the table, and keep mapping until length was less than zero
  or you reached the end of the current table, but if the remaining length is less than the current table's entry length, it would
  still end up mapping instead of decending into a new table to map at a smaller granuale size
- I added an extra condition to the table lookup where if the length is less than the current granule size, it will also do another
  table lookup/create, and that seemed to fix the mapping issue
- it's now mapping correctly, from the looks of it
- after that, I had a fatal error when setting TCR which turned out to just be that the order in which the table sizes are used was
  backwards, so I was setting TCR to 0x82C0_00AA, where 00AA is TIA, TIB, TIC, TID, but the address resolution will stop when it
  finds the first 0, so it should actually be AA00 instead
- after fixing that problem, it would boot to the shell, but then interrupts would stop and it would hang

- I'm not sure what's causing the hanging issue now... It boots fine with 256 byte pages, but 4K pages won't run programs for some
  reason.  I'm not getting a fatal error, and the timer LED flicker has stopped, so I'm not sure where in memory it's executing

- I added a TRACE_ON() but that of course was printing a bunch of serial handler stuff, so I tried turning tracing off conditionally
  in the interrupt handler.  Now I'm getting the bottom halves running repeatedly, which might again be because of the tracing
  printing to the screen, so I might still not be getting a proper signal here


- oh that was interesting, I ran daemontest, killed it, and then ran it again, and it's now hitting the same bus error.  Maybe I
  broke it again with the added length check in mmu_table_map?


2026-04-05:

- I had trouble getting the same behaviour to happen with daemontest, but if I run threadtest first, which crashes back to the
  shell, and then run daemontest, it seems to happen again
  `bus error: i r fc=5 addr=218FBC ssw=B64D mmusr=202 pid=25 pc=10428E`
- with print statements, I can't get it to occur again...  This seems very odd

- back to the threadtest bug for a sec, what if I put a different function, maybe even the same entry point, onto the stack as the
  return address.  If it's the return address causing the problem, then it should repeat the thread infinitely, while all normal
  programs will still exit, since they will call exit()
- it definitely did something different, but I think I used the push_back function incorrectly, and passed in the entry and not the
  address of the entry
- So the first time, with `entry`, it tried to access `2F2F0004`, which was actually the value on the top of the stack, which itself
  seems wrong because it's the first longword in the page
- changing it to `&entry` then put `000003F8` on the stack, which I should note is already on the stack at offset 8 into the page
  (instead of offset 0).  Something definitely seems broken, but it doesn't make sense yet
- hmm... always printing the seg num and offset in arch_add_process_context is showing it as 4 always, which is wrong, as if the
  seek isn't working or something.  This definitely seems wrong
- Oh!  It does seem to actually be running the thread twice, but since it popped the return address once, it then crashes on the
  second run through, so yeah I'm pretty sure this problem is caused by the user exit.  That said, there also seems to be something
  wrong with how the stack is being set up or something...

- another look at the hanging when using 4k pages, and I was trying to get some indication of what's happening.  Setting the LEDs
  when leaving a syscall shows them lit when it hangs, but that doesn't cover interrupts.  I then tried printing the idle thread's
  stack but nothing printed when leaving a syscall, so I put in the schedule function, but there it's printing seemingly infinitely,
  and the stack looks wrong (0x20C000 with mostly random looking data except for a 0000 0000 as the second long word on the stack)
- I didn't let it run a long time, but maybe it's somehow/somewhy stuck in the scheduling loop?  That or the corrupt stack is the
  problem.  I could see this having to do with either the fact it's a kernel thread and the memory map works differently, or how the
  idle thread is created in the init.c code


2026-04-06:

- I don't know why putting something at the start of restore_context doesn't seem to get run... or maybe my condition here is wrong?
  I guess that's it.  It just never actually returns to process 2?
- I instantly know what's wrong after looking at the code that allocates the idle thread, more specifically alloc_kernel_stack which
  allocates and initializes a stack for a kernel thread such as the idle thread.  It's creating a stack that's one page in size, but
  the new exec_with_args function will try to make an iterator that's two pages in size.  I suspect that's failing somewhere in a
  silent way which is causing it to build an incorrect stack... this is just a hunch at this point
- ah yeah, since it's using load_pages, and that's looking at the map, but kernel threads are using the kernel memory map, which
  maps everything linearly, it won't error out when you try to create an iterator for two pages, when only one page is allocated...
  so the solution here is to either pass in the length, or know the length, because you otherwise can't just "check" the length
  generically for user processes, user threads (which allocate their stack on the heap), and kernel threads

- I spent too much time and effort convinced that was the problem, but increasing the stack size to two pages didn't fix the
  problem.  Not surprising given the code works fine with 256 byte pages.  It only hangs with 4k pages
- it definitely is a "wrong stack data" problem though.  Definitely the first return to the idle thread, pid 2, has 0x8BAB_0AEA as
  the top value, and I assume I don't have bus error enabled in the CPLD, so it's likely jumping there and then hanging
- it's definitely there after init, so it's not a corruption happening later.  It doesn't put the right value in the right spot
  in the first place

- Ah!  Part of the problem is that it's not checking and returning an error when exec_initialize_with_args() fails, so it doesn't
  stop when it should.  Now it's failing right away.  There's an error in exec and building the stack
- I had a bunch of things wrong there.  I was returning an error from copy_args but also returning an unsigned stack pointer value,
  and not even checking for the error result (which couldn't have been determined anyways).  I've changed it to return an offset,
  but I still don't like the function signature
- the error is happening in load_pages though, which is a bit odd since it's supposed to be the kernel map
- oh I was actually looking at memory_map_load_page_at instead of memory_map_load_pages_into_kvec, and the error came from there.
  At first I thought it was just a matter of moving the segment ops check inside, but actually it's failing when trying to fetch a
  given page for some reason.  I thought it was supposed to be the kernel memory map though, so I'm a bit confused why
  mmu_table_get_page would fail
- Oooooh! right, it's the large pages problem.  The segment from 0x0000_0000 to 0x0040_0000 has one large 22-bit page, and then from
  there to 0x0070_0000 has a sub table with individual pages.  But that still doesn't make sense why 256 byte pages would work and
  this doesn't...
- Double Oh! it's actually finding the page correctly, but the page address is 0! so it can't distinguish that from an error.  That
  also explains why 256 byte pages worked, because even with a large page, it probably had one with a different start address due to
  the smaller space covered by each top level page.  So this is really just an exceptional case


2026-04-07:

- Ok, so of course it was a bit more involved than that.  It was finding the page mapped to a certain address, but that page address
  was 0, so it was indistinguishable from an error.  I modified the mmu_table_get_page function to take a pointer to a special
  result struct, with a phys and size field, and an error int is returned instead
- there's only like 6 uses of get_page atm, most of which already took &page_size as an arg, so this is an improvement over what was
  there before.  The one place where &page_size was passed as NULL was a just printing out data anyways, so changing it to always
  return the size doesn't change the meaning
- Once that was done, it now crashes on pid 4.  I had the logging message all wrong in init.c, where it was supposed to print out
  the idle thread's stack just after creation, but it was using a non-sensical low number pointer.  Once I finally got that figured
  out, the stack it prints has 0x400020 as the return address on the kernel stack (also note the format is incorrect as well).  So
  that doesn't see quite right, since it's using the kernel memory map
- that said, there's also a fatal error further down
    ```
    bus error: d r fc=1 addr=0 ssw=751 mmusr=402 pid=4 pc=1006B6
    no seg
    fatal bus error: d r fc=1 addr=0 ssw=751 mmusr=402 pid=4 pc=1006B6
    217000 to 218000: flags=2051
    218000 to 219000: flags=2032
    21A000 to 21A000: flags=1033
    21A000 to 21C000: flags=1134


    Fatal Error at 1006B6 while pid 4 running (status: 2004, vector: 2). Halting...
    Stack: 11F330
    11F330: 2004 0010 06B6 B008 1EEE 0751 1081 4A01
    11F340: 0000 0000 0000 0000 0010 06F0 0E11 1000
    11F350: 0010 06BE 0010 06BC 0010 06BA 0011 F300
    11F360: 1000 0DB0 0000 F73B 1000 1081 0011 F3EA
    11F370: 0000 1000 0000 1000 0008 0000 0011 F390
    11F380: 1000 1081 1000 1081 0010 06AC 0010 06F0
    11F390: 0011 F3B4 0000 0000 0011 F3B4 0000 0000
    11F3A0: 0000 0400 0000 0000 0000 0000 0021 888C


    Code: 1006B6
    1006B6: 0E11 1000 1081 4A01 670A 5280 0C80 0000
    1006C6: 0400 66E0 4E75 4FEF F3E8 48E7 3022 4DEF
    1006D6: 0010 4878 0400 42A7 2F0E 4EB9 0010 B704
    ```

- interestingly, that address is the moves instruction inside strncpy_from_user() function, so it's a page fault in kernel space
  being treated as a fatal error?  Did I fix something?  "fix" which took away a "feature" (bug) which allowed page faults to occur
  inside the kernel without causing a panic?
- well more acturately, page faults can happen in the kernel, but currently if they fail inside the kernel, it calls fatal_error().
  It doesn't have to though.  It could instead do something to indicate an error.  Or I'd have to look at what Linux does
- I'm pretty sure Linux has versions of page_fault functions that are labelled _nofault, but I think there's also just some kind of
  guard added before calling a func

- Oh darn, this change does actually change the meaning of the get_page function because it was previously returning a NULL address
  based on the table entry to indicate the page isn't mapped, which might explain why it's broken now.  It needs to more
  intelligently figure out it the address should be 0, or if the page should be considered unmapped


2026-04-08:

- It was a mistake in converting the get_pages() calls to use a result pointer, where I had previously checked `if (existing_page)`
  in load_page_at, which then does CoW if it exists, but I converted it to an `if (error < 0)` which is the opposite meaning.
  Changing that fixed the weird fatal errors I was getting above
- so now it's back to the 4k page issue, and I noticed that my debugging of the idle task stacks was incorrect.  I thought it had
  address 0x400020 as the stack exception return address, but that's actually the context.  It's 0x0000_0040 as the context size,
  and then 0x0020_2CDC as the last register value pushed.  The actual return address is only visible if I print more of the stack,
  which is 0x103E7C, which seems correct even though I haven't double checked it

- well, there's a bug when running ps, but it actually boots and can run ls when I use 4k pages now, after I changed the get_pages
  function to return an error and explicitly check that if it's not a valid mapping for address 0, it will return an error, but not
  if it's valid (previously it was always signalling an error, even if it was a vald mapping to address 0)

- looking into the ps bug, which gets the wrong address to proc argv:
    ```
    setting new page 236000 for 22E000
    free page 222000
    CoW page set
    PID     SIZE    STATE   PARENT  PGID    CMD
    proc argv 20CFEC
    FEC: 4A01 66EC 7000 241F 4E75 7001 60F8 70FF
    FFC: 60F4 0000 598F 206F 0008 4297 2017 4A30
    100C: 0800 6606 2017 588F 4E75 2017 5280 2E80
    101C: 60EA 0000 2F0A 206F 0008 246F 000C 2248
    102C: 2208 D2AF 0010 2001 9089 4A80 6E06 2008
    103C: 245F 4E75 1012 6702 528A 12C0 60E8 0000
    104C: 2F03 2F02 206F 000C 226F 0010 4280 4281
    105C: 3008 3209 2408 4242 4842 84C1 3602 4843

    proc argv[0] 4A0166EC
    failed get_page at 4A016000
    tried to get 4A016000
    no seg
    no page no load
    Error reading /proc/2/stat (-2)
    ```
- it's getting the wrong value for the stack contents, and the stack dump I added shows the problem.  It's getting 0 as the address
  rathen than the page at 20CFEC, and it's caused by load_pages_into_kvec using the wrong address for the page start, since it
  assumes it's a PAGE_SIZE granule, but it's not.  It's much larger
- get_pages does an adjustment of the address for the granule size, and that might even have been what caused my problems when
  trying to support larger granule swapping, but it's definitely a problem here.  I can possibly adjust my uses of get_pages to use
  the granule size result better, but it's also a gotcha, and should be fixed better


2026-04-09:

- I think I fixed the actual fatal exceptions I was having.  One, by changing load_pages_into_kvec to wait until receiving the
  size to do all the calculations it needs, and two, by adding checks in procfs to not deref the map if it's NULL
- I haven't fixed the actual cause of the error, which is having a dead zombie process open still
- also I might not have fixed the issue with the get_page() function returning the adjusted page start and not the actual page
  start address

- So the problem with cleanup and that process that doesn't die but stays as a zombie.  It's the child process of threadtest (the
  thread that's created), and it (pid 13)  correctly gets assigned parent pid 1 after pid 12 (it's true parent) exits, but it
  doesn't actually get cleaned up.  This is because the init process is not unblocked/woken to run waitpid
- Interestingly enough, the daemontest process was also still around.  I guess I had forgotten to kill it, or else the first kill
  attempt didn't work for some reason (ah yes, I gave the wrong pid so it just didn't kil it).  After sending kill to the daemontest
  process, it then reaped the threadtest's thread proc right after that.  So the problem is *just* that it didn't wake up the init
  process after re-assigning parents


2026-04-10:

- I made close_proc return the number of orphans created by the closure, and then called resume_waiting_parents() from the scheduler
  code, just so it's not interdependent.  Now the zombie processes is reaped after the parent exits and the zombie is gone by the
  time I run ps afterwards
- Oh that's interesting, I sent a signal number of 0 to ... an unspecified process?  (`kill -` was the command I ran).  And then It
  segfaulted essentially.  Tried to access address 0
- Ah ok, that made me think it was a kernel bug, but actually the exception occurs in user space, and is caused by passing the
  non-existant pid argument to strtol, because it's missing an arg check.  Adding the arg check should be enough to fix the bug in
  the command itself

- found a new bug when running nettest while trying to test killing a custom handler process that isn't the currently running
  process.  It seems like just running nettest causes the fault
      ```
      bus error: d w fc=1 addr=11FEC8 ssw=711 mmusr=402 pid=13 pc=100E4E
      fatal bus error: d w fc=1 addr=11FEC8 ssw=711 mmusr=402 pid=13 pc=100E4E
      Fatal Error at 100E4E while pid 13 running (status: 2704, vector: 2). Halting...
      ```
- the address of the fault points to memcpy_to_user, which is called from 0x101654, which is in do_recvfrom!  So an error there.
  It's passing in a kernel space address in place of the user space address

- looking into the _user_exit, function arg to clone, thread exit problem.  Looking at the user-side clone impl for uClinux, it
  looks like there's a thread stub in the function, and that the actual clone syscall acts like fork, in that it returns twice to
  the user process.  The stub expects the `fn` and `arg` arguments to be passed to it, but those can be pushed onto the user stack
  itself, and with the new stack used by the child lightweight process/thread, it can't return without crashing, but it doesn't need
  to because it jumps to the stub, calls the function, and then calls the exit syscall


2026-04-11:

- I had to move the stack copying code into context.c, which is where it should be anyways, really, since the way it copies the
  stack is somewhat platform dependent.  I got it working such that the user stack passed into clone() has the `fn` and `arg`
  arguments (I had forgotten to put them on the stack after adjusting the stack before the syscall, which took me a while to figure
  out, but I got it in the end)
- I realized when doing that that the kernel context function that was in context.c wasn't being used by anything, and while I had
  probably intended for it to be, it seems that the old exec.c functions that used to be split, the kernel version didn't push a
  kernel context... Maybe that was even a bug I came across where it wouldn't work correctly.  I've removed the function for now and
  can come back to trying to support kernel threads later.  The only "kernel thread" I have is the idle thread, which runs as a
  user process.  The fact that a kernel thread can't have a user stack is part of the problem, given the context switching assumes
  it will always either return to the kernel or return to a user thread

- I played around with the syscall macros a bit, since I noticed there was some stack use in the clone() stub after it returned from
  the syscall.  I modified the stub to make it unlikely that it would use the stack, which would be a no no for threads, since they
  share the same memory address and could inadvertently mess up its parent process.  I had it separately copying the syscall return
  into errno, and also into a local variable.  I was able to get rid of the local by using errno as the intermediary, which works,
  but might not be 100% correct, and it still can't seem to optimize as much as I feel like it should be able to.  I'm pretty
  unimpressed with the gcc optimization for 68k, but I guess it doesn't get much attention these days, and changes to other parts of
  gcc could regress it

- I might have found another bug, but they're pretty few are far between now.  It's hard to get it to try to allocate large pages,
  as it turns out, but I can both increase the stack size and change it to POPULATE instead of UNALLOCATED, and it will try to
  allocate 3 14-bit pages in 256-byte page mode, and fails with an error -14
- ah, it's hitting the error in mmu_table_copy where the bits requested of the source and destination don't match

- for this large pages issue, if I add GET_TABLE_RETURN_ANY_SIZE to the get_table call in mmu_table_copy, it will get into an
  infinite loop trying to swap out pages the same page over and over (which is likely because the parent table has the write protect
  bit set when it shouldn't).  If I don't have that flag (which is how it was before finding this bug), it will hit the check to see
  that the source and destination pages are the same bit size
- So I think this is one of the main things preventing large pages.  I could possibly fix this, but to do so I have a strong feeling
  I'll need to adjust the page allocator to optionally allocate contiguous chunks on a large page boundary
- the alternative of course is to abandon large page support for now and make it force single page size when using the PREALLOCATED
  flag when mapping segments

- it sounds like linux's hugepage support is a later addition, and isn't always activated.  It says it's to try to reduce the number
  of TLB entries, but it sounds like the 68030 actually makes an individual ATC entry for each page despite the contiguous chunk.
  The large pages also need to be aligned to the nearest large page boundary, since it mentions n-bits of the address being ignored
- this is making me think maybe I shouldn't support this now, since it would complicate a lot.  So all pages that could be swapped
  out would be single pages, and only SEG_FIXED segments could map to large pages

- it looks like changing just the mmu_table_map function to not pass in the GET_TABLE_RETURN_ANY_SIZE flag unless the
  MMU_FLAG_WINDOW flag is passed in seems to have fixed the problem with some large pages being swapped in


2026-04-14:

- I got the last bug in the signal handler stuff working.  It was failing when CONFIG_M68K_USER_MODE was not enabled, because in
  that case the stack is shared.  It wouldn't put the arguments on the stack correctly, and when removing the context, it wasn't
  popping the user args off the stack.  I tried setting the stack and then getting it back again, but that caused usermode/mmu
  enabled configs to break, since the usp was being changed before the context was saved.  Adding more #ifdefs and being careful
  with the order was all it took to get it working under all 3 configs
- I'm really close to being done here.  I'd like to do that kmalloc change to use pages, and I would *really* like to make the
  bufcache use pages too, but that is less critical

