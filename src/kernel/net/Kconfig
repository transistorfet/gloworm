
menu "Networking support"

config NET
	bool "Enable networking"
	default y
	help
	  Include the networking subsystem
if NET

config INETV4
	bool "TCP/IP v4 networking"
	default y
	help
	  Include protocol decoders for IPv4, UDPv4, TCPv4, and ICMP

source "src/kernel/drivers/net/Kconfig"

endif

endmenu

